# 予定管理アプリ 引き継ぎ書

最終更新: 2026-08-21（⚡レスポンス改善＝アプリを開く時間 9.7秒→3.9秒／保存の待ち時間ゼロ化。アーカイブ410件実行済み。**⚠️別アカウントからのデプロイで本番を約20分停止させた事故あり＝下記「5. デプロイ手順」を必読**）
前回: 2026-08-07（🧹トークン節約＝セッション記録12本を [[自社用/予定管理アプリ作成/docs/引き継ぎ_詳細|docs/引き継ぎ_詳細]] へ移動 18,425→4,658トークン。やりかけ＝きんでん事務タブ統合と毎月の運用は「6. 既知の課題」に残置）
機能面の前回: 2026-07-13（🚀集計の接続エラー修正＝書式一括化で185秒→約35秒・GAS v45本番反映済み。gas.jsは**未コミット**。⚠️合言葉の公開リポ漏えい問題が引き続き未対応）
プロジェクト: 予定管理アプリ作成
リポジトリ: github.com/akirasp4-lgtm/sinkarenda-
本番URL: GitHub Pages（index.html / admin.html / president.html）
※ファイルは**リポジトリ・ローカルともルート直下**で運用（`index.html` / `admin.html` / `gas.js`）。本ファイル下部「1. システム構成」の `deploy/` パスは旧構成の名残りなので注意。

> ⚖️ **2026-06-11 グループ決定（GR-BP2側で確定）＝お金は GR管理画面「📊経営タブ」に一本化**。
> このアプリの**金額機能（💰請求計算＝人工×単価・元請別請求集計・きんでん請求書ジェネレーター等）は“未使用”と利用者が確認 → 当面「寝かせる」**（消さない・無害・後で起こせる）。
> ✅ **非表示化は本番反映済み（2026-06-11 commit `9e8c427`）**: admin.html の 💰請求計算タブと工番モーダルの売上(円)欄を `display:none` で隠し、「金額はGR経営タブへ」の案内を追加。コードは全部温存＝復活は display:none を外すだけ。**⚠️復活時の注意: タブボタンは消さず必ず「隠す」**（switchTabがボタンの並び順で動くため、消すと他タブが壊れる）。
> 🔒 **現場画面の給料漏れ口も閉鎖済み（2026-06-11 commit `262d85a`）**: index.html（PINロック無し・全社員が使う）の事務タブCSVダウンロードに**職人マスタ（単価列＝給料情報入り）**と元請別請求集計が並んでいた → プルダウンからコメントアウトで除外。admin.html（PIN付き）からは従来どおりDL可。⚠️GAS(get_sheet)は今も単価込みで配るため、**本物の日当を入れる日に配信側の改修もセットで**（詳細は仕訳マップの「4点セット」）。
> 💤 スプレッドシート「事業部別按分」シート（人工×単価→原価・粗利の自動計算 `generateDivisionAllocation_`）も休止扱い。**今入っている数字はテストの残骸＝本番データではない**（2026-06-11 利用者確認）。計算エンジン自体は完成品なので、将来の「人工→原価の橋」はこれを繋ぐだけでよい。
> このアプリは **予定・現場カレンダー・人工(出面)の記録に専念**。将来、人工(出面)データを GR経営タブの「原価/売上」に自動で流す“橋”を架ける案あり（常用・日当が多い場合のみ着手）。詳細は `GR Business Platform２/引き継ぎ.md` の「直近のセッション 2026-06-11」。
> 🗺️ **機能の持ち場の正典** ＝ `GR Business Platform２/docs/機能仕訳マップ_予定管理とGR管理画面.md`（**「工番事業部の修正」はGR管理画面へ移設予定（候補#1）**。着手時はマップの注意点＝橋＋Notionダブり対策を必読）。

---

> 過去のセッション記録12本（2026-05-14 〜 2026-07-13）は全文を **[[自社用/予定管理アプリ作成/docs/引き継ぎ_詳細|docs/引き継ぎ_詳細]]** へ移動した（2026-08-06・トークン節約のため。**中身は消していない**）。やりかけの仕事と毎月使う運用手順は「6. 既知の課題・残課題」に残してある。

---

## 1. システム構成

### フロントエンド（GitHub Pages）
- `deploy/github-pages/index.html` — 職人用（予定入力・カレンダー）
- `deploy/github-pages/admin.html` — 管理者用（現場管理・集計・各種ツール）
- `deploy/github-pages/president.html` — 社長用（読み取り専用カレンダー）
- `deploy/github-pages/links.html` — 入口（3ボタン）
- `deploy/github-pages/manifest.json` — PWA設定

### バックエンド（Google Apps Script）
- `deploy/gas/gas.js` — 約1900行
- スプレッドシート: 日報・アーカイブ・現場マスタ・元請マスタ・社長予定・操作ログ・車両予約
- **現在のデプロイ版: v31（2026/05/16 現場マスタ 1 行削除機能追加）**
- WebApp URL（変更なし）:
  `https://script.google.com/macros/s/AKfycbxp2eUcpIjCj0ZWyAPPD9m3egJrKdWmXRK2AVnFrmBm4iO1QHCk-FZEH5LFFv7OloqcjQ/exec`

---

## 2. 主な機能

### 共通
- 会社別表示（GRHD / グローライズ自社 / 等）
- URLパラメータ `?c=会社名` または localStorage で会社固定
- カレンダー: **月曜始まり**（`(getDay()+6)%7`）
- 19列ヘッダ（車両カラム追加済み）

### 職人用 index.html
- 予定登録・編集・削除
- **作業区分ゲート**: 「作業区分」を最初に選ばないと他フィールドが触れない（赤字で案内）
- **作業区分**: ⛑️現場作業 / 倉庫 / 休み / 撤去品返却（現場作業のみ工番発行）
- **現場名は `<select>` プルダウン**（iOS Safariで `<datalist>` が出ないため）
- 新規現場登録時は類似現場サジェストを表示
- 車両選択（グローライズ自社のみ、10台のうち会社所有分のみ表示）
- 被りチェック（saveEdit / submitNippo 両方）
- PWA: インストールバナー / シェア / PTR / 触覚フィードバック
- モバイルではリスト表示モード（@media max-width:600px）

### 管理者用 admin.html
- 現場管理・元請管理・月別集計
- 工番修正モーダル（検索付き）
- データアーカイブ
- 工番事業部の修正
- おかしい工番をクリア（`cleanupOrphanJobnos_`）
- **🔀 元請名をマージ** ← 最新追加（commit `7dffb8c`）

### 社長用 president.html
- コンパクトカレンダー（セル62px、月曜始まり）
- chip表示は `title || 時刻` のタイトル優先
- min-width:0; overflow:hidden でセルはみ出し対策

---

## 3. 最新の変更点（直近セッション分）

### 3.0 レスポンス改善（2026-08-21）

**きっかけ**: 利用者から「反応が遅すぎてストレス」。実測したら**1回のデータ読み込みに9.1〜14.6秒**、
毎回1.22MB・2,607行を全部送っていた（見ているのは1ヶ月分）。

**原因は3つ**
1. `doGet` が全行を組み立ててから会社で捨てていた（期間で絞る仕組みも無し）
2. 登録/編集/削除のたびに `await loadData()` で全件読み直し＝**1操作で書き込み+全件読込の二重待ち**
3. `Utilities.formatDate` を 2,600行×3箇所＝**約8,000回**呼んでいた（GASで特に重い命令）

**やったこと**
- gas.js: `fmtDate_`/`fmtTime_`/`fmtDateTime_` を追加（TZ一致を実行時に検査し、ズレたら従来へ自動フォールバック）
- gas.js: `doGet` を「会社・期間で絞ってから組み立て」に変更。`dateFrom`/`dateTo` 対応（未指定なら全件＝後方互換）
- gas.js: `?compact=1` で行を「値だけの配列」で返す形式を追加（項目名は headers で1回だけ）。
  **未指定なら従来形式**なので、画面とGASのどちらが古くても壊れない
- index.html / admin.html: 保存後の `await loadData()` を廃止。`rerenderAll` / `removeLocalByIds` /
  `addLocalRows` / `refreshInBackground` を追加し、画面を先に更新してサーバ照合は裏で走らせる
- gas.js: `archiveOldData_` の1行ずつ `appendRow`/`deleteRow` を**一括処理**へ（setValues 1回＋連続ブロックの deleteRows）
- gas.js: アーカイブのトリガーを月1回→**毎週日曜3時**。`checkTriggers()` を追加

**結果（実ブラウザで同じ測り方）**

| | 修正前 | 修正後 |
|---|---|---|
| アプリを開く | 平均 **9.7秒** | 平均 **3.9秒** |
| 保存・編集・削除の待ち | 約10秒 | **ほぼゼロ** |

**アーカイブについて（重要）**
- 「動いていなかった」わけではなかった。データが 2026-05-02 始まりなので、
  前回トリガー日 2026-08-01 時点では3ヶ月より古い行が**0件**＝消す物が無かっただけ。
- **本当に危なかったのは実装**。1行ずつ処理は数百行で6分の実行上限に達して落ちる作りで、
  9/1に634行、10/1に1,322行が対象になる＝**初回で失敗して以後ずっと溜まり続けるコース**だった。
- 2026-08-21に一括処理版で **410行を実行済み**（全社 2,991→2,581行、最古 2026-05-02→2026-05-22）。本番で正常動作を実証。

**バックアップ**: 実行前の全社2,991行を `_local/backup_20260821/` に保存。

**未反映**: `compact=1` のGAS側デプロイ（画面側は反映済み・急ぎではない。押し忘れても壊れない設計）。


### 3.1 工番重複対策（案A＋C）
**問題**: 「○○現場 材料引取」のような表記揺れで同じ現場に複数工番が振られる。

**対策**:
1. **案A: 作業区分ゲート** — 現場作業の時だけ工番発行
2. **案C: 現場名プルダウン** — 既存現場から選ぶ／新規は明示
3. **管理側マージ機能** — 既存の重複を統合できる

GAS側条件:
```js
row.company === GROWISE
  && !row.souko && !row.yotei && !row.yasumi
  && row.workType === '現場作業'
```

### 3.2 元請マージ機能
**ファイル**: gas.js + admin.html
**GAS action**: `merge_genba`
**動作**: 日報・アーカイブ・現場マスタ・元請マスタの「元請名」をfrom→toに置換、from側マスタは削除（重複なら）

**UI（admin.html 事務タブ）**:
- 青背景の「🔀 元請名を統合（マージ）」ブロック
- 「元請名をマージする」ボタン → モーダル表示
- 統合元・残す側のドロップダウン
- プレビュー（影響件数表示）
- 確認ダイアログ後に実行

**実行済みマージ**: グローライズ → グローライズ自社（日報側0件、マスタ側は重複削除）

### 3.3 撤去品返却
作業区分に追加。工番は発行されない。

### 3.4 社長予定表
- 「時間しか見えない」問題 → タイトル優先表示に変更
- セルはみ出し → min-width:0; overflow:hidden で抑制
- 月曜始まりに統一

### 3.5 車両予約 GAS連携（ラインボット向け）
返信ファイル: `ラインボットへの返信.md`
- 4アクション実装: `vehicle_res_add` / `update` / `delete` / `list`
- トークン: `車両予約用トークン1234`（`VEHICLE_RES_TOKEN`）
- シート「車両予約」14列（自動作成）
- 既存カレンダー機能には一切手を入れていない
- **Phase 2（カレンダーに車両予約表示）は未実装**

---

## 4. 重要なコード構造

### gas.js
- `HEADERS` — 19列（車両カラム含む）
- `doPost(e)` — actionで分岐
  - 既存: add / update / delete / archive / summarize / get_sheet / pres_*
  - 追加: `vehicle_res_*` / `cleanup_orphan_jobnos` / `merge_genba`
- `cleanupOrphanJobnos_(ss)` — 休み・倉庫・予定・現場作業以外の工番を一括クリア
- `mergeGenba_(ss, fromName, toName)` — 元請名統合
- `logOperation_` — 操作ログに記録

### index.html / admin.html 共通の重要関数
- `applyWorkTypeLock(p)` — 作業区分ゲート
- `applyYasumiLock(p, clearValues)` — 休み・倉庫モード時のロック
- `populateLocationSelect(p)` / `onLocationSelect(p)` — 現場名セレクト
- `showSimilarSuggest(p)` / `pickSimilarLoc(p, loc)` — 類似現場サジェスト
- `getCompanyVehicles()` — 会社別車両絞り込み

### admin.html マージ機能
- `openMergeGenbaModal()` — モーダル開いてドロップダウン構築
- `updateMergePreview()` — 影響件数プレビュー
- `runMergeGenba()` — 確認後にGAS呼び出し

---

## 5. デプロイ手順

### GitHub Pages
```bash
git add deploy/github-pages/
git commit -m "..."
git push
```
自動デプロイ（数十秒〜数分で反映）

### GAS

> 🚨 **2026-08-21 事故: 別アカウントから `clasp deploy` して本番を約20分停止させた。**
> このWebアプリは appsscript.json が `executeAs: USER_DEPLOYING`＝**「デプロイした人の権限で動く」**設定。
> オーナー以外（例 miscjigyoubu@gmail.com）からデプロイすると、**URLは変わらないのに実行者だけがすり替わり**、
> そのアカウントは権限承認をしていないため**利用者全員に「アクセスが拒否されました」**が返る。
> **バージョンを戻しても直らない**（原因がコードではなく実行者のため）。オーナーからデプロイし直す以外に復旧方法はない。

**■ 鉄則: 最後のデプロイボタンは必ずオーナー `yotei.glorise@gmail.com` が押す。**
**AIや別アカウントは `clasp push`（エディタへ保存するだけ・公開版は変わらない）までしかやらない。**

手順:
1. `clasp push` でコードを送る（安全。利用者への影響ゼロ）
   → 作業フォルダは `_local/gas_push/`（詳細はそこの README.md）
   → プロジェクト直下で clasp push すると index.html 等まで GAS へ送られるので厳禁
2. **オーナーアカウント**で Apps Script エディタを開く
3. 「デプロイ」→「デプロイを管理」→ **既存デプロイ** `AKfycbxp2e…` の鉛筆アイコン
   → バージョンで「新バージョン」→「デプロイ」
   ⚠️「新しいデプロイ」を作るとURLが変わり全員繋がらなくなる。必ず既存を編集する。
4. 切り戻しも同じ画面でバージョンを選び直す（**オーナーアカウントから**）

**アカウント**
- オーナー: `yotei.glorise@gmail.com`（スプレッドシート所有者＝デプロイ担当）
- clasp: `miscjigyoubu@gmail.com`（2026-08-21に編集者権限を付与。push専用）

**注意**: Monaco editorは日本語文字列の直接送信でブロックされることあり。`String.fromCharCode(0x...)` を使う。

### ⚠️ セキュリティ（未対応）
スプレッドシートが **「リンクを知っている全員が閲覧可」**。中に**職人マスタ（単価＝給料情報）**が入っている。
URLを知られれば誰でも読める状態。アプリはGASが代理で読むので、リンク共有をオフにしても職人さんの画面は動く。

---

## 6. 既知の課題・残課題

### 残課題（依頼があれば）
- **Phase 2（車両予約のカレンダー表示）** — ラインボットからの依頼。Phase 1完了でOKとの指示で保留中
- **Service Worker / オフライン対応** — PWA本格化案として説明済み、未実装
- **起動の遅さ** — 改善プラン提示済み（メリット・デメリット説明）

### 🚧 やりかけ・毎月使う運用（2026-05-30 のセッションから救出）

**① きんでん請求書を事務タブから出す統合＝未完**
- **現状**: 🟡 **「きんでん請求書を事務タブから自分で出せるように」を設計＋実装プラン化まで完了**。チーム調査でGAS方式の核を裏取り（safe_with_mitigations）。**本番実装は未着手**（＝下記ブロッカーのため見切り発車せず）。きんでん請求は**今すぐはローカルPython（依頼ベース）でそのまま出せる**。
- **次の作業場所**: `docs/superpowers/plans/2026-05-30-kinden-jimu-tab-integration.md` の **Task 0（実機検証ゲート）**から。
- ⛔ **ブロッカー**：(1) **clasp が別アカウントに戻り本番(予定管理)へデプロイ不可** → 帰宅後 `clasp login` で予定管理オーナーに入り直しが必要（ブラウザ同意・代行不可）。(2) きんでん機密を公開テストendpointに送る検証はガードレールが正しく拒否。

**② きんでん西 指定請求書ジェネレーター＝完成・毎月使う**
- **現状**: ✅ **きんでん西 指定請求書ジェネレーター 完成（6局登録済み・自動テスト17件PASS）**。きんでん以外は前出の「現場ごと請求」のまま。
- **次の作業場所**: 来月分は局名＋きんでん指示の「当月迄出来高」を渡せば生成できる。請求書の年月の扱いだけ初回提出で要確認（下記）。
- ⚠️ **固定値が局ごとに違う**：**法定保険料率**（F95系＝16.36% / 4G系（アムール等）＝**15.765%**）、**工番の頭**（F95 / **F96**）、工事名・施工場所・注文金額・労務費も各局固有。

> 経緯の全文は [[自社用/予定管理アプリ作成/docs/引き継ぎ_詳細|docs/引き継ぎ_詳細]]。


### 注意点
- iOS Safariは `<datalist>` が出ない → `<select>` を使う
- body の `overflow:hidden` ＋ `.screen{height:100vh;overflow-y:auto}` でタブバー固定（iOS）
- PC表示でセンタリングは max-width:1000px + margin:0 auto を `.screen` に
- 日報の元請変更・現場変更は表記揺れに注意。マージ機能で吸収可能

---

## 7. ファイル一覧（主要）

```
予定管理アプリ作成/
├── 引き継ぎ.md                          ← このファイル
├── ラインボットへの返信.md              ← 車両予約GAS連携の完了報告
├── deploy/
│   ├── gas/
│   │   └── gas.js                        ← GASバックエンド（約1900行）
│   └── github-pages/
│       ├── index.html                    ← 職人用（約2700行）
│       ├── admin.html                    ← 管理者用（約2400行）
│       ├── president.html                ← 社長用（約830行）
│       ├── links.html                    ← 入口
│       └── manifest.json                 ← PWA
```

---

## 8. 直近のコミット履歴（要約）

- `d332b05` — 現場マスタから 1 現場を削除する機能を追加（2026-05-16）
- `3d7fd5d` — 現場名マージ：A/B 選択 → どっちに合わせる？ボタン式UIに変更（2026-05-16）
- `ccc4374` — 現場名マージ機能を追加（表記揺れ統合）（2026-05-16）
- `121cca4` — fix: 完了切替後にページ更新しないと反映されない問題を修正（2026-05-16）
- `a3aba99` — 現場の「完了」フラグ追加：プルダウン非表示／工番は不変（2026-05-16）
- `10c29a3` — アプリ内ヘルプを最新機能で更新（2026-05-14）
- `85b84c9` — CSV廃止→xlsx一本化＋合計セルに SUM 関数を入れる（2026-05-14）
- `31366a8` — fix: 期間指定xlsxで列固定とセル結合が競合するバグを修正（2026-05-14）
- `e0863e6` — 見た目そのまま出力: xlsx エクスポート（全期間 + 期間指定）（2026-05-14）
- `101c7da` — CSV ダウンロードに期間フィルタ＋期間集計CSV を追加（2026-05-14）
- `af2c1bb` — リポジトリ運用ルールを .gitignore に明文化（2026-05-14）
- `48f091e` — gas.js を本番デプロイ状態に同期＋月別確認表ヘッダの日付バグ修正（2026-05-14）
- `3da3c5e` — 現場名選択にリアルタイム検索＋最近使った順ソートを追加（2026-05-14）
- `7dffb8c` — 元請名マージのadmin UI追加
- それ以前 — マージGAS実装、撤去品返却追加、工番ゲート、現場名プルダウン化、社長予定表のレイアウト修正、車両予約GAS連携 等

---

## 9. ユーザー固有の方針メモ

- 「職人が入力ルールを守らない」ので、UI側で**強制的に正しい入力に誘導**する方針
- 既存カレンダー機能には**勝手に手を入れない**（独立ブロックで追加）
- 重要操作は**確認ダイアログ必須**（マージ等は取り消し不可なので特に）
- モバイル（iOS Safari）を最優先のターゲットとする
- 表示優先順: タイトル > 時刻（社長予定表）

---

以上。次のセッションではまずこのファイルを読んでから作業を継続してください。
