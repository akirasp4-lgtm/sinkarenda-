# 予定管理アプリ 引き継ぎ書

最終更新: 2026-05-29（夕方セッション分追記）
プロジェクト: 予定管理アプリ作成
リポジトリ: github.com/akirasp4-lgtm/sinkarenda-
本番URL: GitHub Pages（index.html / admin.html / president.html）
※ファイルは**リポジトリ・ローカルともルート直下**で運用（`index.html` / `admin.html` / `gas.js`）。本ファイル下部「1. システム構成」の `deploy/` パスは旧構成の名残りなので注意。

---

## 2026-05-29 セッションでやったこと（最優先で読む）

**現状**: 🟡 **設計・実装プラン完成・コミット済み。だが実装は未着手。** 次セッションは下記プランの Task 1〜6 を実行するだけ。
**次の作業場所**: このVault（`予定管理アプリ作成/`）。実装 → push（GitHub Pages 自動反映）→ Task 1 のみ GAS 再デプロイ。

### このセッションのメイン成果（未実装・次回ここから）

「カレンダー　修正希望.txt」の3要望をブレスト→設計→実装プラン化した（コードはまだ書いていない）：

- **② 👤人で探す** — 現場管理タブに「人で探す」モード追加。代表/メンバー問わずその人が関わった予定を日付順で一覧
- **③ 📌予定だけ** — 同タブに「予定ピン」モード。初期表示=今日より前の📌（やり残し）を赤系で。月で絞る切替も。各予定を開いて確定化/削除
- **④ 倉庫を請求集計に** — `gas.js` の `generateBillingFilterSheet_`（L1838〜）だけ直し、倉庫を `現場名=倉庫作業`/元請名空で人別×日別に出す。通常の「元請別請求集計」シートは倉庫除外のまま（棲み分け）

**まず読むべき2ファイル（cold start はこの順で）**:
1. `docs/superpowers/plans/2026-05-29-genba-search-warehouse-billing.md` ← **実装プラン（Task 1〜6、コード全部入り）**。これをそのまま実行
2. `docs/superpowers/specs/2026-05-29-genba-search-warehouse-billing-design.md` ← 設計の背景・検証済みの前提（なぜそうするか）

**検証済みの重要な前提**（実コードで確認済み・プランに根拠記載）:
- 倉庫の人工(kosu)は `submitNippo` L1853 で **1.0 固定保存**（休みのみ0）→ フィルタ用シートの `calcEffective_` の `kosu>0` を通過するので倉庫行はちゃんと出る
- `getPinGroups` の「今日」は **ローカル時間**で算出（`toISOString` の UTC だと日本の朝に1日ズレる）
- データ源差し替えのキモ：`gmMode` 変数 ＋ `getActiveGmGroups()` で `renderGmList`/編集/削除の参照を一本化。`updateGmSelects()` 呼び出し3箇所（index: L1475/L1500/L1964、admin: L1351/L1375/L1776）を `refreshGmTab()` に変える

**実行方法の選択が保留中**: サブエージェント駆動 or このセッションで順次。利用者は明示回答前に「セーブ」を指示。

### このセッション前半でやった小修正（コミット済み・デプロイ済み）

| commit | 内容 |
|---|---|
| `9a6d2a6` | 休み/倉庫モード選択時に代表者フィールドが選べないバグ修正（`applyYasumiLock` で leader を明示 enable） |
| `67d747e` | 編集モーダルで作業日を編集可能に（`e-date` 追加、単一編集のみ。一括編集時は非表示） |
| `537b58e` | 行の工番列が空でも現場マスタに発行済みなら詳細表示（非グローライズが代表時の工番消え対策） |
| `aebc569` | コピー時に車両もコピー |
| `ec9d322` | コピー時に登録ボタンが有効化されない不具合を修正（workType ゲート） |

> GAS は **v34（2026/05/21）**まで反映済み（warehouse_today の loc フォールバック等）。Task 1 実装後に再デプロイが必要。

---

## 2026-05-14 セッションでやったこと（最優先で読む）

1. **現場名選択にリアルタイム検索＋最近使った順を追加**（index.html / admin.html）
   - 登録・編集フォームの現場名プルダウン上に🔍検索ボックス（`*-location-search`）
   - プルダウンは「最近使った順」（最終使用日の降順、同日は五十音順）に変更
   - 件数表示：通常時は「(N件・最近順)」、絞り込み中は「(ヒット/全件)」
   - 元請変更時・登録完了時に検索もクリア
   - 主要関数：`populateLocationSelect(p)` 改修＋`onLocationSearch(p)` 追加
   - GitHub commit: `3da3c5e`

2. **月別確認表のヘッダ "00年" バグ修正**（gas.js）
   - 原因：日付列ヘッダ(1〜31)が数値のまま入っていて、Sheets が日付シリアル(1899-12-31〜)と解釈し「00年」表示になっていた
   - 修正：`generateKakuninTable_` のヘッダ整形時に `sheet.getRange(r, 2, 1, rule.daysInMonth).setNumberFormat('0')` を追加
   - GAS は **v24** として再デプロイ（2026-05-14）。WebApp URL は不変
   - GitHub commit: `48f091e`（ついでに gas.js が長らく GitHub と未同期だった分も一括同期）

6. **現場マスタから 1 現場を削除する機能**（admin.html / gas.js）
   - **きっかけ**: 「いらない現場名を消したい」
   - **GAS**: 新規アクション `delete_site`（body: `{genba, loc, force?, updatedBy}`）
     - **多段の安全策**:
       - 日報・アーカイブに参照ある場合 → 削除拒否（マージか完了フラグを促す）
       - 売上が入力されている場合 → 1 回目は警告応答、force=true での再送信が必要
       - 全てクリアなら現場マスタから 1 行削除
     - 操作ログに工番・売上を記録
   - **admin.html**: 工番修正モーダルの各現場行に「🗑️ この現場を削除」ボタン（控えめな赤系）
     - 売上ありエラー時はクライアント側で 2 段階確認 → force=true で再送
     - 削除成功時はモーダルを再読み込み
   - GitHub commit: `d332b05`
   - GAS は **v31** として再デプロイ（Chrome 操作で実施）

5. **現場名マージ機能を追加**（admin.html / gas.js）
   - **きっかけ**: 「現場名が似た名前で変わることがあるがそれをマージできないか」
   - 既存の 元請名マージ（merge_genba）と同じパターンで、同じ元請内の表記揺れ統合を実現
   - **GAS**: 新規アクション `merge_loc`（body: `{genba, fromLoc, toLoc, updatedBy}`）
     - 日報データ・アーカイブの該当行の「現場名」を fromLoc → toLoc に書き換え
     - 統合先の現場マスタに工番がある場合は日報側の工番もそちらに統一
     - 現場マスタは toLoc 行があれば from 行を削除、無ければ from を改名
     - 操作ログにも記録
   - **admin.html**: 事務タブに「🔀 現場名を統合（マージ）」セクション + 専用モーダル
     - 元請プルダウン → from/to 現場プルダウン（最近使った順）
     - プレビュー: 影響件数 + 統一される工番
   - GitHub commit: `ccc4374`
   - GAS は **v30** として再デプロイ（Chrome 操作で実施）

4. **現場の「完了」フラグ追加**（admin.html / index.html / gas.js）
   - **きっかけ**: 「終わった工事を表示させたくない、工番には影響させたくない」
   - **GAS**:
     - 現場マスタに 9 列目「完了」を追加（自動マイグレーション、`getOrCreateJobSiteSheet_`）
     - `doGet` 応答に `jobsites` を含める（`[{genba, loc, jobNo, completed}, ...]`）
     - 新規アクション `update_site_status`: 完了フラグを設定/解除、**工番・売上・既存日報には触らない**
   - **フロント（admin / index 共通）**:
     - `allJobsites` グローバル変数追加
     - `populateLocationSelect` で完了現場を除外（編集中の現在値は残す）
     - 予定詳細画面に「現場ステータス」行＋ボタン（緑バッジ：完了 / グレー：進行中）
   - **代表者の判定**（index.html のみ）: その予定の代表者ロールに自分が紐付いている時のみマーク可
   - **admin.html 工番修正モーダル**: 各現場行に「完了にする」チェックボックス追加（完了行は緑背景）
   - GitHub commit: `a3aba99`
   - GAS は **v29** として再デプロイ（Chrome 操作で実施）

3. **CSV ダウンロード → xlsx 一本化 ＋ 期間指定集計 ＋ SUM 関数入り**（admin.html / gas.js）
   - **きっかけ**: 事務員さんから「5/1〜5/10 だけ抜き出したいのに月単位でしか落とせず、合計がズレる」「見た目このままで出力したい」「どうせなら EXCEL に関数入れて」のフィードバック

   **GAS アクション（全部新規）**:
   - `get_sheet`: 既存に `dateFrom` / `dateTo` パラメータ追加。日報データ／アーカイブのみ作業日列で絞り込み
   - `period_kakunin`: 期間指定の月別確認表風 2D 配列を返す（シートには書かず CSV/xlsx 両用）
   - `export_kakunin_xlsx`: 月別確認表シートを xlsx で（色・罫線そのまま保持）
   - `export_period_kakunin_xlsx`: 一時シートに月別確認表風スタイルで書いて xlsx 化 → シート削除
   - `export_sheet_xlsx`: 任意シートを xlsx で（日報データ／アーカイブは期間絞り対応、一時シートにフィルタ後コピーしてエクスポート）
   - `exportSheetAsXlsxBase64_` ヘルパー：`UrlFetchApp.fetch` で `https://docs.google.com/.../export?format=xlsx&gid=N` を OAuth トークン付きで叩く方式
   - `colLetter_` ヘルパー：列番号 → 列文字（27→AA など）

   **SUM 関数**（Excel で値編集すると合計が自動再計算）:
   - `generateKakuninTable_`：本体の月別確認表シートでも、各データ行右端と合計行に `=SUM(...)` を設定
   - `exportPeriodKakuninAsXlsxBase64_`：期間集計の一時シートにも同様
   - xlsx 出力時は xlsx 仕様の "shared formula" 形式で保存される（Excel で展開して各セルに自動反映）

   **admin.html UI 整理**:
   - CSV ダウンロード機能を**全廃止**（xlsx で全て代替）
   - 残ったボタンは 2 個だけ：
     1. **Excelでダウンロード** — シート選択 + 任意の期間 → 該当シートを xlsx 化
     2. **期間指定でExcel出力** — 開始日〜終了日の職人×日付マトリクスを xlsx 化（SUM 関数入り）
   - 「期間集計CSVをダウンロード」と「月別確認表（全期間）」ボタンは削除（再生重複）

   - GitHub commits: `101c7da`(CSV機能)→`e0863e6`(xlsx機能)→`31366a8`(列固定bug修正)→`85b84c9`(CSV廃止+SUM)
   - GAS は **v27** として再デプロイ（新スコープ `external_request` を有効化）

   **ハマりどころ（次回参考）**:
   - GAS で `UrlFetchApp.fetch` を新規追加すると新スコープが要る → コードを貼った後に**関数を 1 回手動実行**して認可ダイアログを通す必要あり
   - `setFrozenColumns(1)` はタイトル行の `merge()` と競合する（「結合されたセルの一部だけを含む列を固定することはできません」エラー）→ 行固定のみに
   - xlsx の shared formula は `<f t="shared" ref="..." si="N">SUM(...)</f>` の形式で xml に書かれる。`<f>SUM` で grep してもマッチしない → `<f[^>]*>SUM` を使う

4. **環境整備**
   - リポジトリの状態：ローカル `予定管理アプリ作成/` で `git init` して `origin/main` を fetch、main を作成して以後ここから push 可能に
   - `.gitignore` を追加（API キー / スクリーンショット / 個人メモ / ローカル設定 / deploy・docs・scripts 等のローカル作業フォルダを除外）→ commit `af2c1bb`
   - 旧 `docs/引き継ぎ.md`（5/9 版）を `docs/引き継ぎ_旧_20260509.md` にリネーム退避（正本は本ファイル）
   - icon.png をルートに退避：オリジナル 331KB 版は `icon_original_331kb.png`、ルートの icon.png は GitHub と同じ 30KB 版

### このセッションでハマった点（次回参考）

- GitHub のリポは **ファイルがルート直下**で運用されている（`deploy/` 配下ではない）
  → ローカルの `deploy/github-pages/*.html` / `deploy/gas/gas.js` を**ルートに cp してから commit** する流れ
- GitHub の `gas.js` は長らく未更新だったため、push 時に v23 までの蓄積分（vehicle_res_*, merge_genba, 工番ゲート, 19列ヘッダ等）が一気に差分として出る
  → 今回ぜんぶ同期したので、以後は普通に1機能=1コミットで OK

---

---

## 1. システム構成

### フロントエンド（GitHub Pages）
- `deploy/github-pages/index.html` — 職人用（予定入力・カレンダー）
- `deploy/github-pages/admin.html` — 管理者用（現場管理・集計・各種ツール）
- `deploy/github-pages/president.html` — 社長用（読み取り専用カレンダー）
- `deploy/github-pages/links.html` — 入口（3ボタン）
- `deploy/github-pages/manifest.json` — PWA設定

### バックエンド（Google Apps Script）
- `deploy/gas/gas.js` — 約1900行
- スプレッドシート: 日報・アーカイブ・現場マスタ・元請マスタ・社長予定・操作ログ・車両予約
- **現在のデプロイ版: v31（2026/05/16 現場マスタ 1 行削除機能追加）**
- WebApp URL（変更なし）:
  `https://script.google.com/macros/s/AKfycbxp2eUcpIjCj0ZWyAPPD9m3egJrKdWmXRK2AVnFrmBm4iO1QHCk-FZEH5LFFv7OloqcjQ/exec`

---

## 2. 主な機能

### 共通
- 会社別表示（GRHD / グローライズ自社 / 等）
- URLパラメータ `?c=会社名` または localStorage で会社固定
- カレンダー: **月曜始まり**（`(getDay()+6)%7`）
- 19列ヘッダ（車両カラム追加済み）

### 職人用 index.html
- 予定登録・編集・削除
- **作業区分ゲート**: 「作業区分」を最初に選ばないと他フィールドが触れない（赤字で案内）
- **作業区分**: ⛑️現場作業 / 倉庫 / 休み / 撤去品返却（現場作業のみ工番発行）
- **現場名は `<select>` プルダウン**（iOS Safariで `<datalist>` が出ないため）
- 新規現場登録時は類似現場サジェストを表示
- 車両選択（グローライズ自社のみ、10台のうち会社所有分のみ表示）
- 被りチェック（saveEdit / submitNippo 両方）
- PWA: インストールバナー / シェア / PTR / 触覚フィードバック
- モバイルではリスト表示モード（@media max-width:600px）

### 管理者用 admin.html
- 現場管理・元請管理・月別集計
- 工番修正モーダル（検索付き）
- データアーカイブ
- 工番事業部の修正
- おかしい工番をクリア（`cleanupOrphanJobnos_`）
- **🔀 元請名をマージ** ← 最新追加（commit `7dffb8c`）

### 社長用 president.html
- コンパクトカレンダー（セル62px、月曜始まり）
- chip表示は `title || 時刻` のタイトル優先
- min-width:0; overflow:hidden でセルはみ出し対策

---

## 3. 最新の変更点（直近セッション分）

### 3.1 工番重複対策（案A＋C）
**問題**: 「○○現場 材料引取」のような表記揺れで同じ現場に複数工番が振られる。

**対策**:
1. **案A: 作業区分ゲート** — 現場作業の時だけ工番発行
2. **案C: 現場名プルダウン** — 既存現場から選ぶ／新規は明示
3. **管理側マージ機能** — 既存の重複を統合できる

GAS側条件:
```js
row.company === GROWISE
  && !row.souko && !row.yotei && !row.yasumi
  && row.workType === '現場作業'
```

### 3.2 元請マージ機能
**ファイル**: gas.js + admin.html
**GAS action**: `merge_genba`
**動作**: 日報・アーカイブ・現場マスタ・元請マスタの「元請名」をfrom→toに置換、from側マスタは削除（重複なら）

**UI（admin.html 事務タブ）**:
- 青背景の「🔀 元請名を統合（マージ）」ブロック
- 「元請名をマージする」ボタン → モーダル表示
- 統合元・残す側のドロップダウン
- プレビュー（影響件数表示）
- 確認ダイアログ後に実行

**実行済みマージ**: グローライズ → グローライズ自社（日報側0件、マスタ側は重複削除）

### 3.3 撤去品返却
作業区分に追加。工番は発行されない。

### 3.4 社長予定表
- 「時間しか見えない」問題 → タイトル優先表示に変更
- セルはみ出し → min-width:0; overflow:hidden で抑制
- 月曜始まりに統一

### 3.5 車両予約 GAS連携（ラインボット向け）
返信ファイル: `ラインボットへの返信.md`
- 4アクション実装: `vehicle_res_add` / `update` / `delete` / `list`
- トークン: `車両予約用トークン1234`（`VEHICLE_RES_TOKEN`）
- シート「車両予約」14列（自動作成）
- 既存カレンダー機能には一切手を入れていない
- **Phase 2（カレンダーに車両予約表示）は未実装**

---

## 4. 重要なコード構造

### gas.js
- `HEADERS` — 19列（車両カラム含む）
- `doPost(e)` — actionで分岐
  - 既存: add / update / delete / archive / summarize / get_sheet / pres_*
  - 追加: `vehicle_res_*` / `cleanup_orphan_jobnos` / `merge_genba`
- `cleanupOrphanJobnos_(ss)` — 休み・倉庫・予定・現場作業以外の工番を一括クリア
- `mergeGenba_(ss, fromName, toName)` — 元請名統合
- `logOperation_` — 操作ログに記録

### index.html / admin.html 共通の重要関数
- `applyWorkTypeLock(p)` — 作業区分ゲート
- `applyYasumiLock(p, clearValues)` — 休み・倉庫モード時のロック
- `populateLocationSelect(p)` / `onLocationSelect(p)` — 現場名セレクト
- `showSimilarSuggest(p)` / `pickSimilarLoc(p, loc)` — 類似現場サジェスト
- `getCompanyVehicles()` — 会社別車両絞り込み

### admin.html マージ機能
- `openMergeGenbaModal()` — モーダル開いてドロップダウン構築
- `updateMergePreview()` — 影響件数プレビュー
- `runMergeGenba()` — 確認後にGAS呼び出し

---

## 5. デプロイ手順

### GitHub Pages
```bash
git add deploy/github-pages/
git commit -m "..."
git push
```
自動デプロイ（数十秒〜数分で反映）

### GAS
1. Chromeで script.google.com を開く
2. コード貼り付け → 保存
3. 「デプロイ」→「デプロイを管理」→鉛筆アイコン→「新バージョン」→「デプロイ」
4. URLは変わらない、内部バージョンだけ上がる

**注意**: Monaco editorは日本語文字列の直接送信でブロックされることあり。`String.fromCharCode(0x...)` を使う。

---

## 6. 既知の課題・残課題

### 残課題（依頼があれば）
- **Phase 2（車両予約のカレンダー表示）** — ラインボットからの依頼。Phase 1完了でOKとの指示で保留中
- **Service Worker / オフライン対応** — PWA本格化案として説明済み、未実装
- **起動の遅さ** — 改善プラン提示済み（メリット・デメリット説明）

### 注意点
- iOS Safariは `<datalist>` が出ない → `<select>` を使う
- body の `overflow:hidden` ＋ `.screen{height:100vh;overflow-y:auto}` でタブバー固定（iOS）
- PC表示でセンタリングは max-width:1000px + margin:0 auto を `.screen` に
- 日報の元請変更・現場変更は表記揺れに注意。マージ機能で吸収可能

---

## 7. ファイル一覧（主要）

```
予定管理アプリ作成/
├── 引き継ぎ.md                          ← このファイル
├── ラインボットへの返信.md              ← 車両予約GAS連携の完了報告
├── deploy/
│   ├── gas/
│   │   └── gas.js                        ← GASバックエンド（約1900行）
│   └── github-pages/
│       ├── index.html                    ← 職人用（約2700行）
│       ├── admin.html                    ← 管理者用（約2400行）
│       ├── president.html                ← 社長用（約830行）
│       ├── links.html                    ← 入口
│       └── manifest.json                 ← PWA
```

---

## 8. 直近のコミット履歴（要約）

- `d332b05` — 現場マスタから 1 現場を削除する機能を追加（2026-05-16）
- `3d7fd5d` — 現場名マージ：A/B 選択 → どっちに合わせる？ボタン式UIに変更（2026-05-16）
- `ccc4374` — 現場名マージ機能を追加（表記揺れ統合）（2026-05-16）
- `121cca4` — fix: 完了切替後にページ更新しないと反映されない問題を修正（2026-05-16）
- `a3aba99` — 現場の「完了」フラグ追加：プルダウン非表示／工番は不変（2026-05-16）
- `10c29a3` — アプリ内ヘルプを最新機能で更新（2026-05-14）
- `85b84c9` — CSV廃止→xlsx一本化＋合計セルに SUM 関数を入れる（2026-05-14）
- `31366a8` — fix: 期間指定xlsxで列固定とセル結合が競合するバグを修正（2026-05-14）
- `e0863e6` — 見た目そのまま出力: xlsx エクスポート（全期間 + 期間指定）（2026-05-14）
- `101c7da` — CSV ダウンロードに期間フィルタ＋期間集計CSV を追加（2026-05-14）
- `af2c1bb` — リポジトリ運用ルールを .gitignore に明文化（2026-05-14）
- `48f091e` — gas.js を本番デプロイ状態に同期＋月別確認表ヘッダの日付バグ修正（2026-05-14）
- `3da3c5e` — 現場名選択にリアルタイム検索＋最近使った順ソートを追加（2026-05-14）
- `7dffb8c` — 元請名マージのadmin UI追加
- それ以前 — マージGAS実装、撤去品返却追加、工番ゲート、現場名プルダウン化、社長予定表のレイアウト修正、車両予約GAS連携 等

---

## 9. ユーザー固有の方針メモ

- 「職人が入力ルールを守らない」ので、UI側で**強制的に正しい入力に誘導**する方針
- 既存カレンダー機能には**勝手に手を入れない**（独立ブロックで追加）
- 重要操作は**確認ダイアログ必須**（マージ等は取り消し不可なので特に）
- モバイル（iOS Safari）を最優先のターゲットとする
- 表示優先順: タイトル > 時刻（社長予定表）

---

以上。次のセッションではまずこのファイルを読んでから作業を継続してください。
