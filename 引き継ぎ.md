# 予定管理アプリ 引き継ぎ書

最終更新: 2026-08-28（🎓**資格で人を選べるようにした＝本番稼働**。資格マスタ303件を画面へ通し、空き人員を資格で絞れる／期限切れは「持っていない」扱い＝§3.14。GAS v63・Worker・Pages 反映済み、Chrome実機で確認。**免許番号・正式氏名はGASを出る時点で落とす**（歯止め3枚）。Codexレビューの[P1]3件は実データで壊れる物ばかりだった（奥田さんがグローライズとGRHDの両方に実在する等）。テスト659件。⚠️AI提案（月数百円）と毎朝のアラート（外部送信）は利用者判断のため保留）
前回: 2026-08-28（🏢**GRミツマを「グローライズ関東支店」へ寄せ切った＝本番実行済み**。日報78件・職人マスタ8人（うち二重登録2人を統合）・元請マスタ3行。会社別集計を`グローライズ（本社）`/`グローライズ（関東支店）`に分割＝§3.12。GAS v61・GitHub Pages反映済み。**和信カインド・ラーテル・GRHDは1セルも触っていない**。Codexレビューの[P1]3件を潰してから実行。関東支店の数字36/49は旧GRミツマと完全一致を実測）
前回: 2026-08-26（🏢**拠点（本社／関東支店）を新設し、グローライズとGRミツマのカレンダーを1本に統合＝本番稼働**。依頼書 `calendar_request_20260826/` のとおり。全拠点2,331件／本社2,246件／関東支店85件。**和信カインド・ラーテル・GRHDは混ぜていない**（利用者指定）。GAS v54・Worker反映済み・過去2,625行を埋め済み。Codexレビュー[P1]9件＋実機で3件を修正。前回: 社長用カレンダーのCloudflare移行）
前回: 2026-08-26（🚀社長用カレンダーを社員用と同じ速さにし、読み取りもCloudflareへ移行。0.09秒／GASと202件完全一致）
前回: 2026-08-25（🚀保存を「押した瞬間」に＝案A（楽観的表示＋裏送信）実装完了。体感4.4秒→0秒（**書き込みそのものは速くなっておらず約4秒のまま**。速くなったのは体感だけ）。ブランチ `feat/optimistic-save`・**未push**。自動テスト191件PASS、**実ブラウザ・実機での通し確認はまだ誰もやっていない＝§3.6必読**）
前回: 2026-08-22（👀 集計6点をアプリ内で閲覧可に＋10日締めの出面表を画面で即表示＝§3.0e/3.0f。☁️ Cloudflare移行フェーズ1に着手＝Task1・2完了/Task3再判定中、**本番影響ゼロ・未push**。**Task5前に必ず解決する単価の件は§6を必読**）
前回: 2026-08-21（⚡レスポンス改善＝開く 9.7→3.9秒／保存 14→4.4秒／起動時キャッシュで初期表示42ms。アーカイブ410件実行済み。**🔥GASが5回に1回HTTP404を返す不安定さを実測＝§3.0b必読**。**⚠️別アカウントからのデプロイで本番を約20分停止させた事故あり＝下記「5. デプロイ手順」を必読**）
前回: 2026-08-07（🧹トークン節約＝セッション記録12本を [[自社用/予定管理アプリ作成/docs/引き継ぎ_詳細|docs/引き継ぎ_詳細]] へ移動 18,425→4,658トークン。やりかけ＝きんでん事務タブ統合と毎月の運用は「6. 既知の課題」に残置）
機能面の前回: 2026-07-13（🚀集計の接続エラー修正＝書式一括化で185秒→約35秒・GAS v45本番反映済み。gas.jsは**未コミット**。⚠️合言葉の公開リポ漏えい問題が引き続き未対応）
プロジェクト: 予定管理アプリ作成
リポジトリ: github.com/akirasp4-lgtm/sinkarenda-
本番URL: GitHub Pages（index.html / admin.html / president.html）
※ファイルは**リポジトリ・ローカルともルート直下**で運用（`index.html` / `admin.html` / `gas.js`）。本ファイル下部「1. システム構成」の `deploy/` パスは旧構成の名残りなので注意。

> ⚖️ **2026-06-11 グループ決定（GR-BP2側で確定）＝お金は GR管理画面「📊経営タブ」に一本化**。
> このアプリの**金額機能（💰請求計算＝人工×単価・元請別請求集計・きんでん請求書ジェネレーター等）は“未使用”と利用者が確認 → 当面「寝かせる」**（消さない・無害・後で起こせる）。
> ✅ **非表示化は本番反映済み（2026-06-11 commit `9e8c427`）**: admin.html の 💰請求計算タブと工番モーダルの売上(円)欄を `display:none` で隠し、「金額はGR経営タブへ」の案内を追加。コードは全部温存＝復活は display:none を外すだけ。**⚠️復活時の注意: タブボタンは消さず必ず「隠す」**（switchTabがボタンの並び順で動くため、消すと他タブが壊れる）。
> 🔒 **現場画面の給料漏れ口も閉鎖済み（2026-06-11 commit `262d85a`）**: index.html（PINロック無し・全社員が使う）の事務タブCSVダウンロードに**職人マスタ（単価列＝給料情報入り）**と元請別請求集計が並んでいた → プルダウンからコメントアウトで除外。admin.html（PIN付き）からは従来どおりDL可。⚠️GAS(get_sheet)は今も単価込みで配るため、**本物の日当を入れる日に配信側の改修もセットで**（詳細は仕訳マップの「4点セット」）。
> 💤 スプレッドシート「事業部別按分」シート（人工×単価→原価・粗利の自動計算 `generateDivisionAllocation_`）も休止扱い。**今入っている数字はテストの残骸＝本番データではない**（2026-06-11 利用者確認）。計算エンジン自体は完成品なので、将来の「人工→原価の橋」はこれを繋ぐだけでよい。
> このアプリは **予定・現場カレンダー・人工(出面)の記録に専念**。将来、人工(出面)データを GR経営タブの「原価/売上」に自動で流す“橋”を架ける案あり（常用・日当が多い場合のみ着手）。詳細は `GR Business Platform２/引き継ぎ.md` の「直近のセッション 2026-06-11」。
> 🗺️ **機能の持ち場の正典** ＝ `GR Business Platform２/docs/機能仕訳マップ_予定管理とGR管理画面.md`（**「工番事業部の修正」はGR管理画面へ移設予定（候補#1）**。着手時はマップの注意点＝橋＋Notionダブり対策を必読）。

---

> 過去のセッション記録12本（2026-05-14 〜 2026-07-13）は全文を **[[自社用/予定管理アプリ作成/docs/引き継ぎ_詳細|docs/引き継ぎ_詳細]]** へ移動した（2026-08-06・トークン節約のため。**中身は消していない**）。やりかけの仕事と毎月使う運用手順は「6. 既知の課題・残課題」に残してある。

---

## 0.5 🔑 アカウント一覧（どこを触るとき誰でログインするか）

> 2026-08-21に**アカウントの取り違えで本番を約20分停止**させた。このアプリは4つの
> アカウントにまたがっている。作業前に「今どれで操作しているか」を必ず確認すること。

| 部分 | アカウント | 持っているもの / 権限 |
|---|---|---|
| **スプレッドシート＋GAS本体** | **`yotei.glorise@gmail.com`** | **データ本体。オーナー。GASのデプロイは必ずこのアカウントから**（§5参照） |
| clasp（コードの送信のみ） | `miscjigyoubu@gmail.com` | 2026-08-21に編集者権限を付与。`clasp push` まで。**deployはしない** |
| 画面（GitHub Pages） | `akirasp4-lgtm` | `github.com/akirasp4-lgtm/sinkarenda-`（**公開リポジトリ。鍵を絶対に置かない**） |

> ⚠️ **push するときは gh のアカウントを切り替えること**（2026-08-25に2回つまずいた）。
> このPCの gh は既定が `miscjigyoubu` で、そのままだと
> `Permission to akirasp4-lgtm/sinkarenda-.git denied to miscjigyoubu` で弾かれる。
>
> ```
> gh auth switch --user akirasp4-lgtm
> ```
>
> を実行してから `git push`。**現在の作業後は `akirasp4-lgtm` に切り替えたままになっている**ため、
> 他のリポジトリ（`miscjigyoubu` 所有）へ push するときは逆に戻す必要がある。
| Cloudflare（移行先） | `miscjigyoubu@gmail.com` | Worker + D1。アカウントID `681ff426a7469449fb79ee9636a90a26` |

**なぜCloudflareが `yotei.glorise` でなくてよいか**: Worker は Google の認証を一切使わない。
GASの公開URL（誰でも叩けるURL）を叩いてJSONを返すだけで、スプレッドシートの権限も鍵も持たない。
そのためCloudflare側のアカウントは開発・インフラ用の `miscjigyoubu` に寄せるのが自然
（家風ルール「GitHubは仕事用アカウント miscjigyoubu」と同じ考え方）。
`yotei.glorise` でCloudflareを新規に作っても機能は変わらず、管理対象が1つ増えるだけ。

**Cloudflareの既存トークン（売り物側・触らないこと）**: `モンゴル写真` / `貿易画面デプロイ_20260805` /
`モンゴル` / `モンゴルデプロイ` — いずれも Cloudflare Pages 権限のみ。GR Business Platform の
LP（`gr-business-platform.pages.dev`）と管理画面（`gr-admin-prod.pages.dev` / `gr-admin.pages.dev`）用。

**移行作業用トークンの扱い**: `予定管理D1_20260822`（D1=編集 / Workersスクリプト=編集 /
アカウント設定=読み取り、対象アカウントは1つに限定）。**デプロイ作業のときだけ必要で、
公開後のアプリの動作には一切使われない。使い終わったら削除する運用。**
保管は `_local/` 配下（.gitignore済）のみ。**公開リポジトリなので絶対にコミットしない。**

---

## 1. システム構成

### フロントエンド（GitHub Pages）
- `deploy/github-pages/index.html` — 職人用（予定入力・カレンダー）
- `deploy/github-pages/admin.html` — 管理者用（現場管理・集計・各種ツール）
- `deploy/github-pages/president.html` — 社長用（読み取り専用カレンダー）
- `deploy/github-pages/links.html` — 入口（3ボタン）
- `deploy/github-pages/manifest.json` — PWA設定

### バックエンド（Google Apps Script）
- `deploy/gas/gas.js` — 約1900行
- スプレッドシート: 日報・アーカイブ・現場マスタ・元請マスタ・社長予定・操作ログ・車両予約
- **現在のデプロイ版: バージョン66（2026/08/31 9:39・元請の統一を会社ごとに限定）**
  ※以前ここに「v31」と書かれていたのは古い記載。実際のバージョン履歴は Apps Script の「デプロイを管理」で確認できる。
- WebApp URL（変更なし）:
  `https://script.google.com/macros/s/AKfycbxp2eUcpIjCj0ZWyAPPD9m3egJrKdWmXRK2AVnFrmBm4iO1QHCk-FZEH5LFFv7OloqcjQ/exec`

---

## 2. 主な機能

### 共通
- 会社別表示（GRHD / グローライズ自社 / 等）
- URLパラメータ `?c=会社名` または localStorage で会社固定
- カレンダー: **月曜始まり**（`(getDay()+6)%7`）
- **21列ヘッダ**（…車両=19 / **拠点=20**（2026-08-26）/ **部隊=21**（2026-08-27））
- 拠点の切替（全拠点／本社／関東支店）。グローライズとGRミツマは1つの名簿・1つのカレンダー

### 職人用 index.html
- 予定登録・編集・削除
- **部隊（1〜4部隊）**（2026-08-27）— 責任者を選ぶと既定部隊が自動で入る。手で変えた値は上書きされない
- 役割の呼び方は **責任者／班員**（2026-08-27）。※スプレッドシートの保存値は `代表`/`同行` のまま
- **作業区分ゲート**: 「作業区分」を最初に選ばないと他フィールドが触れない（赤字で案内）
- **作業区分**: ⛑️現場作業 / 倉庫 / 休み / 撤去品返却（現場作業のみ工番発行）
- **現場名は `<select>` プルダウン**（iOS Safariで `<datalist>` が出ないため）
- 新規現場登録時は類似現場サジェストを表示
- 車両選択（グローライズ自社のみ、10台のうち会社所有分のみ表示）
- 被りチェック（saveEdit / submitNippo 両方）
- PWA: インストールバナー / シェア / PTR / 触覚フィードバック
- モバイルではリスト表示モード（@media max-width:600px）

### 管理者用 admin.html
- 現場管理・元請管理・月別集計
- 工番修正モーダル（検索付き）
- データアーカイブ
- 工番事業部の修正
- おかしい工番をクリア（`cleanupOrphanJobnos_`）
- **🔀 元請名をマージ**
- **🕘 履歴タブ**（2026-08-27）— 誰が・いつ・何を・何から何に変えたか。削除は全項目を復元できる
- **職人管理で既定部隊・有効/無効を設定**（2026-08-27）
- **現場ごとの案件ステータス8段階**（2026-08-27）

### 社長用 president.html

> 2026-08-26: 社員用と同じ高速化①〜④を適用（§3.7）。**`send-queue.js` を読み込むようになった**ため、このファイルだけを単独で配置しても楽観的保存は働かない（`send-queue.js` が無いと自動で従来の同期保存に倒れる＝壊れはしない）。
- コンパクトカレンダー（セル62px、月曜始まり）
- chip表示は `title || 時刻` のタイトル優先
- min-width:0; overflow:hidden でセルはみ出し対策

---

## 3. 最新の変更点（直近セッション分）

### 3.12 GRミツマを「グローライズ関東支店」へ寄せ切った（2026-08-28・✅本番実行済み）

**利用者指示**
> 「和信、ラーテル、GRHDは触らないで下さい。また、GRミツマは関東支店として
>   グローライズに統合されるので消して下さい」
> 「カレンダーは一つにして、関東支店と本社の売上とか割を分けれるようにしておいてくれたらよくて。」
> 「グローライズの中に本社と関東支店が統合されたから、今あるGR三妻っていうカレンダー自体がもういらない。」

§3.9で「拠点」の軸は作ったが、**会社欄には GRミツマ が残ったまま**だった。
今回それを消し切り、事務員が本社と関東支店を別勘定で集計できるようにした。

#### やったこと

| 場所 | 変更 |
|---|---|
| 日報データ | 会社 GRミツマ→グローライズ **78件**（拠点は全件すでに `関東支店`） |
| アーカイブ | 0件（GRミツマの行なし） |
| 職人マスタ | 64行→62行。高田（関東）・柳澤（関東）・内村（関東）・馬場（応援）・木村（応援）・江頭・管理・繁田 の8人を移し、**江頭と繁田の二重登録を1人にまとめた** |
| 元請マスタ | 51行→48行。`GRミツマ自社`→`グローライズ自社`。きんでん東・ハイテックス・児玉通信を1行に |
| 会社別集計 | 見出しを **`グローライズ（本社）` / `グローライズ（関東支店）`** に分けた（`summaryGroupKey_`） |
| 画面 | 会社の切替チップから GRミツマ を外した。関東は「グローライズ→関東支店」で見る |

#### 数字が変わっていないことの確認（2026-08-28 22:00 実測）

| グループ | 当月人工 | 全期間人工 | 統合前 |
|---|---|---|---|
| グローライズ（関東支店） | **36** | **49** | 旧GRミツマ 36 / 49 → **完全一致** |
| GRHD | 3 | 24 | 3 / 24 → **変化なし**（触っていない） |
| グローライズ（本社） | 506 | 2054 | 499 / 2011（当日入力29行の増加分） |

#### ⚠️ 引き続き残っている実データの問題（統合とは別）

本社と関東支店の**両方に同じ人・同じ日**がある人日が当月12件（江頭10日・高田（関東）2日）。
このため 本社506＋関東36＝542 だが実人日は530。統合前も「グローライズ」と「GRミツマ」に
分かれて同じ二重計上をしていたので**新しく生じた問題ではない**（境目が会社→拠点に移っただけ）。
§3.11の人員重複チェックで拾える対象。

#### 実行のしかた（もう一度必要になったときのため）

`gas.js` の `mergeMitsumaIntoGrowise(apply)` が本体。`apply=false` で下見だけできる。
**やることが無ければ1文字も書かずに終わる**（`何もしていない: true`）ので、二度実行しても被害は出ない。
実行用の一時メニュー（スプレッドシートの「GR保守」）は片付け済み。

**設計上ゆずらなかった点（Codexレビューで3件のP1を潰した）**
1. 書き込みは **元請名→拠点→会社の順**。会社を最後にすることで、途中で落ちても
   「会社がまだGRミツマ＝未完了」と読めて、そのまま再実行できる。
2. **他社の行は1セルも書かない。** 列ごと・シートごとの書き戻しをやめ、変更するセルだけを
   `setValue` し、消す行は下から `deleteRow` する（和信カインド等の数式が値に固まるのを防ぐ）。
   テストで「実際に書き込んだ範囲」を記録して他社行が入らないことを検査している。
3. 会社／拠点／元請名の列が1つでも欠けていたら**中止**する。

#### ⚠️ KYOTEN_COMPANIES から GRミツマ を外さないこと

`index.html` / `admin.html` の `KYOTEN_COMPANIES` には **GRミツマ を残してある**。
会社欄の書き換えは終わっているが、古い端末の未送信データが後から届いたときに、
その予定がカレンダーから丸ごと消えるのを防ぐ保険。

古い `?c=GRミツマ` のブックマークを開いても、中身はグローライズ／関東支店として開く。

### 3.19 🚨本番障害: CronがCPU上限で落ちていた（2026-08-30）

#### 何が起きたか

**毎朝6時のアラートが、運用初日に1通も送れなかった。**

利用者から「これで依頼通りおわったか？」と聞かれ、初回稼働日なので確認したところ発覚。

```
"*/5 * * * *" @ 2026/8/30 11:20:32 - Exceeded CPU Limit
```

- 5分ごとの取り込みCronが **CPU上限超過で毎回失敗**
- 取り込みが **7時間半停止**（2026-08-29 18:25Z 〜 2026-08-30 01:59Z）
- `/api/alerts` は HTTP 503。3回叩いて3回とも失敗
- エラー率 **40.4%**・エラー86件（前日比 +8500%）
- **誰にも通知されなかった**

#### なぜ今になって壊れたか

**データが増えて予算を超えた。** 2,415行 → 2,541行。
じわじわ増えて限界を越えた。**放置すればまた壊れる性質の障害。**

#### ✅ 動いていたもの（安全装置は正しく働いた）

| | |
|---|---|
| カレンダー・空き確認などの画面 | ✅ 動いていた（GASへ自動で切り替わる。少し遅いだけ） |
| 毎朝のアラート | ❌ 0通 |

`read.js` の鮮度ガード（15分＝Cron3回分）が
**古いデータを黙って渡さずエラーにしていた**。前のセッションのこの判断が効いた。

#### 直したこと①: Cronを1回1仕事にする

1回のCronで `syncAll` + `cleanupSyncLog` + `syncPresident` + `cleanupPresSyncLog`
を**4つ同時**に走らせていた。分の値で振り分ける形へ:

| タイミング | 仕事 |
|---|---|
| **毎回** | `syncAll`（予定の取り込み・最優先。ここだけは絶対落とさない） |
| :00 :10 :20 :30 :40 :50 | `syncPresident` |
| :05 | `cleanupSyncLog` |
| :35 | `cleanupPresSyncLog` + `cleanupAiLog` |

⚠️ **この振り分けは一度間違えて直した。** 最初 `syncPresident` を :00/:30 の
**30分に1回**にしたが、社長画面の鮮度ガードは **15分**。
30分に1回では「必ず古い」時間帯が毎時30分ぶんでき、社長画面がその間ずっと
GASへ落ちて遅くなっていた（下の 3.20 の①）。**10分に1回**が正解。
**掃除を減らすのは安全だが、同期を減らすときは必ず鮮度ガードの値と突き合わせる。**

**確認**: デプロイ後、Workerのログで2回連続 `Ok`（11:25 / 11:30）。
`/api/schedule` status=ok rows=2241、`/api/alerts` HTTP 200 に復旧。

#### 直したこと②: 「静かに壊れる」をやめる（Codexレビュー[P1]）

取得に失敗しても**黙って0通で終わっていた**。
「通知が来ない＝問題なし」と誤解される。**一番危ない壊れ方。**

- 取得を **3回まで試す**（5秒→10秒。一時的な不調で落とさない）
- 503でも**中身のJSONを読む**（Workerが返す理由を捨てない）
- それでも駄目なら**管理者3人へ「判定できませんでした」を送る**
- 返り値に `outcome` を付け、`no_problem` と `upstream_unavailable` を**必ず区別**

実際に届く文面（本番で確認済み）:
```
【予定管理】翌日の確認ができませんでした

予定のデータを読み取れなかったため、重複や人員の確認を行えていません。
**問題が無い、という意味ではありません。**

理由: （Workerが返した理由）

カレンダーの画面はそのまま使えます。
```

テスト 240 → **245**。障害通知を消す／リトライを削る、の2つを壊して赤くなることを確認。

#### Codexレビューとの答え合わせ

| Codexの推測 | 実際 |
|---|---|
| `sync_lock` の取り残し（stale lock） | ❌ **外れ**。`LOCK_STALE_MS=90秒`で自動解除、`releaseLock` は `finally` 内 |
| **CPU超過** | ✅ **当たり** |
| 「静かに壊れる」構造が多すぎる | ✅ **当たり**（7時間半、誰も知らなかった） |
| Cronの業務失敗がCloudflare上は成功になる | ✅ 当たり（未対応・下記） |

**教訓: 推測で直さず、`npx wrangler tail` で生ログを見たら1行で確定した。**

#### ✅ 直したこと③: 取り込みの失敗をCloudflareにも失敗として見せる

`syncAll` は失敗しても例外を投げず `{ok:false}` を返すだけ。そのため
**7時間半止まっていたのに Cron Events は緑のまま**だった。異常に見えなかった。
→ 業務として失敗したら `throw` して赤くする。
  ★ロック待ちのスキップは正常なので失敗にしない。

#### ✅ 直したこと④: `/api/alerts` に鍵をかけた（無認証公開だった）

実データで確認したところ、`curl` で叩くだけで**氏名と現場名がそのまま取れた**:
```
・江頭 … きんでん東 ELLEGARDEN柏の葉 と きんでん東 RM新設工事　ELLEGARDEN柏の葉
```

- Worker に `ALERT_KEY` を設定すると、`X-Alert-Key` を持つ要求だけ通す
- ボット側は `.env` に `YOTEI_ALERT_KEY=同じ値` を入れれば自動で送る
- **★鍵が未設定の間は今までどおり通る**（設定前に朝の通知を止めないため）

**⏸️ 鍵はまだ未設定。** 有効にするには2つ:
```
npx wrangler secret put ALERT_KEY          （cf フォルダで実行）
```
そのあと VMの `/home/akira/app/.env` に `YOTEI_ALERT_KEY=同じ値` を追記して再起動。
⚠️ **.env はAIが触らない決まり**なので、そこは利用者が行う。

#### ⏸️ まだ直していないCodexの指摘

- **`sync_log` を可用性判定に使っている** … 監査ログと健康状態を分けるべき（`sync_state` を作る）
- **`/api/sync` も無認証** … `force` が指定できるなら早めに塞ぐ
- **資格データの欠落を「0件」と扱う** … 取得失敗と「該当0件」を区別できていない
- **根本対策**: Cloudflare有料化（月5ドル＝約750円）でCPU上限 10ms→30s。
  データが増え続ける以上、いずれ必要になる可能性が高い

#### テスト

Worker 840 → **848**（1回1仕事・失敗をthrow・鍵の検査。3つとも壊して赤を確認）
ボット 240 → **245**（障害通知・リトライ。2つとも壊して赤を確認）

### 3.18 要件5（AI人員配置提案）完成 ／ 部隊の表示位置 ／ 停滞通知（2026-08-29）

#### ✅ 要件5 — AIに順位付けと理由を聞く（完成・本番稼働中）

候補を出すところ（空き×資格×経験）は既に0円で動いていた。ここが足したのは
**順位付けと「なぜこの人か」の一文だけ**。空き確認で元請を選ぶとボタンが出る。

**実際の出方**（きんでん東・3人必要で実測）:
```
AIの見立て（決めるのは人です）
 1. 江頭（84日）… 経験が最も多く、資格も豊富だから。
 2. 加藤（74日）… 経験が次に多く、現場責任者資格があるから。
 3. 中村（38日）… 経験は少ないが、職長資格を持っているから。
```

**🔒 氏名をOpenAIへ送らない。** 画面が候補を `c1,c2…` に置き換えて送り、
返ってきた番号を名前に戻す。単価も現場名も送らない。
idが `c\d+` 形式でなければ弾く（氏名混入の事故防止）。実データで検査済み。

**💰 課金の上限（3重）**
| | |
|---|---|
| Origin検証 | 画面からの呼び出しだけ通す。**curl直叩きは403** |
| 1日200回 | D1の `ai_log` で数える。**数えられないときは「呼ばない」側に倒す** |
| 1回の量 | 候補40人・max_tokens 500・temperature 0 |

**実測費用: 1回 約0.05円**（応答1.1秒）。上限まで使っても10円/日。毎日10回で月15円。
事前見積り「1円未満」より更に安かった。

- 窓口 … `cf/src/index.js` の `POST /api/suggest`（`cf/src/suggest.js` が本体）
- 鍵 … **Cloudflare Workerのシークレット `OPENAI_API_KEY`**。
  ⚠️ **Web画面から入れた場合は一度 `npx wrangler deploy` しないと反映されない**（実際にハマった）
- 鍵が無ければ `enabled:false` を返し、画面はAI欄ごと隠す＝**0円のまま壊れない**
- **予定は作らない**。プロンプトにも「決めるのは人間です」を必ず入れる

⚠️ VMに置かなかった理由: ボットの公開URLに認証なしのAI窓口を足すと、
知られた瞬間に課金が積み上がる。Workerには既にOrigin検証とレート制限がある。

#### ✅ 部隊をカレンダーの先頭から詳細へ移した

**利用者指示**: 「部隊を詳細に入れてください。1番前は他の予定が見にくくなります」

各予定の先頭に紫の部隊タグを出していたため、現場名の頭が隠れて読みにくかった。
タグをやめ、予定を開いた詳細に「🚩 部隊」の行として出す（空なら行ごと出さない）。
使われなくなった `butaiTag()` と `.butai-tag` のCSSも削除。

⚠️ **予定データに既に17行「第六部隊」が入っていた**（8月に試しに入れられたもの。
千里ホーム豊中リフォーム／インテリアエージェント／ラーテル メゾンドール高麗橋。奥田・池田・林）。
名簿の既定部隊とは別物。9/5・9/9で紫タグが出ていたのはこれ。

⚠️ **スマホは古い画面を持っている**（PWA）。直したのに直っていないように見えたら**まずリロード**。

#### ✅ 案件の停滞通知の宛先（直した）／❌ でも届かない理由がもう1つある

管理者サマリーの宛先が `.env` 空で**0人**だった → 社員DBの管理ロール3人へ落とす。
予定アラートと同じ穴（宛先の出どころが .env と社員DB で2つに割れている）。
テスト6件追加（234→240）、わざと壊して確認済み、本番反映・再起動済み。

🛑 **ただし、それでも1通も届かない。**

| | |
|---|---|
| Notionの案件 | **100件**（全部「進行中」） |
| うち「最終ボール更新日」が空欄 | **100件** |
| 日付が入っている案件 | **0件** |

コードは空欄の案件を停滞とみなさない（初期データ未整備での暴発防止）。
**全件空欄なので判定対象が0件。**コードの問題ではなく、
Notionの案件DBにその日付が一度も入っていないという運用側の話（利用者へ確認済み・回答待ち）。

#### 📝 気づいたが直していないこと

- 空き確認の日付欄に手で打つと `91220/02/06` のような**ありえない年**を受け付ける
- 日付一覧のモーダルが **Escapeで閉じない**（外側クリックでは閉じる）

### 3.17 組織再編に合わせて Notion社員DB に「役割」「部隊長」を足した（2026-08-29）

**利用者指示**
> 「PDF最初に渡したやろ。あれ見て、多分役職を変えてあげないといけないんじゃないか
>   と思うんやけど、俺の考え方どうやろ。新しい役職を作ってつけてあげることを
>   しないといけないかなと思ってる」

#### 渡されていた4本のPDF（プロジェクト直下・**着手前に必ず読む**）

| ファイル | 中身 |
|---|---|
| `GR組織運営方針書_Ver1.0.pdf` | **外向き＝事業部制／内向き＝施工部隊制**。指揮命令系統 代表→統括→副統括→部隊長→施工担当 |
| `GR各事業部組織図2026.pdf` | 全32人の役割分担一覧。誰がどの部隊長か |
| `GR組織再編_事前共有資料.pdf` | 各責任者への期待 |
| `GR_LINE運用再編_説明・実行方針_20260817.pdf` | **LINEグループの再編**。施工統括グループの新設 |

⚠️ **私は最初これを読まずに進めていた。**「PDF読んだ?」と言われて初めて読んだ。
渡された資料は着手前に全部読むこと（資格PDFのときと同じ失敗を繰り返した）。

#### 方針書の言い方は「役職」ではなく「役割」

> 今回の組織再編は役職変更が目的ではありません
> 役職ではなく役割を明確にし、誰が指示を出し、誰が責任を持つかを整理する

だから**既存の「役職」列は1文字も触っていない**（舟田さんの役職「舟田」もそのまま。
利用者確定「船田さんの役職は船田で大丈夫」）。**列を足した。**

#### 足した2列（Notion 社員DB `3520c840-b5b0-8102-91d8-000b9ca39dae`）

| 列 | 値 | 誰に入れるか |
|---|---|---|
| **役割** | 代表／統括／副統括／施工担当／工事管理・事務支援／営業・専門支援／出向 | 全員 |
| **部隊長** | 第一〜第六施工部隊 | **部隊長だけ** |

⚠️ **施工メンバーに部隊を固定で持たせないこと。** これが方針書の中心。
> 施工メンバーは固定所属を持たない／電気・通信・等で施工者を固定分類しない

だから列名を「部隊」ではなく **「部隊長」** にした。名前で用途が縛られる。
ここを崩すと、組織図を作り直しただけで運用は元に戻る。

**役割と部隊長は別の軸**（中島さんは 情報通信統括 かつ 第一部隊長）。1列にまとめない。

#### 入れた中身（10人・組織図と突き合わせ済み）

| 氏名 | 役職(従来・不変) | 役割 | 部隊長 |
|---|---|---|---|
| 小林 | 社長 | 代表 | — |
| 向 | デベロッパー | 統括 | — |
| 奥田 | 管理職 | 副統括 | 第六施工部隊 |
| 真柄 | 職人 | 施工担当 | — |
| 田中／小笠／繁田 | 事務 | 工事管理・事務支援 | — |
| 森／内藤／舟田 | 事務／舟田 | 営業・専門支援 | — |

#### ⚠️ 私が「未解決」と書いた件は、実は決着済みだった

**第六部隊長＝奥田** は **2026-08-27に別セッションが同じPDFを読んで利用者に確認済み**だった。
`gas.js` の `BUTAI_LEADERS` の上にそう書いてある:

> ※第六部隊の部隊長は資料間で食い違っていた（組織図＝奥田／方針書＝向）。
>   利用者に確認し「奥田が正しい」と確定（2026-08-27）。

**PDFを読まずに始めたせいで、決着済みの話を蒸し返した。**
着手前に `gas.js` の先頭コメントを読んでいれば防げた。

**前﨑さんの事業部** … `INF`→`ICT` に **修正済み（2026-08-29）**。
組織図で情報通信事業部の副統括＝**責任者**であり、組織図1行目が
「**事業部は責任者を固定**。施工者は全社共通リソース化」と明記しているため。
画面の職人名簿と同じAPI（`update_member_division`）で変更＝操作ログに残る。
⚠️ 氏名は **前﨑**（﨑）のまま。予定2,415行がこの表記。組織図の「前崎」は資料側の表記ゆれ
（利用者確認済み・2026-08-29「前﨑 これが正しい方」）。

#### ✅ 部隊長6人の「既定部隊」を入れた（2026-08-29・利用者承認「じゃあ入れよう」）

2026-08-27に仕組みだけ作って**データが空のまま**だった。37人全員が空で、
予定2,415行の部隊も全部空＝**依頼2番「部隊で絞り込む」が空振りしていた**。

| 部隊 | 部隊長 |
|---|---|
| 第一部隊 | 中島 |
| 第二部隊 | 前﨑 |
| 第三部隊 | 東 |
| 第四部隊 | 鈴木 |
| 第五部隊 | 高田（関東） |
| 第六部隊 | 奥田 |

画面の職人名簿と同じAPI（`update_member_butai`）で入れた＝操作ログに残る。
**検証: 37人中6人だけに入っている。全員が部隊長。施工メンバーへの誤登録なし。**

🛑 **施工メンバーには絶対に入れない。**依頼書「GRでは固定班ではなく、案件ごとに人員を
組み替える運用」／方針書「施工メンバーは固定所属を持たない」に真っ向から反する。
`gas.js` の `BUTAI_LEADERS` の上のコメントにも同じ警告がある。**6人を超えたら指示違反。**

既定部隊は**入力欄の初期値を出すだけ**（`index.html:2226` `refreshButaiField`）。
責任者を選ぶと部隊欄に入り、手で変えればそちらが優先。実際の値は**予定1件ごと**に保存される。
過去2,415行は空のまま（誰がどの部隊だったかの記録が無く、埋めると推測になるため）。

#### ★事業部と部隊は「別の軸」。1列にまとめない（利用者指摘 2026-08-29）

> 「その施工部隊というか、この部隊の話と事業部は、まあ、ちょっと別というか」
> 「奥田で言うたら六部隊長やけど、一応MISCの副統括やんか」

| | 事業部の軸（**外向き**） | 部隊の軸（**内向き**） |
|---|---|---|
| 役割 | 営業・受注・利益・対外責任 | 現場遂行 |
| 奥田 | MISC事業部 **副統括** | **第六部隊長** |
| 中島 | 情報通信事業部 **統括** | **第一部隊長** |
| 前﨑 | 情報通信事業部 **副統括** | **第二部隊長** |

**同じ人が両方を同時に持つ。**だから Notion も「役割」と「部隊長」を別々の列にした（§上記）。

#### 事業部が固定で付くのは「責任者」だけ

組織図1行目: **「事業部は責任者を固定。施工者は全社共通リソース化し、案件ごとに部隊長が選抜」**

だから施工者の事業部が組織図と違っても**それは間違いではない**（そもそも固定する対象ではない）。
私は 江頭・小笠・井村 の3人を同じ「間違い」として並べたが、性質が違った。

**アプリ上も「所属」ではない。** 名簿の事業部は
**工番を作るときの初期値を出すだけ**（`index.html:2417`。責任者を選ぶと
その人の事業部が工番の頭 `ICT-26-001` に初期表示される）。所属の宣言ではない。

#### 本社直結統括管理部隊（アプリに居場所が無い3人）

`組織運営方針書` の記載:
> ■ 本社直結統括管理部隊 ／ 統括：江頭 ／ 公共・特別支援：立田・井村

| 氏名 | 役割 | アプリの事業部 |
|---|---|---|
| 江頭 | **統括（責任者）** | ICT |
| 立田 | 公共・特別支援 | ICT |
| 井村 | 公共・特別支援 | GRB |

アプリの事業部コードは `ICT / INF / MSC / GRB` の4つだけで「本社直結」が無いため、
3人が ICT と GRB に散らばっている。

**🛑 利用者判断（2026-08-29）:「その事業部のあれは、いじらんでいいわ」**
→ **本社直結のコードは作らない。江頭・立田・井村はこのまま。**
   工番の初期値がずれるだけで実害が小さいため。蒸し返さないこと。

#### 未登録が22人いる

組織図32人に対し、Notion社員DBは**10人だけ**。
**中島・前崎・東・鈴木・高田・江頭が全員いない**（＝施工統括の中心メンバー）。
一方、**予定管理アプリの名簿37人にはこの6人とも入っている**（事業部も入力済み）。
LINEのuser_idは本人操作が要るため、名簿だけ先に作るか要相談。

#### アラートの宛先の答えもPDFにあった

`LINE運用再編` にこう書いてある:
> 部隊長・統括だけの『施工統括』を新設し、**人員配置と横断調整を行う**
> 施工統括会議：小林・中島・前崎・東・鈴木・高田・向・江頭

**予定の重複・人員配置はこの8人の仕事**＝毎朝のアラートの宛先はここ。
事務は「案件入口・営業・見積・受注」であって人員調整ではない（§3.16で私は事務を
宛先に推したが、それはPDFを読む前の誤り）。
ただし**8人中ボット登録は小林・向の2人だけ**なので、今切り替えると3人→2人に減る。
**未登録の6人が登録されてから切り替える。**

### 3.16 毎朝のアラート（要件9）と 候補者を出す（要件5）／2026-08-29

#### ⚠️ まず読むこと — 依頼文の8項目のうち3つは「代用」

要件9の原文はこう:

> **9. AIアラート** 毎朝、以下を自動確認する。
> ・人員重複 ・人員不足 ・空き人員 ・翌日の現場 ・未確定案件
> ・延期案件 ・資格不足 ・**移動時間が現実的でない予定**
> 問題があれば管理者へ通知する。

このうち3つは**今のデータでは額面どおりに出せない**。代わりに何を見ているかを、
コード（`cf/src/alerts.js` の先頭）と通知の文面の両方に書いてある。**黙って別物を出さないこと。**

| 依頼 | 出せない理由 | 代わりに見るもの |
|---|---|---|
| ~~人員不足~~ | ✅**2026-08-29に本物にした**（下記） | **いつもより人が少ない現場**＋責任者がいない現場 |
| 資格不足 | 現場マスタに「必要な資格」の欄が無い | **その日出る人の、まもなく切れる資格** |
| 移動時間 | 現場マスタに**住所が無い**。地図APIは有料 | **前後の日で本社↔関東支店をまたぐ人** |

⚠️ **「地図API」は依頼文に一言も書いていない。**「移動時間が現実的でない予定を確認しろ」
としか書いていない。地図はこちらが考えた手段であって依頼ではない（2026-08-29に利用者へ訂正済み）。
住所を入れて距離で見る場合も、**1回だけ緯度経度に変換して保存すれば通信費は毎回0円**にできる。
本当の負担は184現場ぶんの住所入力。

#### ✅ 「人員不足」を実績ベースで本物にした（2026-08-29）

**現場マスタに「必要人数」の欄は無い。** だが**欄を足しても184現場を手で埋めるまで効かない。**
そこで、その現場の**過去の実績から「いつも何人か」（日ごとの実人数の中央値）**を出し、
その日だけ大きく少なければ知らせる。**入力ゼロ・0円で翌日から効く。**

利用者判断（2026-08-29）: 「まあ、その方がいいんやったらそれでいこう」
（欄を作る案・AIで理由付けする案と3つ並べて比較したうえでの選択）

**しきい値は実データ130日で測って決めた**（勘で決めていない）:

| しきい値 | 全期間で通知が出る日 | これからの33日 | 判定 |
|---|---|---|---|
| いつもの50%未満 かつ2人差 | 3日 | 0日 | 鳴らなすぎ |
| **いつもの60%未満 かつ2人差** | **7日 / 130日** | **1日** | ✅採用 |
| いつもの60%未満 かつ3人差 | 0日 | 0日 | 鳴らなすぎ |

⚠️ **正直な限界。** 223現場のうち判定できるのは **5日以上の実績がある32現場だけ**。
1日で終わる現場（大多数）は「いつも」が決められないので**判定しない**。
長く続く現場の異常だけを拾う仕組みだと理解しておくこと。

**中央値を使う理由**: 平均だと1日だけ大人数を入れた日に基準が引っ張られる。

**🐞 テストが見つけた不具合**: 判定する日自身を「いつも」の計算に入れていた。
①その日が少ないと基準まで下がって鳴るべき日が鳴らない
②実績4日の現場がその日を足して5日になり判定対象になる → その日を除外して修正。

**🐞 わざと壊して見つけたテストの穴**: 割合の判定を消してもテストが緑のままだった
（人数差の判定だけが見張られていた）。**10人→8人で鳴らない**テストを追加して塞いだ。

#### 🐞 Codexレビューで見つかった6件（P1×2・P2×4）— 全部修正済み

**最初の実装はレビューを通していなかった。** 利用者に「レビュー回したんやんな」と
聞かれて初めて回した。**作ったら回すこと。今日は毎回P1が出ている。**

| | 何が起きるか | 直し方 |
|---|---|---|
| **P1** | **未来の予定が「いつも」に入っていた。** 毎朝は翌日を判定するので翌々日以降の予定が必ず基準に混ざる。実績4日の現場が未来1日で5日になり判定対象にもなる | 判定日より**前**だけを使う。しきい値も未来抜きで測り直した |
| **P1** | **現場キーに会社が無かった。** `company=全社` だと別会社の同名現場が合算され、他社の人数で「いつも」が水増しされて平常なのに誤報。**今日の午前に資格で指摘されたのと同じ種類のミスを繰り返した** | `rosterKey(会社)` を鍵に入れる（グローライズ+GRミツマは意図どおり統合される） |
| P2 | 昼勤と夜勤を合算していた。重複判定は昔から別枠なのに、ここだけ足して「いつも8人→昼だけの4人＝不足」と誤報 | 鍵に昼/夜を入れる。通知にも「（夜勤）」を出す |
| P2 | `作業区分='休み'` の行を現場として数えていた（夜勤列が空のまま休みの旧データが実在） | 非現場の作業区分に「休み」を追加。画面の `DASH_NOT_SITE` と同じ並びにした |
| P2 | 全期間の中央値なので工程変化に追従しない（着工期10人・終盤3人の現場で鳴り続ける） | 直近90日に限定 |
| P2 | 11件以上で見出しの件数と本文の行数が食い違う。どれが落ちるかも不定 | 「ほかN件」を出す＋**不足人数の多い順**に整列 |

**実データで見つけた誤報**: 現場名「事務所」の行が**464行、作業区分「その他」**で入っており、
作業区分だけ見ると現場として数えてしまう。バックテスト15件中**5件が「事務所 いつも9人→4人」**だった。
→ **現場名の完全一致**で除外（「倉庫材料準備」等の本物を巻き込まないため部分一致にしない）。

**未来を除いたバックテスト（129稼働日）**:

| しきい値 | 通知が出る日 |
|---|---|
| 50%未満・2人差 | 2日 |
| **60%未満・2人差** | **7日** ✅ |
| 60%未満・3人差 | 1日 |

窓の長さ（90日/60日/全期間）は現データでは差が出なかった。将来のために90日を採用。

**わざと壊す確認**: 6つの修正を1つずつ壊して全部赤くなることを確認した。
うち1件（作業区分の休み）は**最初のテストが壊しても緑のまま**だったため作り直した
（休みの4人を数えても数えなくても結果が同じテストになっていた）。

テスト 779 → **794**。

**本番で確認**: 未来を除いたバックテストと完全一致。これからの60日で出るのは1日だけ。
```
■ いつもより人が少ない現場 1件
・きんでん西 SB　大阪心斎橋筋G … いつも4人 → 2人   （2026-09-08）
```

判定は `cf/src/alerts.js` の `usualHeadcount()` と `SHORT_MIN_DAYS/RATIO/MIN_GAP`。
**過去の集計と当日の集計は必ず同じ条件（`isSiteWork`）を通す。ここがズレると誤報になる。**

#### 通知の量（ここが一番大事）

**問題が無い日は送らない**（利用者判断）。実データで測ったら **30日のうち4日**だけ通知が出た。
毎日届く通知は読まれなくなる（フェーズ2で実測して学んだこと）。

**★切れている資格・期限欄が読めない資格は毎朝の通知に出さない。**
出すと真柄さんの「高所作業認定（2024-05-31 切れ）」が**未来永劫、毎朝出続ける**。
利用者も「その期限切れの資格は一旦ほっといていい（元請さんの物だと思う）」と判断済み。
毎朝出すのは「60日以内に切れる」だけ＝**新しく起きた事**。切れている物は管理画面で見る。

#### 仕組み

```
D1（Cloudflare） → Worker GET /api/alerts → LINE Bot（VMのPython・毎朝6:00）→ 社員へpush
```

- 判定の本体 … `cf/src/alerts.js`
- 窓口 … `cf/src/index.js` の `/api/alerts`。**読み取り専用・鍵なし**
  （`/api/schedule` と同じ扱い。WorkerのURL自体は公開情報）
- Bot側 … `ラインボット/app/apps/automation/scheduler.py` の
  `send_schedule_alerts()` を `send_morning_jobs()`（毎朝6:00）に足した。
  **新しい鍵は要らない＝VMの .env を触らずに動く**（AIは.env編集禁止のため）。

#### 🐞 見る会社を「全社」にしていた（2026-08-29 利用者指摘・指示違反）

利用者の指摘: **「そもそも最初に、グローライズとGRミツマ（＝関東支店）を1つに
まとめましょうって指示あったよね。だから和信カインドとGRHDとラーテルは触らないって
言うたと思うんやけど」**

`SCHEDULE_ALERT_COMPANY` の既定を `全社` にしていた。**これは指示違反。**
触らないと言われた3社の重複まで管理者のLINEへ流れる状態だった。

実際どうなっていたか（2026-08-31 のデータ）:

| 会社の指定 | 名簿 | 重複 | 通知が出るか |
|---|---|---|---|
| 全社（当初の既定） | 59人 | 1件 | **出る** ← その重複は**和信カインドの人** |
| グローライズ | 37人 | 0件 | 出ない ← 正しい |
| 和信カインド | 17人 | 1件 | （触らない会社） |

**既定を `グローライズ` に変更した。** これで本社と関東支店の両方が入る
（拠点は会社とは別の軸。Worker側が旧GRミツマ＝関東支店をまとめて返す）。
実データで確認: グローライズ37人の予定行のうち **本社2,337行・関東支店78行**。
テスト `test_見る会社はグローライズ_触らない3社を巻き込まない` で戻されないよう固定した。

⚠️ **さっき「動いてます」と見せた8/31の例は和信カインドの人だった。**
会社の指定を確かめずに「動いた」と報告したのが誤り。

グローライズだけで150日ぶん（6/1〜10/28）走らせ直した結果 → **13日で通知が出る**。
関東支店の人（高田（関東））もちゃんと入っている。実際の文面:

```
【予定管理】08/22（土） の確認

■ 予定が重なっています 1件
・江頭 … きんでん東 ELLEGARDEN柏の葉 と きんでん東 RM新設工事　ELLEGARDEN柏の葉

■ 拠点をまたぐ移動 1件
・高田（関東） … 08/22（土）関東支店 → 08/23（日）本社

― この日の予定 ―
現場 5件 / 出る人 17人 / 空き 18人
```

#### 🐞 宛先0人だった（2026-08-29 利用者指摘・修正済み）

利用者の指摘: **「朝6時と9時は各自に届いてるのに、なんで宛先0人なん?」**

原因は**宛先の出どころが2つあった**こと。

| | 宛先をどこから取るか | 結果 |
|---|---|---|
| 既存の朝夕の個人配信 | **社員DB**（`list_employees()`） | 12人に届いていた |
| 足したアラート（当初） | **.env** の `SCHEDULE_ALERT_RECIPIENTS` | 誰も設定していない＝**0人** |

同じボットの中で宛先の出どころが2つあるのが間違い。既存の配信と同じ**社員DB**を見て、
**役職で管理者だけに絞る**形へ直した（`scheduler.py` の `manager_recipients()`）。

- 管理ロール ＝ `MANAGER_ROLES = ("社長", "デベロッパー", "舟田")`。
  ⚠️ 既存の `_is_manager()` は役職に「統括」が入っているかで見ているが、
  **本番の社員DBに「統括」は0人**だった（実機で確認）。使わないこと。
- 端末の探し方も既存配信と同じ順（`employee_devices` が正・無ければ旧 `line_user_id`）。
- **職人には送らない。**「重複しています」が59人に飛ぶと読まれなくなる。
- `.env` に書けばそちらが優先（宛先を変えたくなった時の上書き口として残してある）。

本番で確認: **0人 → 3人**。問題がある日で空打ちしたら3人ぶんの本文が出た。

宛先3人は社員DBの `会社` 欄が空。このボット自体が社内Bot（登録12人中グローライズ2・残り空欄）
なので、**会社での絞り込みは宛先側ではなく「見る会社」の設定で行う**（上記）。

⚠️ **同じ穴がもう1つある。** `notify_stalled_projects()`（案件の停滞通知）も
`PROJECT_STALL_RECIPIENTS` / `FUNADA_LINE_USER_ID` だけを見ていて、どちらも空。
**この機能は今、動いていても誰にも届いていない。** 別機能なので今回は直していない。

#### 🐞 Codexレビューで見つけて直した6件（2026-08-29）

| | 何が起きるか | 直し方 |
|---|---|---|
| P1 | **倉庫の人を「現場」「責任者なし」に数えていた。** 倉庫作業1行だけで「現場1件・責任者がいません」の誤通知が出る（再現済み） | 現場を数えるところで `r.souko` の行を落とす |
| P1 | **未確定案件・延期案件が通知の条件から抜けていた**＝依頼8項目のうち2つが実質未実装 | ただし「見積中◯件」を毎朝出すと読まれなくなる。**その日に人が入っているのに見積中のまま**（＝明日動くのに受注が決まっていない）だけを問題として出す。総数はまとめ行へ |
| P2 | **資格の警告が最大60日間ずっと毎朝出る作りだった**（「新しく起きた事だけ」と書いておきながら） | 節目の日だけに絞る `QUAL_NOTIFY_DAYS = [60,30,14,7,3,1,0]`。通知回数が1/8 |
| P2 | **同じ移動を2朝続けて通知していた**（前後どちらも見ていたため） | 「その日→翌日」だけにする。どの移動もちょうど1回・2日前に届く。本社↔関東支店に限る（「両方」は移動ではない） |
| P2 | **画面とWorkerで「夜勤」欄の読み方が違った。** Worker側だけ `trim()` していて、「休み␣」がWorkerでは休み・画面では通常勤務＝**同じデータで件数が食い違う** | 画面に合わせて `trim()` をやめた。両方を同じデータで動かして突き合わせるテストを追加 |
| P3 | テストの列数が23列（`HEADERS` に既に入っている物を二重に足していた） | 21列へ |

#### ★重複判定は「画面」と「通知」で同じ結果になること

`cf/src/alerts.js` の `findConflicts` は `index.html` の PHASE2-CONFLICT-RULE の**写し**。
`cf/test/alerts.test.js` が **両方を同じデータで実際に動かして JSON を突き合わせる**。
実際にこのテストが移植ミス（`butai` 項目の欠落）を1件見つけた。

#### 要件5（候補者を提案する）— AIは使っていない＝0円

依頼文が欲しいのは**候補者のリスト**。それは
**その日空いている ＋ 資格を持っている ＋ その元請の経験がある** で出せる。

空き確認に「元請の経験がある人を上に」を足した。実データでの見え方:

> 空いている 32人（「きんでん東」の経験が多い順）
> 江頭（84日）・加藤（74日）・中村（38日）・真柄（27日）・高田（関東）（20日）…

判定は `// ===== PHASE5-PICK-RULE:BEGIN =====`（2画面で同一）。
**★予定は勝手に作らない**（依頼文「最終決定は管理者が行う」）。テストで固定してある。

AIを足すとしたら「なぜこの人か」の文章と細かい順位付けだけ。そのときの費用は
**1回1円未満**が現実的な線（送るのは60人ぶんの名前と資格だけ）。

### 3.15 管理画面に「経営」を足した（要件10／2026-08-29）

**利用者判断**: 「普通に管理画面やね。admin.htmlに入れるべき。」

#### 依頼文の原文（これが仕様。§0の要約ではなく原文を残す）

> **10. 管理画面**
> 経営者が最初に確認する画面では、
> 「今日」… 稼働人数／空き人数／現場数／重複警告／未確定案件
> 「今週」… 人員稼働率／空き予定／案件予定
> が一目で確認できるようにする。

原文は「経営ダッシュボード」ではなく **「管理画面」**。8項目すべて指定済みだった。

#### やったこと

- `admin.html` の下部ナビの**先頭**に「📈 経営」タブを追加（`screen-dash`）
- **開いた最初に出るタブを 事務 → 経営 に変更**（原文「最初に確認する画面」に合わせた）
  → 元に戻すなら `switchTab('dash')` を `switchTab('jimu')` に戻すだけ
- **職人用(index.html)には入れない。** 経営の数字は現場に出さない

#### 画面の中身

| | 出すもの |
|---|---|
| 今日 | 稼働（現場◯／倉庫◯）／空き／現場の数／重複警告／未確定案件（見積中）／休み |
| 今週 | 人員稼働率／案件（動く現場）／**7日分の日別表**（稼働・空き・休み・現場） |

#### ★数え方の決めごと（ここを間違えると数字が信用されない）

- **空き確認の画面と同じ分類を使う。** 同じ「空き24人」が画面ごとに違うと誰も数字を信じない。
  `renderAvailDay` と同じ優先順位（現場に出ているが最優先／休みと出勤が両方なら出勤）。
- **稼働＋空き＋休み＝名簿の人数** が常に成り立つ。テストで固定してある。
- **稼働には倉庫作業も入れる**（仕事をしているため）。内訳は「現場◯／倉庫◯」で見せる。
- **現場の数**＝その日に動いている「元請＋現場名」の種類。休み・📌予定・ゴースト・現場名が空の行は数えない。
- **人員稼働率の分母は「名簿 × 予定が1件でもある日」**。
  日曜など誰も出ない日を分母に入れると実態より低く出て、率の意味が無くなる。
- **未確定案件＝現場マスタのステータスが「見積中」**（受注が決まっていないもの）。
- **重複警告はフェーズ2の判定をそのまま使う**（`currentConflicts()`）。別の数え方をしない。

判定は `// ===== PHASE4-DASH-RULE:BEGIN =====` 〜 `:END =====` の間（admin.html のみ）。
`cf/test/phase4-dash.test.js` がこの区間を取り出して **vmで実際に動かす**。

#### 🐞 タブを増やすと壊れる仕組み（既存の見張りが働いた）

`switchTab()` は `querySelectorAll('.tab')` と `tabs` 配列を**添字で**対応させている。
ボタンを1つ足すと押したタブと違う画面が開く。
既存テストは「`class="tab"` の個数」を決め打ちで数えていたが、それだと
**両方を同時に間違えたら通ってしまう**。
→ **ボタンの `onclick="switchTab('X')"` の並びと `tabs` 配列を突き合わせる**形に変えた
（わざと配列から1つ抜いて赤くなることを確認済み）。

#### 🐞 Codexレビューで見つかった[P1]2件（どちらも出していたら効かない物だった）

| # | 何が起きるか | 直し方 |
|---|---|---|
| 1 | **データが届いても数字が0のまま。** `loadData` の成功経路は `rerenderAll()` を通らないのに、テストは `rerenderAll` の中だけを見張っていた＝**テストは緑・画面はずっと0**。会社を切り替えた直後も前の会社の数字が残っていた | データが変わる3つの入口（`loadData` の成功／`rerenderAll`／`switchCompany`）すべてから `renderDash()` を呼ぶ。テストもその3か所を見る形に変えた |
| 2 | **拠点を押しても数字が変わらないのにラベルだけ拠点名が出る**＝「拠点で絞った数字」に見える。さらに**他社の見積中案件が混ざる**（グローライズ表示時は会社指定なしで全件取得するため） | 経営の画面では拠点バーを隠し、「本社＋関東支店の合計」と明記。未確定案件は `getGenbaMasterNames()` で今の会社の元請だけに絞る |

P2も3件直した:
- **事務所・倉庫作業・移動を「現場の数」に数えていた**（現場名さえ入っていれば1現場になっていた）→ `DASH_NOT_SITE` で明示的に除外
- **稼働率の分母に休みの人が入っていた**（有休が多い週ほど低く出る）→ 分母を「名簿−休み」に
- **無効の人の判定が会社をまたいでいた**（GRHDの奥田さんを無効にすると、グローライズの奥田さんまで名簿から消えていた）→ 会社ごとに判定。**空き確認の画面も同時に直った**

さらに、**「その日の状態」の分類を1つの関数（`dayStateByName`）にまとめた**。
空き確認と経営で別々に書いていたので、片方を直すと数字が食い違う作りだった。

#### スマホ

タブが7つになったが、幅390pxで各56px・はみ出し無しを実測（Chromeで計測）。

### 3.14 資格で人を選べるようにした（フェーズ3の土台／2026-08-28）

**利用者指示**
> 「資格書は…それで割り振ってくれる？」
> 「判断できるところを判断してやっといてくれる？」

§3.13で資格マスタ303件を入れたので、それを画面から使えるようにした。

#### やらないと判断したこと（費用と外部送信）

設計書のフェーズ3は3つある。**2つは利用者の判断が要るので手を付けていない。**

| | 判断 |
|---|---|
| 資格を使った人選 | ✅ **やった**。0円・画面の中だけ |
| AI配置提案（要件5の本体） | ❌ **保留**。Claude APIで月数百円かかる。§2「お金がかかる物は最初に言う」 |
| 毎朝のアラート（要件9） | ❌ **保留**。LINE/メールで外に送る。設計書にも「通知先は利用者に確認してから」とある |

#### 何ができるようになったか

- **空き確認**に「資格で絞る」を足した。その日に動ける人を、資格を持っている人だけに絞れる
- **有効期限が切れた資格では「持っている」と数えない**。切れた資格で現場に出せないため
- 期限が **切れた/60日以内/読めない** 資格を一覧で出す

#### データの流れ（新しく足した所）

```
資格マスタ(303行) → gas.js readQualifications_ → doGet の qualifications
   → cf/src/sync.js sanitizeForStorage → D1 → cf/src/read.js filterSnapshot
   → index.html / admin.html の allQuals → PHASE3-QUAL-RULE の判定
```

**★個人情報は入口で削る。** 資格マスタの 免許番号・正式氏名・取得日・出典 は
`projectQualifications_`（gas.js）で落とし、**GASを出る時点で 氏名/会社/資格名/区分/有効期限 だけ**にする。
現場画面はPINが無く全社員が使い、内容はD1と端末のlocalStorageにも残るため。
歯止めは3枚（GAS投影 → Workerの再投影 → 画面の `qualSafe`）。

判定は `// ===== PHASE3-QUAL-RULE:BEGIN =====` 〜 `:END =====` の間。
**2画面で1文字も違ってはいけない**（`cf/test/phase3-qual.test.js` が vm で実際に動かして検査）。

#### 🐞 Codexレビューで見つかった[P1]3件（実データで壊れる物ばかりだった）

| # | 何が起きるか | 直し方 |
|---|---|---|
| 1 | **読めない有効期限が「期限なし＝一生有効」に化ける**。`2026-02-31` のような存在しない日もJSが勝手に3/3へ繰り上げて通していた | 実在する日か検査し、読めない値は `'?'`。画面では `unknown` 扱いにして**「持っている」と数えない**（安全側） |
| 2 | **氏名だけで索引していた**。奥田さんはグローライズとGRHDの両方に実在し、川端さんもグローライズとラーテルにいる。**他社の同名の人の資格で有資格と判定されていた** | 会社込みの鍵 `qualKey(company,name)` にした。グローライズとGRミツマだけ束ねる |
| 3 | 選んでいた資格が一覧から消えると、**黙って「絞り込みなし」に戻って全員表示に化けていた** | 選択は残し「この日に使える人はいません」と見せる。偽DOMで実際に動かすテスト（`phase3-qual-select.test.js`）を追加 |

P2も5件直した（D1の空上書き防止・localStorageのホワイトリスト・「今日」を日本時間に固定 ほか）。

#### ✅ 本番で確認したこと（2026-08-28 深夜）

GAS **v63** ／ Cloudflare Worker ／ GitHub Pages を全部反映し、実物で確認した。

| 確認 | 結果 |
|---|---|
| GASの応答（curl） | 資格303件。項目は `company/expires/kind/name/qual` の5つだけ。**免許番号は1文字も出ていない** |
| D1（Worker経由） | グローライズ 303件（うち旧GRミツマ26件も出る） |
| 和信カインド・ラーテル・GRHD | **どれも0件**＝他社に漏れていない |
| 現場画面（Chrome実機） | 「玉掛け」で絞ると **9人中4人が空き**（立田・向・藤木・舟田）と出た |
| 管理画面（PIN 8800） | 同じく303件。資格の選択肢100種類 |

#### 📌 実データで分かったこと（事務へ伝えること）

- **有効期限がすでに切れている資格が8件**
  河原/第一種工事検査員（2023-08-28）・川端（達）/高所作業認定・江頭/高所作業認定・
  真柄/班長認定・真柄/高所作業認定（いずれも2024-05-31）・加藤/現場責任者（2024-06-14）・
  川端（達）/班長認定（2026-05-01）
  → **この資格が要る現場には、今は出せない**（画面がそう判定する）
- **有効期限の欄が読めない資格が11件**（中島・藤木・鈴木・中村・林・小笠・井村・
  田中（智）・高橋・繁田×2 の「高所作業認定」）
  欄に `発行日より3年` と書いてあり、日付が入っていない。
  → 画面には「有効期限の欄が読めません（資格マスタを直してください）」と出る。
    **資格マスタに実際の期限日を入れれば消える。**
- `2029/3/31(合格した年から5年後の3/31)` のように**日付＋カッコ書き**の欄が7件あり、
  これは日付として読めるようにした（あと施工アンカー施工士）。

#### ⚠️ 資格マスタに「会社=GRミツマ」の行が26件残っている

江頭さん・繁田さんの分。§3.12の統合より前に取り込んだため。
**コード側で吸収してあるので実害は無い**（`qualCompanyKey` と `cf/src/read.js` の
両方でグローライズと同じ束として扱う）。データを直すなら資格マスタのB列だけ、
「完全に一致するセル」「このシート」で置換すること。
**⚠️「すべてのシート」でやると変更履歴・操作ログの記録まで書き換わる。絶対にやらない。**

### 3.13 資格マスタを作った（2026-08-28・303件を投入）

**利用者指示**
> 「資格書は今からお前に教えるから、それで割り振ってくれる？」
> 「\ds218j-glorise\グローライズ情報通信部_事務業務関連_従業員関連_資格証
>   この中に資格書入ってるから、ちゃんと見て名前通りに割り振ってくれる？ミスんなよ。」

要件5「AI人員配置提案」が **「資格・経験のデータが0件」で止まっていた**。その材料を入れた。

**スプレッドシートに `資格マスタ` シートを新設（303件）**
列 = 氏名 / 会社 / 正式氏名 / 資格名 / 区分 / 免許番号 / 取得日 / 有効期限 / 出典

出典はNAS上の `資格者証一覧(GR最新).xlsx`（1人1シート・34人分）。

#### 名寄せの結果（機械で決めず、判断が要る分は利用者に確認した）

| 分類 | 人数 | 扱い |
|---|---|---|
| 1人にはっきり決まる | 18人 | そのまま割り当て |
| 職人マスタに同姓が複数 | 4人 | **利用者に確認して決めた**（下記） |
| 職人マスタに居ない | 12人 | 利用者判断「**入れない**」 |

**利用者に確認して決めた4人**
- 資格ファイルの `川端` → **グローライズの `川端（達）`**。
  > 「お、違うぞ。ラーテルの川端はネットGの川端じゃないぞ。川端達がネットGやぞ。」
  > 「ネットGから来てるっていうか、グローライズからネットGに応援に行ってる人で、
  >   あくまでもグローライズの人間やからね、その川端（達）は。」
- 資格ファイルの `江頭` / `繁田` → グローライズ（§3.12で二重登録を1人にまとめた側）
- 資格ファイルの `奥田` → グローライズ（GRHDの奥田とは別人）

#### 🐞 投入時にやらかして直したこと

1. **免許番号の先頭のゼロが消えた**（2人分。`006`→`6` のように頭の0が落ちた）。
   Googleスプレッドシートは「書式なしテキスト」を先に設定しても貼り付けで効かない。
   → 免許番号・取得日・有効期限に `'`（アポストロフィ）を付けたTSVで貼り直し、実際のセルで頭の0が戻ったのを目視確認。
2. **ふりがなが本文に混ざった**（`職長+安責者　能力向上ショクチョウアンセキシャノウリョクコウジョウ`）。
   xlsxの `sharedStrings.xml` は `<si>` の中に本文 `<t>` とふりがな `<rPh>` を両方持つ。
   全部の `<t>` をつなぐと混ざる。→ `rPh` の中は読み飛ばすようにした。
3. **元請マスタの A1 に範囲名を打ち込んで見出しを消した**（名前ボックスと間違えた）。
   すぐ元に戻し、A1は `元請名` に復旧済み。

#### ⚠️ この先の注意

- **氏名・資格の中身はGitに入れない。** 個人情報なのでスプレッドシートにだけ置く。
  公開リポジトリ（`akirasp4-lgtm/sinkarenda-`）のファイルには1文字も書いていない。
- **画面はまだ無い。** データが入っただけ。資格での絞り込み・割り振りはこれから。

### 3.11 見つける（フェーズ2／2026-08-27・✅本番リリース済み）

**要件2（絞り込み）・要件3（人員重複チェック）・要件4（空き人員）・要件8の後半 を実装。**
**画面だけで完結させた。gas.js・Cloudflare Worker・スプレッドシートは1文字も触っていない。**
判定に必要な列（作業区分・夜勤・元請・現場名・氏名・日付）はフェーズ1で21列に揃っていて、
すべて `allNippos` に載っているため。失敗しても画面を戻すだけで済む。

計画書 → `docs/superpowers/plans/2026-08-27-zensha-jinin-haichi-phase2.md`

#### ⭐ 重複の判定ルール（これが本体）

> 同じ人・同じ日に、**作業区分が現場系**（`現場作業` `置局` `着打ち` `撤去品返却`）で
> **元請＋現場名が異なる**行が2件以上ある。ただし昼と夜勤は別枠。
> `夜勤`列が `予定` `休み` の行は対象外。ゴースト行も対象外。

**ゆるいルールにしなかった理由（実データ2,664行で数えた）**

| 案 | 件数 | なぜ採らないか |
|---|---|---|
| 同じ人・同じ日に2件以上 | 250件 | 「現場＋安全会議」「現場＋事務所」など正常が大半 |
| 出退勤の時間が重なる | 217件 | `08:00-17:00` の定型なのでほぼ全部が該当 |
| 人工の合計が1.0超 | 189件 | 人工は給料の数字。全行が1で時間の意味を持たない |
| **同じ日に別々の現場系** | **47件**（今日以降12件） | ✅ 物理的に不可能。**これだけが本物** |

**毎日200件出る警告は誰も読まない。** 読まれる数まで絞るのが目的。

コードは `index.html` / `admin.html` の
`// ===== PHASE2-CONFLICT-RULE:BEGIN =====` 〜 `:END =====` の間。
**DOMに一切触らない純関数だけ**にしてあり、`cf/test/phase2-conflict.test.js` が
その区間だけを取り出して **vmで実際に動かして** 検査する（正規表現の見張りでは
「書いてあるが動かない」を通してしまうため）。
**2つの画面でこのブロックは1文字も違ってはいけない**（違えばテストが落ちる）。

#### 🐞 実機で見つけた欠陥（自分で見つけて直した）

**本社⇔関東をまたぐ重複を丸ごと見逃していた。**
判定を会社ごとに分けていたため、§3.9で1つの名簿に統合したはずの
グローライズとGRミツマが別々に扱われていた。

| 判定方法 | 全期間 | 今日以降 |
|---|---|---|
| 会社で分ける（誤） | 39件 | 11件 |
| **1つの名簿として見る（正）** | **47件** | **12件** |

差の8件はすべて本社⇔関東をまたぐ本物の重複。設計書§1.1の実測値47/12と一致するのは後者。
→ `rosterKey()` を足して直した。**判定側の名簿一覧と画面の `KYOTEN_COMPANIES` が
食い違ったら落ちるテスト**を入れてある（また分かれるのを防ぐため）。

> **教訓**: 「会社」と「名簿」は別の概念。会社は法人、名簿は一緒に配置する人の集まり。
> 拠点を入れた時点でこの2つはずれた。**人を突き合わせるときは必ず名簿の側で見る。**

#### 作った物

| 場所 | 何ができるか |
|---|---|
| カレンダーの見出しの下 | 赤いバナー「同じ日に別の現場に入っている人が N 件」→ タップで一覧 |
| 予定の登録・編集 | 保存前の警告を新ルールへ差し替え。**どの現場と重なるか**まで出す |
| 現場別管理の4つ目 | **🔎 詳しく探す**（社員名／部隊／元請／現場名の一部／作業区分／日付の範囲） |
| 空き確認の上 | 日付を選ぶと**空いている人の名前**。休み・倉庫・出勤の人数も |
| 空き確認の上（同じ枠） | 現場が**延期・中止**になった人を別枠で（要件8の後半） |

#### 🔑 守っていること（触るときに壊しやすい所）

1. **重複と空き人員の母集団は `companyNippos()`＝拠点で絞らない。**
   本社ビューで見ていても、その人が関東の現場と重なっていれば重複。
   実機で確認済み: 拠点を切り替えても空き14人・重複1件のまま変わらない。
   一方 **「詳しく探す」は `filteredNippos()`＝拠点を効かせる**（探す目的が違う）。
2. **`class="tab"` を新しく足さない。** `switchTab()` が
   `querySelectorAll('.tab')` と配列を**添字で対応**させているので、1つ増えるだけで
   下部ナビの選択表示が全部ずれる。トグルは `.btn-secondary` + `.active` を使う。
   （index.html 4個 / admin.html 6個 をテストで固定）
3. **検索結果に一括編集・一括削除を出さない。** 結果は複数の現場・複数の日にまたがるので、
   そこで一括削除を押せると関係のない予定まで消える。専用の枠 `#gm-search-result` に出し、
   一括編集付きの `#gm-list-wrap` とは同時に表示しない。
4. **拠点の絞り込みを2つ置かない。** 見出しの下の切替が唯一の拠点操作。
5. 元請と現場名は**制御文字で区切ってから**突き合わせる。区切らないと
   「きんでん」+「西A現場」と「きんでん西」+「A現場」が同じに見えて重複を見逃す。

#### ✅ 実機で確認したこと（Chrome・2026-08-27）

- 重複バナー: グローライズ名簿の今日以降 **1件**（8/27 江頭「きんでん東／三鈴産業」＋
  「きんでん東／RM新設工事　三鈴産業所有地」）。タップで一覧が開き、編集・削除は無い
- 全社の数字が設計書の実測値と**完全一致**（47件／今日以降12件）
- 保存時の警告を実データで4通り試した:
  同じ現場に班員追加→出ない ／ **同じ日に事務所→出ない（今までは出ていた）** ／
  同じ日に別の現場→**出る** ／ 別の日に別の現場→出ない
- 詳しく探す: 条件なし1105件 → 中島108 → ＋現場作業80 → ＋8月17。
  元請=きんでん西156 → ＋現場名に「松原」5。クリアで1105に戻る。削除ボタンは無い
- 拠点の切替が検索に効く（全拠点1105／本社1035／関東70＝合計一致）
- 空き確認: 8/27 は**空き14人・休み1人・倉庫0人・出勤22人**＝名簿37人と一致。週の表とも一致
- バナーは高さ44px（指で押せる大きさ）・横スクロールなし・コンソールエラー0

#### 🚩 フェーズ2でやっていないこと

- 延期・中止の実データがまだ無い（現場マスタは施工中178／完工7）。
  **使い始めれば出る**が、今は0件なので実機で見た目を確認できていない
- 部隊での絞り込みは動くが、**部隊の値がまだ全件空**なので実質使えない（割当は利用者待ち）
- フェーズ3（AI配置提案・毎朝アラート）／フェーズ4（経営ダッシュボード）

---

### 3.10 部隊・案件ステータス・変更履歴（フェーズ1／2026-08-27・✅本番リリース済み）

**依頼**: 利用者からの10項目のブリーフ（全社横断のAI人員配置・予定管理システム）
**設計書**: `docs/superpowers/specs/2026-08-27-zensha-jinin-haichi-design.md`
**実装計画**: `docs/superpowers/plans/2026-08-27-zensha-jinin-haichi-phase1.md`
**Codexレビュー**: `_local/review/codex_out3_20260827.txt`（設計段階・9件）／
`_local/review/codex_impl_20260827.txt`（実装差分）

#### 依頼10項目の扱い（実データ2,664行を確認した結果）

| # | 依頼 | 判定 |
|---|---|---|
| 1 | 全社統合予定表（本社/関東/全社の3切替） | ✅ **2026-08-26に完成済み**（§3.9） |
| 2 | 絞り込み（社員名・部隊・支店・案件名・元請・日付・工事種別） | ✅ **フェーズ2で作った**（§3.11「詳しく探す」） |
| 3 | 人員重複チェック | ✅ **フェーズ2で作った**（§3.11。保存時の警告とカレンダーのバナー） |
| 4 | 空き人員検索 | ✅ **フェーズ2で作った**（§3.11。日付を選ぶと名前が出る） |
| 5 | AI人員配置提案 | ✅ **完成（§3.18）**。候補出しは0円（空き×資格×経験）、AIでの順位付け・理由付けも本番稼働中。**氏名は送らない**（c1・c2に置換）。**実測 1回0.05円**。予定は作らない |
| 6 | 本社⇔関東の応援対応 | ✅ **2026-08-26に完成済み** |
| 7 | 変更履歴 | ✅ **フェーズ1で作った** |
| 8 | 案件ステータス8段階 | ✅ **フェーズ1で作った**（延期・中止で空いた人の一覧は§3.11） |
| 9 | 毎朝のアラート | ✅ **2026-08-29に完成**（§3.16。LINE Botの毎朝6:00に相乗り。問題が無い日は送らない）。見る会社＝**グローライズ（本社＋関東支店）のみ**／宛先＝社員DBの管理ロール3人（社長/デベロッパー/舟田）。**8項目のうち3つは代用＝§3.16を必ず読むこと** |
| 10 | 管理画面（経営者が最初に見る画面） | ✅ **2026-08-29に作った**（§3.15。admin.htmlの先頭タブ「経営」） |

加えて社長が2026-08-24に決めていた **1〜4部隊 / 代表者→責任者** をフェーズ1に含めた。

#### 🔴 要件3「重複チェック」は今のデータでは判定できない（最重要の発見）

額面どおり作ると**狼少年になる**。本番2,664行で実測した結果:

| 判定のしかた | ヒット | 中身 |
|---|---|---|
| 同じ人・同じ日に2件以上 | **250件** | 「現場＋安全会議」「現場＋事務所」など正常が大半 |
| 出勤〜退勤の時間が重なる | **217件** | 出退勤は99%入力済みだが `08:00-17:00` の定型。ほぼ全部重なる |
| 人工の合計が1.0超 | 189件 | 人工は給料の数字。**全行が1**で時間の意味を持たない |
| **同じ日に別々の「現場作業」系** | **47件**（今日以降12件） | ✅ 物理的に不可能。**これだけが本物** |

**フェーズ2で採用する判定ルール（設計書 §1.1 に確定済み）**:
> 同じ人・同じ日に、作業区分が現場系（`現場作業` `置局` `着打ち` `撤去品返却`）で
> 元請＋現場名が異なる行が2件以上ある。昼と夜勤は別枠。`夜勤`列が `予定` `休み` の行は対象外。

#### 作ったもの

**日報データ 20列 → 21列**（`gas.js:16`）。21列目 `部隊`。値は `1部隊`〜`4部隊` と空欄。
**★部隊は人ではなく予定1件ごとに持つ**（GRは固定班ではなく案件ごとに人員を組み替えるため。
社長の「同じ責任者が1部隊と2部隊を同時に持つ」ケースをそのまま表現できる）。

**職人マスタ 4列 → 6列**: `既定部隊`（入力の初期値用）／`有効`（`×` で空き人員リストから外す）。
admin の職人管理から編集できる（`update_member_butai` / `update_member_active`）。

**現場マスタ 11列 → 12列**: `ステータス`（見積中/受注/準備中/施工中/残工事/完工/延期/中止）。
既存の `完了` 列は残し**従属**させる（集計エンジン1,500行が参照しているため）。
移行は**読むときに導出**する方式で既存184件は無書き換え。

**新シート `変更履歴`**（8列）: 日時/操作/旧ID/新ID/項目/変更前/変更後/実行者。
admin に「履歴」タブを追加。**削除は21列すべてをJSONで残すので復元できる**。

**役割の呼称**: 画面表示だけ `責任者` / `班員` に。**保存値の `代表` / `同行` は1文字も変えていない**。

**データ掃除** `cleanupMastersPhase1()`: 既定でdry-run。`cleanupMastersPhase1_APPLY()` で実行。

#### ⚠️ 絶対に守ること

**1. 保存値 `代表` / `同行` を変えてはいけない**
`=== '代表'` という比較が **gas.js / index.html / admin.html に合計39箇所**ある。
1箇所でも漏らすと `gas.js` の `buildDailyValues_` の代表者判定が壊れ、
**事業部が決まらず工番が空で保存される**。
`cf/test/ui-wiring.test.js` がこの39という数を見張っている。

**2. 案件ステータスを旧APIで書いてはいけない**
`update_site_status`（completed の真偽値だけ）を残すと、
管理者が設定した「延期」「中止」が「完工」「施工中」へ勝手に戻る。
職人用の「完了にする」ボタンは撤去済み。GAS側もステータス設定済みなら拒否する。

**3. 変更履歴は「書いてから原本を触る」**
`update` / `delete` は **値を作る → 履歴を書く（flushまで確認）→ 原本を触る** の順。
履歴が書けなければ原本を1行も動かさずエラーで返す。
`add` だけは握りつぶす（失うものが無く、止めると新規登録が全部死ぬため）。

**4. 部隊の割当表はまだ無い（利用者しか知らない情報）**
誰が1〜4部隊かはコードからも実データからも導けない。**推測で埋めてはいけない。**
フェーズ1は仕組みだけ作り、中身は空で出す。利用者が職人管理の画面で1人ずつ設定する。
既存2,664行のバックフィルは割当表が決まってから別途。
その際の注意: 鍵は氏名だけでなく **(会社, 氏名)** ／対象は現場系の予定だけ ／
**日報データとアーカイブの両方**に流す ／ 必ず dry-run を先に出す。

**5. 「予定が0件の14人」を機械で無効化してはいけない**
`川端` `井上` `作本` `児玉` `杉本仁（兄）` `いくや` など**実在の職人が混ざっている**。
予定が無いことと人でないことは別の話。`cleanupMastersPhase1` は候補一覧を出すだけ。

#### 🔍 レビューと自己検証で見つけた欠陥

**Codexの設計レビュー（着手前・9件／致命4・重要4・軽微1）**
1. [P1] `parseRows`/`groupNippos` が部隊を捨て、無関係な編集で部隊が消える（拠点と同じ形）
2. [P1] `logHistory_` が失敗を握りつぶし、旧行削除後に履歴を書いていた
3. [P1] 削除履歴が要約だけで復元不能
4. [P1] 掃除が重複行を先勝ちで捨て、単価（給料）を失う恐れ／`/応援/` で無効化を推測
5. [P2] 旧行の逆順を考慮せず、ID変更のみで鎖が切れる
6. [P2] 部隊の割当表が存在しないのに流し込む計画だった
7. [P2] `index.html` の完了ボタンが8段階ステータスを上書きする
8. [P2] キャッシュ名は `settingsMembers`（`memberCache` は存在しない）／新2列を未読込
9. [P3] テストが `validate` を使用（実物は `validateGasPayload`）

**自己レビューで見つけたもの（着手前・6件）**
- `vm` に読み込んでも `const` はコンテキストの属性にならない → テスト方式が丸ごと動かなかった
- `archiveOldData_` がアーカイブ側に `ensureHeaders_` を呼ばず21列目の見出しが空になる
- `diffDailyRows_` の鍵が同一人・同一日の複数行（本番250件）で衝突する
- 掃除が clearContent → setValues の順で、途中で落ちると職人マスタが空になる
- `saveSnapshot` が職人の項目を3つに絞り、キャッシュ起動時だけ既定部隊が空になる
- 既定部隊を編集する画面が無く「自動で入る」が発動しない

**実装中に自分で見つけたもの（3件）**
- 🔴 **部隊がGASに送られていなかった**（`members` には載せたが `rows.push` に載せ忘れ）
  ＝画面では選べるのに1件も保存されない。**再発防止に `cf/test/ui-wiring.test.js` を新設**
- **完了列の実値は `✓` で `TRUE` ではない** → `TRUE` だけで判定すると完工済み7件が「施工中」に化ける
- 文字化けの直し方が甘く、全文字が化けている場合に**文字数だけで会社を決めていた**
  （`?????` → `GRミツマ`）→ 手がかりが半分以上残っている場合だけ直すよう修正

**本番の実機テストで見つけたもの（2件・v56で修正）**
- **変更履歴の「実行者」が空だった** — 画面からの直接送信（`index.html:2495` /
  `admin.html:3242`）はトップレベルの `updatedBy` を送らず行の中にだけ入れている。
  依頼⑦の「誰が登録・変更・削除したか」を満たせていなかった →
  `doPost` で行から拾って補う（送信経路に依らず埋まる）
- **削除履歴の日付が `Sun Dec 31 2028 00:00:00 GMT+0900` だった** — シートから読んだ値は
  Date型で `rowFullJson_`/`rowSummary_` が素の `String()` を使っていた。人が読めず復元も難しい
  → `2028-12-31` / `08:00` と画面と同じ表記に揃える

#### ⚠️ 私（Claude）の誤報を1件訂正（2026-08-27）

**「会社名が文字化けした行が1件ある」と報告したが、これは誤りだった。**
私の端末の文字表示の問題で、実データは全部正常。
本番の実行（`cleanupMastersPhase1`）でも `会社名修正: 0件` と出ている。
文字コードで確認した実際の値: グローライズ 2,285 / 和信カインド 255 / GRミツマ 85 / GRHD 39 ＝ 2,664。

**教訓**: 端末に出た文字が化けて見えても、それがデータの問題とは限らない。
**文字コード（コードポイント）で確かめるまで「データが壊れている」と言わない。**
`fixMojibakeCompany_` は残してあるが、実際には1件も直していない（無害）。

#### テスト

**270件 → 391件**（121件追加）。
- `cf/test/gas-phase1.test.js` — 純粋関数（`vm` に読み込む。★`const` は末尾の export 用1行で取り出す）
- `cf/test/gas-integration.test.js` — 偽スプレッドシート上で `buildDailyValues_` を実際に動かす
- `cf/test/ui-wiring.test.js` — 画面の配線もれを機械的に見張る（**バグを捕まえられることを実証済み**）

#### ✅ リリース記録（2026-08-27 実施・すべて本番に出ている）

| 順 | 内容 | 結果 |
|---|---|---|
| 0 | スプレッドシートをコピーしてバックアップ | ✅ 「【バックアップ】2026-08-27 フェーズ1データ掃除の前 運用カレンダー」<br>ID `13kp8iy-AKxP0cx-D4GR9jIHJ-VcHDO4JQHMtrHwqw90` |
| 1 | Cloudflare を出す（無影響） | ✅ 件数2,664のまま変化なし |
| 2 | GAS を出す | ✅ **バージョン55**（デプロイIDは不変）。21列 `車両/拠点/部隊` を確認 |
| 3 | `cleanupMastersPhase1()` dry-run → `_APPLY()` | ✅ 重複統合4・追加1・**食い違い0**。行数2,664のまま。職人71→68 |
| 4 | D1へ取り込み | ✅ Cronが取り込み、職人68人が反映 |
| 5 | 画面を出す `git push` | ✅ GitHub Pages ビルド完了 |
| 6 | Chromeで実機確認 | ✅ 下記 |
| 7 | 実機テストで見つけた2件を修正 | ✅ **バージョン56** |

⚠️ **`/api/sync` を curl で叩くと Origin制限で弾かれる**（`許可されていないOriginからのリクエストです`）。
5分ごとのCronが自動で取り込むので、待てばよい。

#### 🏢 部隊は「第一〜第六」（正式資料に基づく・2026-08-27 修正）

**利用者から正式資料4点を受領**し、実装が組織の実態と食い違っていたことが判明。
社長の8/24打合せメモは「1〜4部隊」だったが、**2026年の正式な組織は6部隊**。
全2,664行の部隊がまだ空のうちに直したので**破壊ゼロ**（v57）。

出典（`予定管理アプリ作成/` 直下に置いてある）:
`GR各事業部組織図2026.pdf` / `GR組織運営方針書_Ver1.0.pdf` /
`GR組織再編_事前共有資料.pdf` / `GR_LINE運用再編_説明・実行方針_20260817.pdf`

| 部隊 | 部隊長 | 事業部 | 職人マスタの表記 |
|---|---|---|---|
| 第一部隊 | 中島 健太郎 | 情報通信事業部（統括：中島／副統括：前崎） | `中島`（グローライズ） |
| 第二部隊 | 前崎 蓮 | 情報通信事業部 | **`前﨑`**（★﨑。組織図の「前崎」と字が違う） |
| 第三部隊 | 東 努 | 都市インフラ事業部（統括：東／副統括：鈴木） | `東`（グローライズ） |
| 第四部隊 | 鈴木 陸央 | 都市インフラ事業部 | `鈴木`（グローライズ） |
| 第五部隊 | 高田 真也 | 関東支店（統括：高田） | `高田`（**GRミツマ**） |
| 第六部隊 | 奥田 翔貴 | MISC事業部（統括：向／副統括：奥田） | `奥田`（グローライズ） |

⚠️ **第六部隊は資料間で食い違っていた**（組織図＝奥田／運営方針書・事前共有＝向）。
利用者に確認し **「奥田が正しい」** と確定（2026-08-27）。

**★★設計原則2が正式資料で裏付けられた**

組織運営方針書 §5・§10 が明記:
> 「施工メンバーは固定所属を持たない」
> 「案件ごとに部隊長が最適なメンバーを選抜・編成する」
> 「誰が所属かではなく、誰がその案件に最適かでチームを編成する」
> 「部隊長だけ固定。施工メンバーは全社共通」

＝**部隊を人ではなく予定1件ごとに持たせた判断は正しかった。**
帰結として **職人マスタの「既定部隊」を入れてよいのは部隊長6人だけ**。
施工技術者12名に既定部隊を入れると、この運用方針に真っ向から逆らうことになる。

**組織図の名前と職人マスタの照合（2026-08-27 実施）**

- ✅ 一致: 中島／東／鈴木／高田／奥田／向／河原／真柄／中村／藤木／立田／井村／林／
  小笠／繁田／高橋／田中（智）／舟田／森／池田／内藤／江頭／吉元／柳澤／内村
- ⚠️ **字が違う**: 組織図「前崎」→ マスタ「**前﨑**」／LINE方針「栁澤」→ マスタに `柳澤` と `栁澤` の**両方がある**
- ⚠️ **マスタに無い**: `瀬古`（施工技術者12名の1人。**予定を入れられない**）／`野間`（楽天出向）／`小林`（代表・職人ではないので当然）
- ⚠️ **同一人物が二重登録の疑い（GRミツマ×グローライズ）**:
  `高田`(GRミツマ) と `GRME髙田`(グローライズ)／`柳澤` と `栁澤` と `GRME栁澤`／
  `内村`(GRミツマ) と `GRME内村`(グローライズ)
  → ✅ **2026-08-28に解消済み（§3.12）**。高田（関東）・柳澤（関東）・内村（関東）は1人にまとめ、
    江頭・繁田の二重登録も1行にした。職人マスタは64行→62行。
- ✅ **「予定0件の15人」の正体が一部判明**: `川端`(ラーテル)＝組織図の「（ネットジー）川端＝出向支援」、
  `内村`＝「関東：内村＝施工技術者」。**どちらも実在の人**。機械で無効化しなかったのは正しかった

#### ✅ 実機で確認したこと（2026-08-27）

- GAS応答が **21列**（`車両/拠点/部隊`）／member に `butai`・`active`／jobsite に `status`
- 画面: 部隊の欄・「そのまま」の出し分け・**責任者/班員**のラベル・部隊タグ・履歴タブ
- **職人用から「この現場を完了にする」ボタンが消えている**（管理者の延期を上書きしない）
- 案件ステータス: **完工7 / 施工中177**（`✓` の実値を正しく読めている）
- 本番で1件登録→削除し、**部隊「3部隊」・拠点「関東支店」が実際に保存された**ことを確認
- 変更履歴に **実行者「向」** と **21項目すべて**が読める形で残ることを確認
- テスト行はすべて削除済み。**本番データは2,664行のまま無傷**

⚠️ **curlで411エラーが返っても、GASには届いて書き込まれている**ことを実測した
（テスト行が3件できた）。引き継ぎ書§3.6の「エラーが返ったのに書き込まれていた」の再現。
**エラーを見て再送する前に、必ず入っていないか確認すること。**

#### ✅ 二重登録の統合を本実行（2026-08-27 21:32・完了）

利用者指示「その二重は確かに二重なので高田（関東）のように変えておいて下さい」を実行した。

| | 行数 | 氏名の変更 | 更新者の変更 |
|---|---|---|---|
| 日報データ | 2,675 | 116 | 76 |
| アーカイブ | 1,083 | 0 | 0 |
| 職人マスタ | 68 → **64**（4人分をまとめた） | | |

- 内訳（事前の実測と1件も違わなかった）: GRミツマ\|高田62 ／ グローライズ\|GRME髙田40 ／
  GRミツマ\|柳澤8 ／ GRミツマ\|栁澤1 ／ グローライズ\|GRME栁澤3 ／ GRミツマ\|内村2 ＝ 116
- **食い違い0・中止理由なし・行数の増減0**。会社は1つも書き換えていない
- 変更履歴に `merge_members` が8行（氏名の統合7＋更新者の統合1・実行者 `system`）
- 画面の「空き確認」で **高田（関東）／柳澤（関東）／内村（関東）が1人1行**になったことを実機確認。
  コンソールエラー0
- バックアップ: `14938Bs-4_3I2ebA-fkMrLEuFUM-iw-r9htuszqlZEYA`（統合の前）

**メモ欄の "GRME栁澤" 等36件は触っていない。** 「GR請 ミツマに発注分 GRME栁澤、協力2名」のような
人が書いた文章で、機械で置換すると意味が変わるため。直したのは **氏名列と更新者列だけ**。

#### 🐞 実機で見つけた欠陥（2026-08-27・修正済み commit 2c999b1）

**職人マスタの A1 は空欄**（昔からずっと。`getOrCreateMemberSheet_` も3〜6列目しか直していない）。
それを知らずに「1列目が氏名」を必須にしていたため、実機のdry-runが中止した。

- テストの偽シートが `['氏名','会社',…]` と**本番と違う形**だったので気付けなかった
- 偽シートを本番と同じ形（A1が空欄）に直した瞬間 **9件が落ちて**、実機の中止をテストで再現できた
- 1列目は「空欄 または 氏名」を許すように緩めた。**2列目の `会社` は引き続き必須**
  （本人の判定を(会社,氏名)の組でやるので、ここが違うと別人を巻き込む）
- 別の形のシートでは今も1文字も書かずに止まることをテストで固定した

> **教訓**: 偽スプレッドシートは「本番の実際の形」に合わせる。
> きれいな形で作ると、本番でしか出ない不整合を最後まで見逃す。

#### ⚠️ Apps Scriptエディタの「実行する関数」が選べない（この環境での既知の障害）

関数を選んでもラベルが `doPost` に戻り、**実行すると別の関数が動く**。実際に、
`mergeDuplicateMembers_APPLY` を選んだのに dry-run が動いた（＝書き込みは起きなかった）。

- **回避策**: スプレッドシートに一時的な `onOpen` メニュー（GR保守）を作り、そこから実行した。
  確認ダイアログ付き。**作業後にこのブロックは消してある**（必要になったらまた足す）
- **鉄則**: 実行・デプロイの前に、選択が画面に反映されているか必ず目で確かめる。
  デプロイでも同じ現象が起きた（「新バージョン」を選んだのに v58 のままだった）。
  座標クリックは効かず、**要素参照でのクリックなら通る**

#### 📌 その他この日にやったこと

- **GAS バージョン59 をデプロイ**（デプロイIDもURLも変更なし）
- テスト行1件（2028-12-29 `__テスト__` / `__6部隊確認__`）を**スプレッドシートから直接削除**。
  アプリの削除は `confirm()` を使うため、背面タブだとダイアログが操作できずタブごと固まる。
  **この1行の削除は変更履歴に残っていない**（自分で作ったテスト行のため）
- 現在の本番: 日報データ **2,674行** ／ 職人マスタ **64人** ／ 部隊はまだ全件空

#### 🚩 やっていないこと
- 部隊の割当表の投入（利用者が決める）
- 既存2,664行への部隊のバックフィル
- フェーズ2（絞り込み・重複チェック・空き人員）／フェーズ3（AI提案・毎朝アラート）／
  フェーズ4（経営ダッシュボード）
- 要件9の「移動時間が現実的でない予定」— **現場マスタに住所の列が無い**。
  住所の入力と地図API（有料）が前提

---

### 3.9 拠点（本社／関東支店）を新設し、グローライズとGRミツマを1本に統合（2026-08-26・✅本番稼働）

**依頼**: `calendar_request_20260826/calendar_request_20260826.md`（向井さん宛の改修依頼書）
**設計書**: `docs/superpowers/specs/2026-08-26-kyoten-honsha-kanto-design.md`

**依頼の核心**
> 会社名で分けるのではなく、「本社／関東支店」という管理区分を別で持たせること。

法人（グローライズ／GRミツマ）と拠点（本社／関東支店）を**別の軸**にした。
GRミツマを関東支店として運用していても、将来GRミツマ法人で本社案件を持つことは
ありうるため、片方から他方を導出しない。**値は行ごとに保存する。**

**★★利用者指定（最重要・絶対に壊さないこと）**
> 「関東は、今のカレンダーでいうミツマとグローライズだけの話。
>   ラーテルと和信カインドは混ぜたらあかん」

拠点の軸は **グローライズとGRミツマの2社限定**。和信カインド・ラーテル・GRHD は
拠点ビューに1件も出さない。他社の行に誤って拠点が入っていても出さない（構造的に防止）。
社長用（president.html）も対象外＝別シートで拠点の概念が無い。

**結果（本番実測）**

| ビュー | 件数 | 中身 |
|---|---|---|
| 全拠点 | 2,331 | グローライズ2,246 ＋ GRミツマ85（**統合された1つのカレンダー**） |
| 本社 | 2,246 | グローライズのみ |
| 関東支店 | 85 | GRミツマのみ |
| 和信カインド等を選択時 | 従来どおり | 拠点バーも拠点欄も出ない |

- 「両方」は1件登録で本社・関東の**両方の画面に出る**（依頼書③の二重登録廃止）
- 全拠点ビューでは予定に「本社」「関東」「両」の印が付く（依頼書②）
- 現場を選ぶと現場マスタの拠点が自動で入る＝**日々の入力は増えない**
- 職人名簿もグローライズ＋GRミツマで1つに統合（空き確認の食い違いを解消）

**入れたもの**
- `gas.js`: `HEADERS` 19→20列（`拠点`）／現場マスタ11列目に `拠点`／`resolveKyoten_`
  （画面の明示値 > 現場マスタ > 会社の既定値。**他社は常に空欄**）／`backfillKyoten()`
- `cf/src/sync.js`: ヘッダ検査を「先頭19列一致＋20列目は必ず拠点」へ
- `cf/src/read.js`: `matchKyoten`（両方は両ビュー・他社は拠点ビューに出さない）
- `cf/src/index.js`: `/api/schedule?kyoten=`
- `index.html` / `admin.html`: 拠点バー・拠点欄・グループ統合・名簿統合

**⛔ 次に触る人が絶対にやってはいけないこと**

1. **画面をGASより先に出さない。** 新画面は `kyoten` を送るが19列のGASは保存しない。
   利用者が選んだ「関東支店」「両方」が**復元不能に消える**。他の順番違いは
   「遅くなる」だけで原本は無傷。復元不能なのはこの1つ。
2. **`KYOTEN_COMPANIES` に和信カインド・ラーテル・GRHDを足さない**（利用者指定）。
3. **`backfillKyoten()` を現場マスタ優先に戻さない。** 現場名だけをキーにしているため
   同名衝突で誤った拠点を大量確定させ、再実行では「既に入っている」扱いで直せない。

**反映の順番（実施済み）**: Worker → GAS(v54) → 過去データ埋め → D1同期の確認 → 画面

**過去データ**: 2,625行中2,331行を会社から一括で埋めた（グローライズ→本社2,246／
GRミツマ→関東支店85）。**和信カインド255行・GRHD39行は空欄のまま**。行数は変わらず。
バックアップ: `_local/backup_20260826_kyoten/gas_before_backfill.json`

#### 🔍 レビューと実機確認で見つけた欠陥

**Codexレビュー（本番に出す前に実施）[P1]9件・[P2]4件** — 主なもの:
- 編集モーダルが既存の拠点を読み込まず、時間を直しただけで拠点が本社へ移動していた
- 全社ビューから編集すると拠点が常に空になり、選んだ「両方」が失われていた
- 拠点ビューの切替が入力途中のフォームを書き換えていた
- 拠点列が無い取り込み（移行途中）でWorkerが他社を素通ししていた＝最重要要件の違反
- 画面をGASより先に出すと復元不能、という順番の危険

**Chrome実機確認で見つけた3件（自動テストでは出なかった）**
1. **関東支店ビューが常に0件**。会社の絞り込みがグローライズのままで、GRミツマの予定を
   そもそも読み込んでいなかった。**依頼書の核心が解決していなかった**
2. 他事業の会社に切り替えても、フォームの拠点欄が出たまま残っていた
3. 関東支店ビューの空き確認に、グローライズの職人だけが並び全員「空き」に見えていた

**教訓**: 1と3は「画面の会社選択と取得パラメータの噛み合わせ」の問題で、
**自動テストでは構造的に検出できなかった**。実機で全ビュー・全タブを回して初めて分かった。

#### 🎨 拠点の切替UIは1度作り直している（2026-08-26・利用者指摘）

**最初の作り（失敗）**: 会社バーの下に固定表示（`position:fixed; top:41px`）。
利用者から3点の指摘:
1. 「使い方が隠れている」— `.screen` の上余白は会社バー用の52pxのままで、
   拠点バーが41〜74pxを占めるため、見出しと「使い方」ボタンを覆っていた
2. 「拠点が上過ぎて選びにくい」— 画面最上部で親指が届かない
3. 「スマホが特に見にくい」— チップが文字12px・高さ約26pxと小さかった

**今の作り**: 固定をやめ、**今見ている画面の見出しのすぐ下**へJSで移動する
（`placeKyotenBar()`）。中身と一緒に流れるので何も隠さない。3つを横並びの
大きなボタン（高さ44px以上・文字15px）にした。タブを切り替えたらその画面へ移る。

**実測（スマホ幅390px）**: ボタン1つ113×44px・文字の折り返しなし・
見出しと使い方ボタンの両方が見えている。4タブすべてで見出しの直後に付くことを確認。

**次に触る人へ**: 固定表示に戻すなら `.screen` の `padding-top`（現在52px／
PC版60px）も必ず一緒に増やすこと。ここを忘れたのが今回の原因。

#### 🚩 今回やっていないこと（依頼書④・器だけ）

- 売上・粗利の拠点別集計。現場マスタに拠点を持たせるところまで。集計エンジン
  （1,500行）には手を入れていない。金額機能は2026-06-11の決定で「寝かせる」扱いのため
- 「案件名・担当者・顧客」の紐付け（依頼書①の後半）。現場マスタに既にある項目との
  差分を実データで確認してから
- 現場マスタの拠点をアプリから編集するUI（今はスプレッドシート直編集）


### 3.0 レスポンス改善（2026-08-21）

**きっかけ**: 利用者から「反応が遅すぎてストレス」。実測したら**1回のデータ読み込みに9.1〜14.6秒**、
毎回1.22MB・2,607行を全部送っていた（見ているのは1ヶ月分）。

**原因は3つ**
1. `doGet` が全行を組み立ててから会社で捨てていた（期間で絞る仕組みも無し）
2. 登録/編集/削除のたびに `await loadData()` で全件読み直し＝**1操作で書き込み+全件読込の二重待ち**
3. `Utilities.formatDate` を 2,600行×3箇所＝**約8,000回**呼んでいた（GASで特に重い命令）

**やったこと**
- gas.js: `fmtDate_`/`fmtTime_`/`fmtDateTime_` を追加（TZ一致を実行時に検査し、ズレたら従来へ自動フォールバック）
- gas.js: `doGet` を「会社・期間で絞ってから組み立て」に変更。`dateFrom`/`dateTo` 対応（未指定なら全件＝後方互換）
- gas.js: `?compact=1` で行を「値だけの配列」で返す形式を追加（項目名は headers で1回だけ）。
  **未指定なら従来形式**なので、画面とGASのどちらが古くても壊れない
- index.html / admin.html: 保存後の `await loadData()` を廃止。`rerenderAll` / `removeLocalByIds` /
  `addLocalRows` / `refreshInBackground` を追加し、画面を先に更新してサーバ照合は裏で走らせる
- gas.js: `archiveOldData_` の1行ずつ `appendRow`/`deleteRow` を**一括処理**へ（setValues 1回＋連続ブロックの deleteRows）
- gas.js: アーカイブのトリガーを月1回→**毎週日曜3時**。`checkTriggers()` を追加

**結果（実ブラウザで同じ測り方）**

| | 修正前 | 修正後 |
|---|---|---|
| アプリを開く | 平均 **9.7秒** | 平均 **3.9秒** |
| 保存・編集・削除の待ち | 約10秒 | **ほぼゼロ** |

**アーカイブについて（重要）**
- 「動いていなかった」わけではなかった。データが 2026-05-02 始まりなので、
  前回トリガー日 2026-08-01 時点では3ヶ月より古い行が**0件**＝消す物が無かっただけ。
- **本当に危なかったのは実装**。1行ずつ処理は数百行で6分の実行上限に達して落ちる作りで、
  9/1に634行、10/1に1,322行が対象になる＝**初回で失敗して以後ずっと溜まり続けるコース**だった。
- 2026-08-21に一括処理版で **410行を実行済み**（全社 2,991→2,581行、最古 2026-05-02→2026-05-22）。本番で正常動作を実証。

**バックアップ**: 実行前の全社2,991行を `_local/backup_20260821/` に保存。

**compact=1 は 2026-08-21 中にデプロイ済み**（利用者がオーナーアカウントで実施）。転送量 1,039,821→617,926バイト（41%減）を確認。

---

### 3.0b 🔥 GASの不安定さ（実測して判明・速度より重大）

本番の doGet を連続で叩いて分かったこと:

| 回 | 所要 | 結果 |
|---|---|---|
| 1 | 56.3秒 | 正常 |
| 2 | 11.7秒 | **HTTP 404**（Googleの「ページが見つかりません」8,019バイト） |
| 3 | 32.4秒 | 正常 |
| 4 | 5.2秒 | 正常 |
| 5 | 3.9秒 | 正常 |

- **5回に1回ほど HTTP404 が返る。**混雑時にはロック時間切れ「現在予定を更新中です」も返る。
- 所要時間が 3.9秒〜56秒と大きくばらつく。**GAS側の性質でコードでは根治できない。**
- 8,019バイトの応答＝この404。速度計測でこのサイズが出たら中身を必ず確認すること
  （エラーページは速く返るので「速くなった」と誤読する。実際に一度誤読しかけた）。

**対策として画面側に入れたもの（2026-08-21 commit d4b7ee8）**
1. `loadData` を**2回まで自動で取り直す**。1回目が404でも利用者には見えないまま復帰する。
2. それでも失敗したら、**前回のキャッシュを赤い警告付きで表示し続ける**。
   従来は `allNippos=[]` にしていたため**画面が真っ白**になっていた。
3. 失敗時も `dataLoadOk` は false のまま＝**登録・保存は不可**。ダブり防止の安全装置は維持。

### 3.0c 起動時キャッシュ（commit c6be498）

`localStorage` に前回の読み込み結果を保存し、起動直後に描いてから裏で最新に差し替える。

- **起動時のキャッシュ描画 42〜45ms**（従来は4〜50秒の空白画面）
- キャッシュ 423KB（compact形式のまま保存）
- **単価(rate)は端末に保存しない**。職人マスタは name/company/division のみ。
  給料情報を全端末の localStorage に残さないため（意図的に捨てている）
- キャッシュ表示中も `dataLoadOk` は false＝見るだけ。登録・保存は最新到着後
- 検証済み: 通信を人工的に失敗させても件数2,261が維持され、赤バッジが出て、
  登録は禁止されたまま、復帰すると自動でバッジが消える

### 3.0e 集計をアプリ内で閲覧できるようにした（2026-08-22 commit d292384 / a160a14）

**背景**: 利用者から「実際にシートを見に行っている人が数名いる。Excelを落とさないと
確認できないので、シートを覗くほうが早い」との指摘。移行するとシートが表から見えなく
なって困る、という懸念でもあった。

**実装**: 事務タブに「集計をこの画面で見る」を追加。生成済みの集計シートを、そのままの
並び（▶月見出しの色分け・合計行の強調）で画面に描く。横スクロール対応。
「集計を更新して見る」で再集計してから表示もできる。ダウンロードは記録用として従来どおり残す。

| 画面 | 見られるもの |
|---|---|
| admin.html（PIN付き） | 月別確認表 / 会社別集計 / 月別集計 / **元請別請求集計** |
| index.html | 月別確認表 / 会社別集計 / 月別集計（**金額を含む3つ以外**） |

**セキュリティ**: 2026-06-11の給料漏れ対策と同じ線引きを維持。元請別請求集計（金額あり）と
職人マスタ（単価列）は現場画面に出さない。**新しい公開範囲は一切増やしていない**（実機で確認済み）。

**事業部別按分は意図的に除外**。引き継ぎ書のとおり現在の数字はテストの残骸であり本番データ
ではないため、画面に出すと誤解を招く。復活させるなら中身を入れ替えてから。

**タブは増やしていない**。switchTab がボタンの並び順に依存する構造（§4の警告）のため、
タブ追加は避け、admin.html の起動時に最初に開く事務タブ内に置いた。

**実測**: 会社別集計68行=2.3秒 / 月別確認表141行=2.6秒 / **元請別請求集計1,590行=2.5秒**。

**⏳ 未確認**: index.html の事務タブは今も `isAdminMode` のとき以外は非表示。つまり
**管理者モードでない職人は、この集計ビューアを開けない**。シートを覗いていた「数名」に
職人が含まれるなら、職人にも見せるかどうかの判断が要る（金額なしの3つだけなら追加露出はゼロ）。

### 3.0f 期間の出面を画面で即表示（2026-08-22 commit 1136f66 / 3b3c774）

**利用者から聞いた実運用（設計の前提として重要）**
- このアプリの出発点は「TimeTreeの入力を事務員がExcelへ手打ちで書き写して、
  誰がどの現場に何日出たかを計算していた」作業の自動化。
- **10日ごと（1〜10 / 11〜20 / 21〜末）に「あなたは何日出ました」を事務員がLINEで送る。**
  そのたびにExcelを出力してスクショを撮っていた。
- 「Excelを出力しなくてもパッと見れる情報」と「証拠として残すためのExcel出力」は**別物**。
  前者が無いのが不満だった。

**実装**: 事務タブに「期間の出面をパッと見る（10日締め用・Excel不要）」を追加。
- クイックボタン: 1〜10日 / 11〜20日 / 21日〜末 / 今月 / 先月
- 職人×日付の人工マトリクス＋各人の合計。名前列は横スクロールで固定
- **サーバに問い合わせない**（手元の allNippos から計算）ので押した瞬間に出る
- admin.html には「この期間でExcel（証拠用）」を併設。画面で見た期間のまま出力できる

**計算規則**（GASの generatePeriodKakuninData_ / generateKakuninTable_ と同一にしてある）
- 休み / 予定 は除外
- 同じ人の同じ日は「昼の最大値 + 夜勤の最大値」（重複入力を二重に数えない）
- 倉庫は通常勤務として数える
- 表示用ゴースト(isGhost)は除外（GAS側には存在しないため。**ここを忘れると数字がずれる**）

**検証済み（2026-08-22 実機）**: 2026年8月・全社で、画面版とGAS生成の月別確認表を突き合わせ。
**40名全員一致、総合計556人工も一致。**

> ⚠️ **運用上の注意（事務員に伝えること）**
> 画面版は**生データ**を見ているので常に最新。集計シート／Excelは**最後に「集計更新」を
> 押した時点**の内容。検証時も 6:37 時点の古いシートと比べて12名がズレて見えた
> （集計更新後は完全一致）。**画面とExcelが違って見えたら、まず集計更新を押す。**

### 3.0g 集計ビューアの対象シート（2026-08-22 利用者指定で確定）

利用者が「表に出してほしい」と指定した6点。残り（日報データ・アーカイブ・各マスタ・
操作ログ）は「プログラム的に動いているもののログだから必要ない」との判断。

| 画面 | 見られるもの |
|---|---|
| admin.html（PIN付き） | 元請別請求集計_フィルタ用 / 事業部別按分 / 元請別請求集計 / 月別確認表 / 会社別集計 / 月別集計 |
| index.html | 月別確認表 / 会社別集計 / 月別集計（**金額を含む3つは出さない**） |

**セキュリティ**: 2026-06-11の給料漏れ対策と同じ線引きを維持。金額を含む3つと職人マスタ
（単価列）は現場画面に出さない。**新しい公開範囲は増やしていない**（実機で確認済み）。

**事業部別按分**: 一度は除外していた（引き継ぎ書に「数字はテストの残骸＝本番データでは
ない・2026-06-11利用者確認」の記録があるため）が、利用者の明示指示により追加。
**選択時に画面へ注意書きを出している。**中身を本番データに入れ替えたら注意書きは外してよい。

### 3.0d 保存の実測（1件登録して計測・後片付け済み）

| | |
|---|---|
| サーバへ書き込む往復 | **4.35秒** |
| 画面が書き換わる | **0.046秒** |
| 合計（体感） | **約4.4秒**（修正前は約14秒） |

⚠️ セッション中に「保存はほぼゼロ秒」と説明したが**誤り**。正しくは約14秒→約4.4秒。
4.35秒はGASがスプレッドシートに書きに行く時間で、GASのままでは消せない。



### 3.1 工番重複対策（案A＋C）
**問題**: 「○○現場 材料引取」のような表記揺れで同じ現場に複数工番が振られる。

**対策**:
1. **案A: 作業区分ゲート** — 現場作業の時だけ工番発行
2. **案C: 現場名プルダウン** — 既存現場から選ぶ／新規は明示
3. **管理側マージ機能** — 既存の重複を統合できる

GAS側条件:
```js
row.company === GROWISE
  && !row.souko && !row.yotei && !row.yasumi
  && row.workType === '現場作業'
```

### 3.2 元請マージ機能
**ファイル**: gas.js + admin.html
**GAS action**: `merge_genba`
**動作**: 日報・アーカイブ・現場マスタ・元請マスタの「元請名」をfrom→toに置換、from側マスタは削除（重複なら）

**UI（admin.html 事務タブ）**:
- 青背景の「🔀 元請名を統合（マージ）」ブロック
- 「元請名をマージする」ボタン → モーダル表示
- 統合元・残す側のドロップダウン
- プレビュー（影響件数表示）
- 確認ダイアログ後に実行

**実行済みマージ**: グローライズ → グローライズ自社（日報側0件、マスタ側は重複削除）

### 3.3 撤去品返却
作業区分に追加。工番は発行されない。

### 3.4 社長予定表
- 「時間しか見えない」問題 → タイトル優先表示に変更
- セルはみ出し → min-width:0; overflow:hidden で抑制
- 月曜始まりに統一

### 3.5 車両予約 GAS連携（ラインボット向け）
返信ファイル: `ラインボットへの返信.md`
- 4アクション実装: `vehicle_res_add` / `update` / `delete` / `list`
- トークン: `車両予約用トークン1234`（`VEHICLE_RES_TOKEN`）
- シート「車両予約」14列（自動作成）
- 既存カレンダー機能には一切手を入れていない
- **Phase 2（カレンダーに車両予約表示）は未実装**

### 3.6 保存を「押した瞬間」に＝案A（楽観的表示＋裏送信）（2026-08-25）

> ⚠️ **2026-08-26 訂正**: 本節に「ブランチ `feat/optimistic-save`・未push」とあったが、実際には **main にマージ済み・origin/main へ push 済み**（未pushコミット0件・`git rev-list --count origin/main..main` で確認）。つまり社員用の楽観的保存は**すでに本番(GitHub Pages)で動いている**。

**きっかけ**: 社長打合せ（2026-08-24）で「保存が遅い、そんなお客さんに出されへん」。

**何を変えたか**
- **新規登録（`submitNippo`）だけ**、保存が押した瞬間に完了して見えるようにした。体感 **約4.4秒 → 0秒**。
- 送信は画面の裏で続ける。届くまでは予定に⏳（未送信）の印が付き、画面上部に「未送信 N件」の帯が出る。届いたら印・帯とも黙って消える。
- 対象は新規登録のみ。**編集・削除は従来どおり**（返事を待つ）。

**なぜこの形にしたか（実際に速くする、ではなく順番の入れ替え）**
保存4.4秒の内訳を計測: GASの固定オーバーヘッド（69行の小さいシートを読むだけ）が**約1.0〜1.2秒**、実際の書き込み（マスタ読み直し＋2シートへの書き込み）が**約3秒**。
**GASの固定オーバーヘッド（約1秒）は構造上どうやっても消えない**＝「実際に速くする」路線には壁がある。
一方、送信ボタンを押した瞬間には送るデータが（IDまで含めて）もう全部そろっている。待っている4.4秒は「GASが受け取ったという返事」を待っているだけ。
→ **返事を待たずに先に画面へ出す＝順番の入れ替えで体感だけを0秒にできる。**
⚠️ **書き込みそのものは速くなっていない。**スプレッドシートへの実際の書き込みは今後も**約4秒**かかる。速くなったのは「利用者が待たされる感覚」だけ。

**仕組み**
- `send-queue.js`（新規）— 未送信の登録を端末（localStorage）に貯める箱。DOM・fetchに触らない純ロジックなので `cf/test/send-queue.test.js` から vitest でテストできる（`sync-guard.js` と同じ方針）。
- 画面側（`index.html`/`admin.html`）の送信係 `drainQueue` が箱から1件ずつ取り出してGASへ送る。
- **2回目以降の送信（＝一度失敗して再送するとき）だけ**、送る直前にGASから最新データを読み直し「もう入っていないか」を確認してから送る。**初回送信では確認しない**（毎回の登録に余計な待ちを足さないため）。
  根拠: 本番で実測したところ**「送信側にエラー画面が返ったのに、実際は書き込まれていた」が実際に発生した**。再確認なしで再送すると二重登録になる。
- 未送信は 5秒→15秒→45秒→2分→5分 とバックオフしながら自動再送。**10回失敗すると自動再送を止め、帯が赤くなって手動再送に切り替わる**（勝手には捨てない）。

**触ったファイル**: `send-queue.js`（新規）/ `index.html` / `admin.html` / `cf/test/send-queue.test.js`（新規）。
**`gas.js`・Cloudflare（`cf/src/`）・D1・`backend.json` には一切触っていない。**

**戻し方**: 上記4ファイルの変更のみなので、**`git revert` して push すれば約1分で元に戻る**（GitHub Pages反映）。サーバー側（GAS・D1）の巻き戻しは一切不要。

**未送信の見せ方**
- 予定カードに⏳の印。画面上部に sticky の帯「⏳ 未送信 N件（自動で送信中）」。送信できたら印・帯とも黙って消える。
- 10回失敗して諦めた分があるときは帯が赤くなり「⚠ N件が送信できていません。タップして再送してください」に変わる（タップで手動再送）。

**未送信の予定は編集・削除できない**（送信が終わるまで）。理由:
- 編集すると `saveEdit` が新しいIDで別行を直送してしまい、後から元の未送信も届くと「編集前」と「編集後」が両方サーバーに残る（二重登録）。
- 削除すると、サーバーにまだ無い行を消そうとするだけで何も起きず、その後未送信が届いて**消したはずの予定が復活する**。
- ⚠️ **この観点は設計書にも実装計画にも無かった**。レビューで3ラウンドかけて見つかった（下記「見つかった欠陥」参照）。「未送信を取り消す」機能は作っていない（YAGNI。通常は数秒〜数分で送信が終わるため）。

**未送信も人工の集計（10日締めの出面表）に数える**（2026-08-25 利用者判断。「本人の予定であり、数分後には必ず送信されるため」）。

**参照**:
- 設計書: `docs/superpowers/specs/2026-08-25-optimistic-save-design.md`
- 実装計画: `docs/superpowers/plans/2026-08-25-optimistic-save.md`
- 作業台帳（裁定の全記録・一次資料）: `.superpowers/sdd/2026-08-25-optimistic-save/progress.md`
- 自動テスト: **191件PASS**（既存153件＋今回追加38件。このセッションで再実行して確認済み）

#### ⚠️ まだ確認していないこと（実ブラウザ・実機でしか確認できない／台帳に「未実施」と明記）

**これらは「確認して問題なかった」のではなく、まだ誰も確認していない。**

- 帯の実際の見た目（`position:sticky` を body 先頭に挿すレイアウト・既存ヘッダとの重なり）
- 鮮度警告のバッジ（`cal-stale-badge`・fixed・z-index 9999）と未送信の帯（z-index 60）を同時に出したとき、**赤い警告が帯を覆う可能性**
- **実機スマホ（iOS Safari・ホーム画面のPWA）でのダブルタップ**（実装担当が確認したのはデスクトップChromeのみ）
- iOSで Safari のタブとホーム画面のPWAで localStorage が共有されない件（未送信がタブとPWAで別々に見える可能性）
- **機内モードでの通し確認**（登録→未送信のまま残る→リロードしても消えない→電波復帰で自動送信）は**まだ誰もやっていない**
- GAS の `add` と `update` のロック到着順

→ 本番へ出す前に、上記を実機（できればiOSのSafariタブとホーム画面PWA両方）で一通り確認すること。

#### この作業で見つかった欠陥（記録）

| # | 欠陥 | どこで見つかったか | 何が起きるはずだったか |
|---|---|---|---|
| 1 | 別タブの未送信が消える | 着手前スキャン（実装前） | 別タブで登録した予定が、他タブの書き込みで丸ごと上書きされて消える＝登録が黙って失われる |
| 2 | 壊れた項目があると一覧を読むだけで例外を投げる | Task1レビュー | `list()`/`pendingRows()` が落ち、帯の表示だけでなく送信ループ自体が止まる＝未送信が永久に送られない |
| 3 | 保存領域が満杯のとき**箱ごと消していた** | Task2レビュー（実装担当の自己申告を検証） | quotaで書き込み失敗時にキーを丸ごと `removeItem`。以後そのタブは二度と送らず、**未送信は閉じた瞬間に完全に失われる** |
| 4 | 「全件を1つのキーにまとめて丸ごと書き戻す」形では二重登録の窓を原理的に防げない | Task2 修正ラウンド1・2（2回とも塞ぎきれず） | 別タブの書き込みを巻き込む競合が窓を狭めても消えなかったため、**保存の形を1件＝1キーへ作り替えた** |
| 5 | **未送信の予定を編集・削除できてしまう** | Task4レビュー（3ラウンド） | 編集→新IDで別行を直送し二重登録／削除→サーバー未着のため何も消えず、後で未送信が届いて予定が復活 |
| 6 | 複数日登録の**2日目以降がガードをすり抜ける** | Task4 修正ラウンド2・3 | `isGroupPending` がキュー項目のid（初日分のみ）と照合しており、2日目以降は編集・削除ガードを素通り |

**この作業で見つかった欠陥の大半は、私（AI）の設計・計画に由来するもので、実装担当の逸脱ではなかった。**
（レビューを重ねて見つけている＝レビューを省いてよいという意味ではない）

---

### 3.7 社長用カレンダーを社員用と同じ速さにした（2026-08-26・✅本番リリース済み）

**きっかけ**: 利用者「社員用のカレンダーの速度は上がってるけど、社長用は全然上がってない。同じにしてほしい。**予定とか混ぜたらあかんけどな**」。

**判明した事実**: `president.html` は最終更新 2026-06-15 で、**8月に社員用へ入れた高速化が1つも入っていなかった**。
社員用の改善は2段階（8/21＝①〜③、8/25＝④）だが、社長用はどちらも未適用だった。

| # | 中身 | 実施 |
|---|---|---|
| ① | 保存・削除の後の全件読み直し廃止（画面を先に直し、照合は裏で） | ✅ |
| ② | 起動時キャッシュ（localStorage・前回の内容を即描画） | ✅ |
| ③ | 通信失敗時に2回まで自動で取り直し／失敗しても画面を空にせず赤帯 | ✅ |
| ④ | 楽観的保存（送信を待たずに完了表示・裏で送信・自動再送） | ✅ |

**実測（Chrome・サーバーを1回3秒に固定して計測）**

| | 修正前 | 修正後 |
|---|---|---|
| 開いて予定が出るまで | 通信完了まで真っ白 | **2.3ms / 6ms / 81ms** |
| 保存を押してから画面が戻るまで | 3,000ms超（書込＋全件読み直しの2往復） | **10ms / 13ms** |

#### ⚠️ 二重登録を防ぐ三重の守り（利用者の最重要要件）

社員用の `add` は**画面側がIDを作る**が、社長用の `pres_add` は**サーバーがIDを作る**設計だった。
そのままでは再送前の「もう入っているか」をIDで確認できず、二重登録を防げない。よって:

1. **画面側** — 2回目以降の送信の直前に `pres_list` を読み、同じIDが既にあれば送らない（初回は確認しない＝毎回の保存に待ちを足さないため）
2. **サーバー側（gas.js）** — `pres_add` で同じIDが既にあれば追記せず `{id, duplicate:true}` を返す。1枚目をすり抜けても止まる
3. **自動フォールバック** — サーバーが画面側のIDを採用しなかったと分かった時点で `pres-srv-accepts-id-v1='0'` を端末に記録し、**楽観的保存を自分でやめて従来の同期保存に戻る**

→ **この3枚目があるので、gas.js と president.html のどちらを先に本番へ出しても安全**（順番を気にしなくてよい）。

**社員用と同じ安全装置も全部移植**: 未送信は編集・削除不可（欠陥#5と同じ観点）／5秒→15秒→45秒→2分→5分のバックオフ／10回失敗で赤帯＋手動再送（勝手に捨てない）／閉じても端末に残り次回起動時に自動再送／未送信は⏳表示＋上部に帯。

**社員用に残っている未確認事項をこの画面では解消**: 社員用は「鮮度の帯(fixed top:0)」と「未送信の帯(sticky)」が重なる可能性が§3.6に未確認事項として残っている。社長用は `layoutBanners()` でヘッダーの下に縦に積む方式にし、**実測で重なりゼロを確認**（ヘッダー0〜54px／赤帯54〜83px／未送信帯83〜112px／本文120px〜）。

**触ったファイル**: `president.html`（+446行）/ `gas.js`（+22行・`pres_add` の1か所のみ）。
**`index.html` / `admin.html` / `send-queue.js` / `sync-guard.js` / Cloudflare は一切触っていない。**

**検証結果（すべて再実行可能）**

| 何を | 件数 | 実行方法 |
|---|---|---|
| 既存テスト（壊していないか） | 191件 PASS | `cd cf && npx vitest run` |
| ①②③の検証 | 17件 PASS | 検証台はスクラッチパッド。下記「再作成が必要」参照 |
| ④の検証（二重登録・欠落中心） | 31件 PASS | 同上 |

④の検証で直接確かめた項目: 送信失敗したのに実は届いていた場合→1件のまま／確認の通信までダメでもサーバー側の関所で1件のまま／連続10件保存でサーバー10件ちょうど・ID重複ゼロ／圏外で入れて閉じて開き直す→失われず電波復帰で自動送信／未送信は編集・削除不可／古いサーバーでは楽観化しない。

> ⚠️ **検証台の置き場所**: `_local/verify/verify_pres.js`（①②③・17件）と `_local/verify/verify_pres4.js`（④・31件）。
> 実行は `node _local/verify/verify_pres4.js`（このフォルダを cwd にして）。依存パッケージ不要（Node標準の `vm` で president.html の `<script>` を読む）。
> **⚠️ `_local/` は .gitignore 対象＝git に入らない。このPCにしか無い。** 別PC・別アカウントへ渡すなら `cf/test/` へ移して vitest 化する必要がある。
> **次のセッションで「テストを恒久化するか」を判断すること。**

#### ⚠️ 未解決・要注意（1件）

検証の途中で一度だけ、起動時キャッシュから復元した予定の件数として **202** という説明のつかない数字を観測した。
その後 7件仕込んで7件、10回連続で「画面＝キャッシュ＝サーバー」が常に一致し、**再現しなかった**。
検証スクリプト側の取り違えの可能性が高いが、**無かったことにはしない**。実機確認時の注目点として残す。

#### ✅ リリース記録（2026-08-26 実施・すべて本番に出ている）

| 手順 | 結果 |
|---|---|
| 1. コミット | `e1220eb`（`president.html` / `gas.js` / `引き継ぎ.md`）。※`README.md` と `docs/superpowers/plans/2026-06-15-jp-holidays-calendar.md` の別件変更は**あえて含めていない・未コミットのまま残っている** |
| 2. GitHub Pages へ push | 完了（`726d3df..e1220eb`）。配信確認済み＝新 `president.html` が届き、依存する `send-queue.js` も HTTP200 |
| 3. clasp push | 完了。`_local/gas_push/` から。**プロジェクト直下ではない**（直下でやると index.html までGASへ行くため厳禁） |
| 4. GASデプロイ | **バージョン53（2026/08/26 17:10）**。オーナー `yotei.glorise@gmail.com` で「デプロイを管理」→既存デプロイを編集→新バージョン。**デプロイIDは `AKfycbxp2e…v7OloqcjQ` のまま＝URL不変** |
| 5. 本番の生存確認 | doGet が **HTTP200・`status:"ok"`**・「アクセスが拒否されました」ゼロ。8/21のような停止は起きていない |

⚠️ **§3.7 執筆時の「このフォルダに `.clasp.json` が無い」は誤読だった。**
正しくは**プロジェクト直下には意図的に置いていない**だけで、`_local/gas_push/.clasp.json` に
スクリプトIDが用意されている（手順は `_local/gas_push/README.md` が正典）。次回もそこから push する。

⚠️ **gh の push が自動許可の枠で弾かれることがある**（2026-08-26に発生・2回連続で拒否）。
その場合は利用者が手で `git push origin main` を実行する。gh のアカウントは `akirasp4-lgtm` が
有効なら切替不要。

**デプロイ後の仕様**: **1回目の保存だけは今までどおり待たされ、2回目から一瞬になる**
（サーバーが画面側のIDを採用するかの判定を1回目で行うため）。故障ではない。

#### 🚩 残っていること

- **実機スマホ（iOS Safari・ホーム画面のPWA）での通し確認が未実施。** 特に §3.6 に挙がっている
  機内モードでの通し（登録→未送信のまま残る→リロードしても消えない→電波復帰で自動送信）と、
  Safariタブ／ホーム画面PWAで localStorage が別々になる件。**社員用・社長用の両方で未確認のまま。**
- **実サーバーに対する `pres_add` の二重登録ガードの通し確認が未実施。** 239件の自動テストは
  模擬サーバー相手のもので、**本物のGASへ同じIDを2回送って `duplicate:true` が返るかは未検証**。
  三重の守りの3枚目（自動フォールバック）があるため壊れはしないが、確認すれば1枚目・2枚目の実証になる。
  やる場合は社長予定シートに1件書いて消す作業になるので、**利用者の許可を取ってから**。
- ~~検証台の恒久化の判断が未決~~ → ✅ **2026-08-26 恒久化した**。`cf/test/pres-backend-switch.test.js`
  が president.html の `<script>` を Node の vm に読み込んで vitest から検証する（実装とテストが
  同じコードを見る）。`_local/verify/` の2本は残してあるが、別PC・別アカウントへはこちらが渡る。
- ~~「202」の謎~~ → ✅ **2026-08-26 解明した。異常ではなかった**。202は**社長予定の全件数**
  （D1取り込みでもGASでも202件で一致）。カレンダーに64件しか出ないのは「その月だけ描画している」
  ため。キャッシュは全件を持つので、復元件数が202になるのが正しい挙動だった。

**戻し方**: `president.html` と `gas.js` の2ファイルのみ。`git revert` + push で画面側は約1分で戻る。GAS側は前バージョンへロールバック。

---

### 3.8 社長予定の読み取りをCloudflare(D1)へ移した（2026-08-26・✅本番稼働中）

**きっかけ**: §3.7 の後、利用者から「職人社員用と社長用、同じぐらいのスピードになった?」。
同じ測り方で本番を3回ずつ測ったところ、**開いた瞬間の表示と保存の体感は同等になっていたが、
裏で最新を取ってくる速さだけ約15倍違った**（社員用0.12〜0.35秒＝Cloudflare／社長用2.65〜4.14秒＝GAS）。
両画面とも「最新が届くまで保存させない」安全装置を持つため、この差はそのまま
**「開いてから登録できるまでの待ち時間」**として社長に見えていた。

**結果（本番実測）**

| | 前 | 後 |
|---|---|---|
| 社長用の読み取り | 2.65 / 4.14 / 2.68 秒 | **0.106 / 0.089 / 0.079 秒** |
| 予定が出るまで | 148ms | **55ms**（社員用は361ms＝社長用の方が速い） |

**GASとの突き合わせ**: 202件 対 202件。**欠落0・余分0・中身の食い違い0**（全件比較）。

**作ったもの**（社員用と同じ形をもう一組。社員用のコードは1行も変えていない）
- `cf/schema-president.sql` — `pres_snapshot` / `pres_sync_log`
- `cf/src/pres-sync.js` — 5分ごとのCronでGASの `pres_list` を取り込む
- `cf/src/pres-read.js` — 鮮度15分ガード付き。`pres_list` と同じ形で返す
- `cf/src/index.js` — `POST /api/president` と `POST /api/pres-sync` を追加
- `president.html` — `sync-guard.js` を読み込み、`backend.json` で読み取り先を切替

**⚠️ 次に触る人が絶対に壊してはいけない3点**

1. **社長予定は `sync_log` に1行も書かない。** 社員用の鮮度判定（`read.js:getLastSuccessAt`）は
   `sync_log` の直近の `ok=1` を見るため、混ぜると**社員用の同期が失敗し続けていても
   「新しい」と誤判定**して古いデータを正常として返す。専用の `pres_sync_log` を使う。
2. **`cf/schema.sql` を再適用しない。** 先頭で `DROP TABLE` する破壊的な作りで、本番切替後の今
   流すと社員用のD1が一瞬空になる。社長予定の追加は `cf/schema-president.sql`（`IF NOT EXISTS` のみ）。
3. **再送前の「もう入っているか」の確認はGASのまま。** `president.html:701` の `callGas` を
   D1に変えてはいけない。最大5分古い写しで判定すると**二重登録に直結**する。

**PINの扱い**: 利用者の指示で**変更していない**（`1203`）。Cloudflare側は
`npx wrangler secret put PRES_PIN` で登録（`wrangler.toml` は公開リポジトリに入るため書かない）。
PIN照合はD1に触る前に置いてあり、**不正なリクエストはD1の枠を1件も消費しない**。
未設定なら誰も通さず503（空文字どうしの一致で素通りしない）。
> ⚠️ **PIN `1203` は `gas.js:24` と `president.html` に直書きされ、公開リポジトリに入っている**
> ＝既に外から見える。利用者へ報告済みで、その上で「変えない」判断（2026-08-26）。
> PIN試行の回数制限は**あえて入れていない**：総当たりする必要がそもそも無く、失敗回数を
> D1へ書くと無料枠を削る新しい弱点を作るだけのため。

**戻し方**: `president.html` を1つ前のコミットへ戻して push（約1分）で全部GASに戻る。
D1の `pres_snapshot` / `pres_sync_log` は放置してよい（**社員用は一切参照していない**ため無害）。

**設計書**: `docs/superpowers/specs/2026-08-26-president-d1-design.md`

#### 🔍 Codexレビューで見つけて直した欠陥（3ラウンド）

**本番に出した後にレビューへ出した。順序が逆で、これは私（AI）の手落ち。**
このプロジェクトは過去の変更を毎回Codexと相互レビューしており、そのたびに実際に
重大な欠陥が出ている（Cloudflare移行の計画段階で7件、楽観的保存で6件）。
今回は自動テストだけで本番へ出し、そのあと利用者に指摘されてレビューへ回した。
**結果として、テストを291項目通しても見つからなかった欠陥が3ラウンドで5件出た。**

| R | 指摘 | 何が起きるはずだったか |
|---|---|---|
| 1 | 再送で「もう入っていた」と判定した分岐が `refreshInBackground()` を呼んでいない | GASには入っているのにD1には無い状態が最大5分続き、社長の画面から保存したばかりの予定が消える。作り直して重複させる |
| 1 | `refreshInBackground` が同期の結果を待ってからしか `mark()` しない | 書き込み完了〜同期完了の数秒間はGAS優先が立たず、別タブ・再読込が古いD1を読む |
| 2 | 上の修正で `mark(attemptStartedAt)` としたが、`clear` は「開始より後のmarkは消さない」規則 | **成功しても永久に解除できず、社長用がGAS固定＝遅いまま**。同一ミリ秒だと偶然通るので不定動作 |
| 2 | 未送信を消してから GAS優先を立てていた | 消してから立てるまでにタブが閉じると「未送信も無い・GAS優先も無い」状態が端末に残る |
| 3 | `mark` に世代を渡すと、古い試行が新しい試行の記録を巻き戻す | 並行保存でAの失敗がBの記録を巻き戻し、Bの成功で解除されて未取り込みのD1を信用する |

**教訓（次に触る人へ）**
- `sync-guard.js` の `mark` は **必ず引数なし**で呼ぶ。**「mark（今）→ beginAttempt」の順**にすること。
  逆順や世代渡しをすると、上のR2・R3の欠陥が再発する。
- 素の `Date.now()` で書いたテストは、同一ミリ秒に当たると**偶然通ってしまい欠陥を隠す**。
  `cf/test/pres-backend-switch.test.js` の `makeTickingDate()`（呼ぶたび1ms進む時計）を使うこと。

#### ⚠️ Codexが挙げた未対応3件（共有部品 `sync-guard.js` の性質・社員用も同条件）

**いずれも社員用（index.html / admin.html）がまったく同じ使い方をしており、
2026-08-25から本番稼働中。社長用固有の問題ではない。**共有部品を直すと社員用の
再検証が要るため、**利用者の判断待ちとして未対応のまま残す**。

1. `clear()` が「読む→判定→書く」の3手順で原子的でない。別タブとの競合で新しい mark を消しうる
2. `Date.now()` はミリ秒精度なので、同一ミリ秒の新しい mark を誤って解除しうる
3. `mark(now)` が数値引数を受け付けたままで、「常に引数なし」を実装で強制できていない

**影響の大きさ**: いずれも「GAS優先の記録が失われる → 最大5分古いD1を読む」まで。
Cronが5分ごとに取り込むため上限は5分。社長は通常1台で使うので、複数タブ競合の
条件自体が社員用（40人）より起きにくい。

**直すなら**: `sync-guard.js` に単調増加の世代番号を導入し、clearは世代の完全一致を要求。
mark から引数を削除。**社員用の全テスト＋実機確認をセットで**行うこと。

---

## 4. 重要なコード構造

### gas.js
- `HEADERS` — 19列（車両カラム含む）
- `doPost(e)` — actionで分岐
  - 既存: add / update / delete / archive / summarize / get_sheet / pres_*
  - 追加: `vehicle_res_*` / `cleanup_orphan_jobnos` / `merge_genba`
- `cleanupOrphanJobnos_(ss)` — 休み・倉庫・予定・現場作業以外の工番を一括クリア
- `mergeGenba_(ss, fromName, toName)` — 元請名統合
- `logOperation_` — 操作ログに記録

### index.html / admin.html 共通の重要関数
- `applyWorkTypeLock(p)` — 作業区分ゲート
- `applyYasumiLock(p, clearValues)` — 休み・倉庫モード時のロック
- `populateLocationSelect(p)` / `onLocationSelect(p)` — 現場名セレクト
- `showSimilarSuggest(p)` / `pickSimilarLoc(p, loc)` — 類似現場サジェスト
- `getCompanyVehicles()` — 会社別車両絞り込み

### admin.html マージ機能
- `openMergeGenbaModal()` — モーダル開いてドロップダウン構築
- `updateMergePreview()` — 影響件数プレビュー
- `runMergeGenba()` — 確認後にGAS呼び出し

---

## 5. デプロイ手順

### GitHub Pages
```bash
git add deploy/github-pages/
git commit -m "..."
git push
```
自動デプロイ（数十秒〜数分で反映）

### GAS

> 🚨 **2026-08-21 事故: 別アカウントから `clasp deploy` して本番を約20分停止させた。**
> このWebアプリは appsscript.json が `executeAs: USER_DEPLOYING`＝**「デプロイした人の権限で動く」**設定。
> オーナー以外（例 miscjigyoubu@gmail.com）からデプロイすると、**URLは変わらないのに実行者だけがすり替わり**、
> そのアカウントは権限承認をしていないため**利用者全員に「アクセスが拒否されました」**が返る。
> **バージョンを戻しても直らない**（原因がコードではなく実行者のため）。オーナーからデプロイし直す以外に復旧方法はない。

**■ 鉄則: 最後のデプロイボタンは必ずオーナー `yotei.glorise@gmail.com` が押す。**
**AIや別アカウントは `clasp push`（エディタへ保存するだけ・公開版は変わらない）までしかやらない。**

手順:
1. `clasp push` でコードを送る（安全。利用者への影響ゼロ）
   → 作業フォルダは `_local/gas_push/`（詳細はそこの README.md）
   → プロジェクト直下で clasp push すると index.html 等まで GAS へ送られるので厳禁
2. **オーナーアカウント**で Apps Script エディタを開く
3. 「デプロイ」→「デプロイを管理」→ **既存デプロイ** `AKfycbxp2e…` の鉛筆アイコン
   → バージョンで「新バージョン」→「デプロイ」
   ⚠️「新しいデプロイ」を作るとURLが変わり全員繋がらなくなる。必ず既存を編集する。
4. 切り戻しも同じ画面でバージョンを選び直す（**オーナーアカウントから**）

**アカウント**
- オーナー: `yotei.glorise@gmail.com`（スプレッドシート所有者＝デプロイ担当）
- clasp: `miscjigyoubu@gmail.com`（2026-08-21に編集者権限を付与。push専用）

**注意**: Monaco editorは日本語文字列の直接送信でブロックされることあり。`String.fromCharCode(0x...)` を使う。

### 🛑 スプレッドシートのリンク共有 — 利用者判断で「このまま」（2026-08-29 確定）

> 「それはもうリンク知ってる人は別に見れてもいいのよ。そのファイルは。」

**この話をもう蒸し返さないこと。** 提案済み・却下済み。

状況（2026-08-29に実機で確認した事実）:

| ファイル | 一般のアクセス |
|---|---|
| 運用カレンダー（本番） | リンクを知っている全員 → **閲覧者** |
| GRBS予定表 | リンクを知っている全員 → **編集者** |
| 【バックアップ】3本 | フォルダから継承（閲覧者） |

- 運用カレンダーの中の**職人マスタに単価（給料）**が入っている。**バックアップ3本にも同じ物が入っている。**
- 名前で権限があるのは 運用カレンダー＝2アカウント（オーナー / miscjigyoubu）、GRBS予定表＝2アカウント（オーナー / a.mukai.glorise）だけ。
- **事務員さんは名前で登録されておらず、リンク経由で見ている**（GRBS予定表は**編集**している）。
  → だから公開を切ると**事務員さんが締め出される**。切るなら「先に全員をGmailで登録 → その後に制限」の順が必須。
- 運用カレンダーの共有は**親フォルダ「カレンダー運用フォルダ」から継承**。単独で切ろうとすると
  フォルダごと制限になり、**GRBS予定表も巻き込む**（Googleが警告ダイアログを出す）。
- **アプリ自体は影響を受けない。** GASはオーナー権限で動いている
  （リンク共有が「閲覧のみ」なのに書き込めているのが証拠）。

⚠️ **GRBS予定表は予定管理アプリの管轄外**（コード内に参照なし）。事務員が毎日更新し、統括が閲覧し、
社長へLINE通知が飛ぶファイル。**触らないこと。**

### ⚠️ セキュリティ（その他・未対応）

---

## 6. 既知の課題・残課題

### ⏸️ 次に判断すること（2026-08-21 時点・利用者不在のため保留）

**① 保存を「押した瞬間に完了」に見せるか（未実装・要判断）** → **2026-08-25 利用者判断で実装済み（案A）。詳細・戻し方・未確認事項は §3.6**
今は保存に4.4秒かかる。サーバの返事を待たずに先に「登録しました」と出せば体感は一瞬になるが、
**電波の悪い現場で送信に失敗した場合、本人は登録できたと思って帰ってしまう**リスクがある。
予定表は「誰がどの現場に行くか」を決めるものなので、この誤認は欠員に直結する。
→ **利用者の判断が要る。私（AI）の独断ではやらない。** やる場合は失敗時に赤い全画面警告を出す等の設計とセットで。
→ **2026-08-25 に案A（楽観的表示＋裏送信。送信失敗時は端末に保持し自動再送、10回失敗で手動再送に切替）として実装。§3.6参照。ブランチ `feat/optimistic-save`・未push・実機確認は未実施。**

**② GASから離れるか（設計まで完了・着手は未定）**
- 遅さと不安定さの根本原因は**スプレッドシートをデータベース代わりに使っていること**。
  29行の小さいシートを1枚読むだけでも1.6〜2.6秒かかる＝データ量ではなくGASの起動・往復の問題。
- **2026-08-21→22で訂正された重要事実**（次回の判断に直結するので必読）
  - 2026-08-21 利用者談「全員ウェブアプリからしか使っていない」
  - 2026-08-22 訂正「**実際にシートを見に行っている人が数名いた**」。理由は
    「Excelをいちいちダウンロードしないと入力確認ができないから、シート覗きのほうが早い」。
  - **正しい理解**: 直接**書き換える**人はいない（＝流し込みは一方通行でよい。移行の前提は維持）。
    ただし直接**見る**人はいる。見ているのは月別確認表・会社別集計・月別集計・元請別請求集計あたり。
    操作ログは誰も見ていない。
  - → **2026-08-22に「集計をこの画面で見る」を実装済み**（§3.0e）。これでシートを覗く必要が
    なくなったため、移行してシートが表から見えなくなっても困らない状態を先に作った。
- 想定する形:
  ```
  現場のスマホ → 速いデータベース(0.2秒) → 即反映
                      ↓ 事務員が「集計更新」を押した時だけ流し込む
                スプレッドシート → 既存の集計プログラム(1,500行)はそのまま
                      ↓
                事務タブからExcelダウンロード ← 操作は今と全く同じ
  ```
- **集計エンジン（月別確認表・会社別集計・元請別請求集計・事業部別按分）は書き直さない。**
  移すのは「日報データの読み書き」だけ。
- きんでん請求書は**そもそもこのアプリに入っていない**（ローカルPython運用）ので移行対象外。
- 置き場の候補: **Supabaseの別プロジェクト**（無料枠2つまで＝お客様用と別勘定。知見あり）か
  **Cloudflare Workers + D1**（別会社に分散したい場合。ただし不慣れ）。
  → 利用者が「Supabaseはもう結構使ってる」と言った理由（枠の心配か／分散したいのか）を確認してから決める。**未確認**。
- 実働の見積もり: **2〜4日**（Cloudflareは不慣れなため4〜6日を見込む）。

#### 置き場の決定（2026-08-21 利用者確定）
**Cloudflare Workers + D1 を採用。** 理由は「一箇所（Supabase）に寄せすぎて、そこが落ちたら
売り物も自社の道具も一緒に止まるのが不安」（利用者の言）。今日GASが5回に1回404を返すのを
目の当たりにした直後の判断であり、妥当と判断した。

**料金（2026-08-21 公式ページで確認・無料枠内の見込み）**

| | 無料枠 | 想定使用量 | 余裕 |
|---|---|---|---|
| リクエスト | 10万回/日 | 約1,000〜2,000回/日 | 50〜100倍 |
| 行の読み取り | 500万行/日 | 約42万行/日 | 約12倍 |
| 行の書き込み | 10万行/日 | 約2,000行/日 | 約50倍 |
| 保存容量 | 5GB | 約0.5MB | 1万倍 |

- 社員40人×1日15回起動＋1日200回保存で試算。独自ドメイン不要（*.workers.dev が無料）。
- ⚠️ **無料枠を超えると課金ではなく「停止」する。**予算事故は起きないが、超えたらその日使えない。
  余裕は2桁あるので現実的には起きない想定だが、使用量が見える状態にしておくこと。
- 有料は月5ドル（約750円）から＝リクエスト1,000万回/月。社員が10倍でも月750円で足りる。
- 注意: Workers無料枠は**1回の呼び出しにつきCPU時間10ms**。重い処理を1リクエストに詰めない設計にする。

#### 進捗 — ✅ フェーズ1は完了し、**2026-08-25 から本番稼働中**

> ⚠️ **2026-08-26 全面訂正。** ここには 2026-08-22 夜時点の「Task 4・5 未着手／本番への影響は
> ゼロ／git push もしていない」という記述が残っていたが、**実態と正反対だった**。
> Task 4・5 とも完了して main に入り、**読み取り先は既に Cloudflare に切り替わっている**。
> このまま放置すると「もう終わっている Task 4 を次のセッションが着手し直す」事故になるため書き換えた。

**計画書**: `docs/superpowers/plans/2026-08-22-cloudflare-migration-phase1.md`
**作業台帳（裁定の全記録）**: `.superpowers/sdd/2026-08-22-cloudflare-migration-phase1/progress.md`
実行方式は superpowers:subagent-driven-development（タスクごとに実装担当を立て、毎回レビューを通す）。

| タスク | 状態 |
|---|---|
| Task 1 D1のテーブル定義 | ✅ 完了 |
| Task 2 GASからD1へ取り込む | ✅ 完了 |
| Task 3 読み取りAPI | ✅ 完了（`0330907` までレビュー修正） |
| Task 4 Cloudflareに実物を作る | ✅ 完了（`d677784`「D1・Worker本番構築と全行突き合わせ（GASと完全一致・約23倍高速）」） |
| Task 5 画面に切り替えスイッチ | ✅ 完了（`6f1012f` 切替スイッチ＋自動フォールバック） |
| **本番切替（社員用）** | ✅ **2026-08-25 実施**（`d9c0dbe`「読み取り先をCloudflare D1へ切り替え（backend.json: gas → d1）」） |
| **社長予定もD1へ** | ✅ **2026-08-26 実施**（§3.8）。3.2秒→0.09秒。GASと202件で完全一致 |

**現在の本番の姿（2026-08-26 実測で確認）**

- 本番 `backend.json` は `{"backend":"d1","workerUrl":"https://yotei-api.miscjigyoubu.workers.dev"}`
- **社員用（index.html / admin.html）の読み取りは Cloudflare。** 2,242行を返して **0.12〜0.35秒**（3回測定）
- ~~社長用（president.html）の読み取りは GAS のまま~~ → ✅ **2026-08-26 にCloudflareへ移した（§3.8）。0.079〜0.106秒**
- **書き込みは全画面とも GAS のまま**（`backend.json` の note に明記。D1は読み取り専用の写し）
- 取り込みは Cron で5分ごと。鮮度が古ければ `read.js` が `status:'error'` を返し、画面は自動でGASへ戻る

**D1の持ち方**: 行ごとのテーブルではなく **snapshot テーブルに1行**（GASのcompact応答をJSON文字列で丸ごと）。
理由は原子性（単一文なので中途半端な状態が見えない）と費用（1回の同期＝1行の書き込み。無料枠の0.3%）。
**単価(rate)はD1へ持ち込んでいない**（給料情報を外部に置かないため）。

**下記「⚠️ Task 5 に必ず持ち込む要件（#6の解決策）」の単価表示の件は ✅ 解決済み**（2026-08-26 コードで確認）。
`admin.html` の `openSettings()` が `loadSettingsMembers()` でGASから取り直し、`allMembers` ではなく
専用キャッシュ `settingsMembers` を使って描画する。さらに3回のレビューで、**GASが一部の職人しか
返さない「部分応答」でも、欠けた人だけ単価欄を編集不可にして赤い警告を出す**（`rateOk` を個人単位で
確認）ところまで固めてある。0で実際の給料を上書きする事故は塞がれている。

#### この設計で見つかった計画の欠陥（7件・すべて動かす前に発見）

| # | 内容 | 発見場所 |
|---|---|---|
| 1 | D1への一括投入が件数上限に当たる | 着手前スキャン |
| 2 | 職人マスタの主キーが実データと不一致（同姓別会社が消える） | Task 1 レビュー |
| 3 | 分割投入の途中失敗で中途半端なデータを正解として返す | Task 2 着手時 |
| 4 | 失敗の伝え方が非対称で呼び出し側が書き忘れると壊れる | Task 2 レビュー |
| 5 | 保護されていない例外経路が残り、JSDocの宣言と実装が不一致 | Task 2 再レビュー |
| 6 | **計画の自己矛盾で職人の単価が空欄表示になる（下記）** | Task 3 レビュー |
| 7 | 会社名の空白差で特定の行だけ静かに消える | Task 3 レビュー |

#### ⚠️ Task 5 に必ず持ち込む要件（#6の解決策）

「単価をD1に入れない」と「画面を書き換えず読み取り先だけ差し替える」は**両立しない**。
`admin.html` の `renderSettings`（4046行）は `loadData` と同じ `allMembers` から
`member.rate` を読んで単価欄を描くため、D1へ切り替えると**全職人の単価が空欄表示**になる。
応答は `status:'ok'` なので自動フォールバックも作動しない＝静かに誤情報を出す。

**解決策（確定済み）**: Task 5 で `openSettings()` が職人マスタを **GASから直接**
取り直してから描画する（`action:'get_sheet', sheet:'職人マスタ'`）。設定画面を開いた
ときだけの1回の通信なので日常の速度に影響しない。**backendの値で分岐させず常にGASから取る**
（分岐由来の事故を防ぐ）。これでD1は単価を持たないまま、単価の表示・編集は従来どおり動く。

※ 単価の自動書き戻しは起きない（`updateMemberRate` は単価欄の onchange でのみ発火し、
事業部変更・職人の追加削除は単価に触れない）。ただし事務員が「給料設定が消えた」と
誤解して入力し直す二次被害はありうるため、切り替え前に必ず解決すること。

#### ⚠️ フェーズ2の検討事項に必ず含めること

合言葉(`k`)認証をCloudflare Worker側が見ていない。現在 `CAL_REQUIRE_TOKEN` はOFF運用
なので実害は無いが、**将来ONにするならWorker側にも認証を足さないと恒久的な抜け道になる。**

#### 🎯 最重要の設計要件（2026-08-21 利用者指示）
> **「切り替わったと気が付かないように切り替えたい」**

これを満たすための決め事:
1. **切り替えスイッチを設定ファイル1個にする** — `{"backend":"gas"|"d1"}` を1行書き換えるだけで
   切り替わる。アプリ本体・URL・アイコン・合言葉・localStorageの設定はすべて据え置き。
   **切り戻しも1行・数十秒。**
2. **新しい置き場がエラーを返したら、その場で自動的にGASへ落ちる**（自動フォールバック）。
   利用者にはエラーすら見せない。今日入れた自動リトライと同じ考え方。
3. **段階移行（データが欠ける瞬間を作らない）**
   - 第1段階 影運転: D1を作り全データをコピー。アプリはGASのまま
   - 第2段階 両書き: 保存時にGASとD1の両方へ書く（どちらも常に最新）
   - 第3段階 読みだけ切替: ここで速くなる。**書き込みはまだ両方なので、問題があれば即戻せる**
   - 第4段階 書きも切替: GASは「集計を作る係」として残す
4. **第3段階は利用者1人だけ先に切り替えられるようにする。**数日試してから全員へ広げる。
   職人さんは何も知らないままある日速くなっているだけ、という状態を作る。
5. **元に戻せない一方通行の作業を1つも作らない。**


### 🎯 次にやること（社長案件・2026-08-24 打合せで確定。2026-08-25 記録）

出典（録音の話ごとまとめ）:
`自社用/社長の話/2026-08-24/2026-08-24_話ごとまとめ/02_部隊・責任者ごとに振り分ける.md` /
`自社用/社長の話/2026-08-24/2026-08-24_話ごとまとめ/03_関東支店と大阪のカレンダーを1本にする.md`

**① 関東支店と大阪のカレンダーを1本にまとめる**
- 今は関東支店のカレンダーが別にあり、三ツ間側で入れた予定を**手でグローライズ側にも二重入力**している（セイジさんが電話で「関東支店で動くとき俺らの名前どうしたらいいの」と困っていた案件）。
- 社長の決定：「関東支店のカレンダー」ではなく「**グローライズとしてのカレンダー**」に統合し、**中で関東支店／大阪を切り替えられる**ようにする。メンバーの入れ替え・代表者の選択は利用者側でできるように。
- 副産物：誰が余っているかも見えるようになる。

**② 責任者（部隊長）ごとの振り分け**
- 今は事業部（4つ）を個人に割り振り、代表者が選ばれたときにパーセント按分する形。社長は「もう事業部ごとの振り分けでは足りない」と判断。
- **1部隊・2部隊・3部隊・4部隊**に分け、責任者1人につき「自分の1部隊」＋「もう1班動くときの1班分」を最初から作れるようにする（例：奥田さんの案件で塗装と内装が同時に始まって班が分かれるケース）。
- 詳細画面に**案件名と責任者**（「代表者」→「責任者」に呼称変更）を並べて表示する。
- 未登録メンバー（高田・柳・内村・山下・大和 ほか）の追加も必要。

**③ ②に着手するタイミングで `gas.js` の `add` を冪等にする（案②）を同梱する**
- 理由: ②の実装で `gas.js` を触ることになるので、そのついでに入れる（**デプロイ回数を増やさないため**）。
- 内容: 同じIDが既にあれば `add` を二重登録せず飛ばす。案A（§3.6・クライアント側の存在確認）に加えて**サーバー側でも二重登録を防ぐ二段構え**にする。

### 残課題（依頼があれば）
- **Phase 2（車両予約のカレンダー表示）** — ラインボットからの依頼。Phase 1完了でOKとの指示で保留中
- **Service Worker / オフライン対応** — PWA本格化案として説明済み、未実装
- **起動の遅さ** — 改善プラン提示済み（メリット・デメリット説明）

### 🚧 やりかけ・毎月使う運用（2026-05-30 のセッションから救出）

**① きんでん請求書を事務タブから出す統合＝未完**
- **現状**: 🟡 **「きんでん請求書を事務タブから自分で出せるように」を設計＋実装プラン化まで完了**。チーム調査でGAS方式の核を裏取り（safe_with_mitigations）。**本番実装は未着手**（＝下記ブロッカーのため見切り発車せず）。きんでん請求は**今すぐはローカルPython（依頼ベース）でそのまま出せる**。
- **次の作業場所**: `docs/superpowers/plans/2026-05-30-kinden-jimu-tab-integration.md` の **Task 0（実機検証ゲート）**から。
- ⛔ **ブロッカー**：(1) **clasp が別アカウントに戻り本番(予定管理)へデプロイ不可** → 帰宅後 `clasp login` で予定管理オーナーに入り直しが必要（ブラウザ同意・代行不可）。(2) きんでん機密を公開テストendpointに送る検証はガードレールが正しく拒否。

**② きんでん西 指定請求書ジェネレーター＝完成・毎月使う**
- **現状**: ✅ **きんでん西 指定請求書ジェネレーター 完成（6局登録済み・自動テスト17件PASS）**。きんでん以外は前出の「現場ごと請求」のまま。
- **次の作業場所**: 来月分は局名＋きんでん指示の「当月迄出来高」を渡せば生成できる。請求書の年月の扱いだけ初回提出で要確認（下記）。
- ⚠️ **固定値が局ごとに違う**：**法定保険料率**（F95系＝16.36% / 4G系（アムール等）＝**15.765%**）、**工番の頭**（F95 / **F96**）、工事名・施工場所・注文金額・労務費も各局固有。

> 経緯の全文は [[自社用/予定管理アプリ作成/docs/引き継ぎ_詳細|docs/引き継ぎ_詳細]]。


### 注意点
- iOS Safariは `<datalist>` が出ない → `<select>` を使う
- body の `overflow:hidden` ＋ `.screen{height:100vh;overflow-y:auto}` でタブバー固定（iOS）
- PC表示でセンタリングは max-width:1000px + margin:0 auto を `.screen` に
- 日報の元請変更・現場変更は表記揺れに注意。マージ機能で吸収可能

---

## 7. ファイル一覧（主要）

```
予定管理アプリ作成/
├── 引き継ぎ.md                          ← このファイル
├── ラインボットへの返信.md              ← 車両予約GAS連携の完了報告
├── deploy/
│   ├── gas/
│   │   └── gas.js                        ← GASバックエンド（約1900行）
│   └── github-pages/
│       ├── index.html                    ← 職人用（約2700行）
│       ├── admin.html                    ← 管理者用（約2400行）
│       ├── president.html                ← 社長用（約830行）
│       ├── links.html                    ← 入口
│       └── manifest.json                 ← PWA
```

---

## 8. 直近のコミット履歴（要約）

- `d332b05` — 現場マスタから 1 現場を削除する機能を追加（2026-05-16）
- `3d7fd5d` — 現場名マージ：A/B 選択 → どっちに合わせる？ボタン式UIに変更（2026-05-16）
- `ccc4374` — 現場名マージ機能を追加（表記揺れ統合）（2026-05-16）
- `121cca4` — fix: 完了切替後にページ更新しないと反映されない問題を修正（2026-05-16）
- `a3aba99` — 現場の「完了」フラグ追加：プルダウン非表示／工番は不変（2026-05-16）
- `10c29a3` — アプリ内ヘルプを最新機能で更新（2026-05-14）
- `85b84c9` — CSV廃止→xlsx一本化＋合計セルに SUM 関数を入れる（2026-05-14）
- `31366a8` — fix: 期間指定xlsxで列固定とセル結合が競合するバグを修正（2026-05-14）
- `e0863e6` — 見た目そのまま出力: xlsx エクスポート（全期間 + 期間指定）（2026-05-14）
- `101c7da` — CSV ダウンロードに期間フィルタ＋期間集計CSV を追加（2026-05-14）
- `af2c1bb` — リポジトリ運用ルールを .gitignore に明文化（2026-05-14）
- `48f091e` — gas.js を本番デプロイ状態に同期＋月別確認表ヘッダの日付バグ修正（2026-05-14）
- `3da3c5e` — 現場名選択にリアルタイム検索＋最近使った順ソートを追加（2026-05-14）
- `7dffb8c` — 元請名マージのadmin UI追加
- それ以前 — マージGAS実装、撤去品返却追加、工番ゲート、現場名プルダウン化、社長予定表のレイアウト修正、車両予約GAS連携 等

---

## 9. ユーザー固有の方針メモ

- 「職人が入力ルールを守らない」ので、UI側で**強制的に正しい入力に誘導**する方針
- 既存カレンダー機能には**勝手に手を入れない**（独立ブロックで追加）
- 重要操作は**確認ダイアログ必須**（マージ等は取り消し不可なので特に）
- モバイル（iOS Safari）を最優先のターゲットとする
- 表示優先順: タイトル > 時刻（社長予定表）

---

以上。次のセッションではまずこのファイルを読んでから作業を継続してください。


---

### 3.20 コードレビュー(xhigh)で見つけた12件（2026-08-30 昼）

障害対応の直後にレビューを回した。**自分が障害対応で入れた変更に、新しい不具合が3つ混ざっていた。**

#### 直した（9件）

| | 内容 | なぜ危なかったか |
|---|---|---|
| ① | 社長予定の同期を10分に1回へ | 30分に1回にしたが鮮度ガードは15分。社長画面が毎時半分の時間、GASへ落ちて遅くなっていた（**自分で入れた回帰**） |
| ② | AIの記録を1呼び出し＝1行に | 予約で1行・成功でもう1行INSERTしていたため、**1日200回の上限が実質100回**になっていた |
| ③ | 選択の同期をチップを描く前へ | 日付や会社を変えた直後、**✓が付いているのに「0人」**という食い違いが出ていた |
| ④ | `ai_log` の掃除を追加（:35） | 掃除が無く無制限に増える。上限判定のCOUNTが毎回全行を走査していた |
| ⑤ | AI記録の成功/失敗を同じ行へ | 成功した呼び出しも `ok=0` の行が残り、失敗率が実際の2倍に見えていた |
| ⑥ | 現場管理の日付をエスケープ | 同じ画面の別の場所は `esc()` 済みで不整合。引用符事故は今回すでに3回踏んでいる |
| ⑦ | `goToCalendarDate` の二重描画を解消 | `switchTab('list')` が既に呼ぶのに、もう一度呼んで2,200件超を二度組み立てていた |
| ⑧ | CORSに `X-Alert-Key` を追加 | 画面から鍵付きで呼ぶと preflight で弾かれる |
| ⑨ | `scheduledTime` が無いときの分判定 | 掃除や社長予定が走るかどうかが実行時刻まかせになっていた |

#### 直さなかった（3件・理由あり）

- **掃除が1時間に1回**（その回のCronが落ちると飛ぶ）… 掃除は「◯日より古い行を消す」形なので、
  1回飛んでも次の回がまとめて消す。**害が無いので触らない。**
- **現場に出ない人（内藤・田中（智）・加藤　秀男）の除外がAIの中だけ** … 空き確認のチップには
  今も出るので、**手で選べば予定に入れられる**。画面からも消すかは利用者の判断が要る → **要相談**。
- **`/api/sync`（書き込み側）が無認証** … 読み取り側の `/api/alerts` には鍵を付けたが、
  書き込み側は Origin を詐称すれば誰でも同期を強制できる。鍵を1本増やす作業 → **要相談**。

#### 確認

- テスト **862件PASS**（848→862）。①②③の守りを**1つずつ壊して赤くなること**を確認
- デプロイ後、10分に1回へ戻した回（12:30）と掃除を2つ重ねた回（12:35）が**どちらも `Ok`**。CPU上限の再発なし
- `/api/schedule` status=ok rows=2271 / `/api/alerts` HTTP 200

### 3.21 元請の検索と表記ゆれ直し（2026-08-31）

**利用者の言葉（これが仕様）**

> 「直接入力で入れた元請けを検索できるようにしてほしい」
> 「マージも検索もできないから困る」
> 「グローライズ自社って元請けがあるのにグローライズで入れられてて検索も出来ないし経費精算アプリにもでてこないんだよ」
> **「お前に直せって言えばできるのはわかってるけどそれじゃ事務員が直せない」**

★最後の一言が肝。**AIがデータを直すのではなく、事務員が画面から直せる形にした。**

#### 何が壊れていたか（実データで確認）

元請プルダウンは**元請マスタ（33件）だけ**を出していた。「直接入力する」で打った元請は
予定に残るのにプルダウンへ戻ってこない → 次に同じ元請を入れるとき毎回打ち直し。

実データ：予定に出てくる元請 **42種**／マスタ登録 **33件**／**未登録16件・87行**。
同じ会社が4通りに割れていた：`HSJ` `ＨＳＪ` `株式会社HSJ` `HSJ-KNSI`。

#### 直したこと

| | 内容 |
|---|---|
| ① | 元請プルダウンに**絞り込み欄**（入力・編集の両方）。直接入力ぶんも「マスタ未登録」の見出し付きで出す（最近使った順・件数付き） |
| ② | **全角・半角・ひらがな・カタカナを同じ物として扱う**（「ＨＳＪ」を `hsj` で、「グローライズ自社」を `ぐろ` で引ける） |
| ③ | 事務タブに**「元請名の表記ゆれを直す」カード**。似ている元請を自動で組にして出す |
| ④ | 統一先がマスタ未登録なら**その場で登録まで済ませる** |
| ⑤ | 表記ゆれではない未登録の元請は「元請マスタに登録」ボタン |
| ⑥ | `escAttr()` を追加し option の `value` に使う |

**事務員の画面に出る候補（実データ・4組）**

- ハイテックス(1件・登録済) ／ ハイテックス（北九州）(31件・未登録)
- 株式会社HSJ(3件) ／ ＨＳＪ(2件) ← 芯が同じ
- 株式会社HSJ(3件) ／ HSJ-KNSI(2件) ← 含む関係
- 株式会社エイシン(0件・登録済) ／ エイシン(2件・未登録)

そのほか「元請マスタに登録しますか」に11件（休み・関東支店出社など**元請でない物は登録不要**と画面に明記）。

#### 🔴 Codexレビューで見つかった一番危ない話（会社をまたぐ書き換え）

**同じ元請名を複数の会社が使っていた（実データ）：**

| 元請名 | グローライズ | 他社 |
|---|---|---|
| 不動産 | 1件 | **GRHD 21件** |
| オリエンス | 2件 | **和信カインド 114件** |
| ラーテル | 24件 | **和信カインド 13件** |

従来の `merge_genba` は**会社の列を見ずに全行を書き換える**。
グローライズの画面で「1件」と確認して統一すると、**GRHDの21件・和信カインドの114件まで書き換わる**。
**「和信カインド・ラーテル・GRHDは触らない」という決まりに正面から反していた。**

→ GASに**会社を必ず受け取る新しい入口 `merge_genba_company`** を追加し、画面はそちらだけを呼ぶ。
  - ★**新しい名前にしたのは、GASが古いままだと「不明なaction」で止まる＝何も壊さずに失敗するため。**
  - ★従来の `merge_genba` は残した（admin.htmlの既存機能を壊さないため。**そちらは今も全社を書き換える**）
  - ★現場マスタには会社の列が無いので、他社も使っている元請では触らない

> ✅ **2026-08-31 9:39 デプロイ済み（バージョン66）。**
> オーナー `yotei.glorise@gmail.com` から、既存デプロイ `AKfycbxp2e…` を鉛筆で編集して更新。
> **デプロイIDは変わっていない**（Worker・index.html・admin.html の3か所と一致することを事前確認）。
>
> **通し確認**：存在しない元請名で `merge_genba_company` を呼び、
> `nippoUpdated:0 / masterAction:"from_not_found"` を確認（**1行も変えていない**）。
>
> **既存機能の回帰**：GASの通常読み取り 2,579行・5.3秒／`/api/schedule` 200／`/api/alerts` 200／
> 画面側も `gfixMerge` が新しい入口を呼ぶことを実機で確認。

#### そのほかCodexが見つけて直したこと

- **古い画面（通信できずキャッシュ表示中）からでも統一できた** → `dataLoadOk` を見る
- **ボタンの向きが逆に読めた**（「→ これに統一」なのに消えるのは押した行）→「この名前をやめる」に変え、残す行は押せなくした
- **通信中に会社を切り替えると別会社へ登録していた** → 押した時点の会社を握って離さない
- **似ていない元請が数珠つなぎで1組になった**（大阪電設／大阪電設工業／電設工業）→ 芯が同じ物だけ1組、含む関係は2つずつ
- **index.html の `clearForm` だけ絞り込みを消していなかった**

#### ⚠️ まだ直していない（要判断）

1. **権限**：`index.html` は `?mode=admin` を付けるだけで事務タブが出る（PIN無し）。GAS側にも管理者判定が無い。
   **事務員が直せることを優先した設計**だが、鍵を掛けるかは利用者判断。※誰が統一したかはGASの操作ログに残る
2. **GASのマージは途中で失敗すると部分的に書き換わったまま戻せない**（従来から）
3. **アーカイブにしか無い元請は候補に出ない**（GASが日報しか返さないため）
4. **`esc()` は引用符を逃がさない**。`value="${esc(名前)}"` の形が index 13か所・admin 29か所ある。
   今回は新しい2か所だけ `escAttr()` にした。全体を直すには `onclick="fn('${esc(x)}')"` の**11か所**を
   先にイベント委譲へ直す必要がある（`esc()` が `'` を `&#39;` にすると実体参照が先に戻ってJSが壊れる）

#### 🔎 「経費精算アプリにも出てこない」の調査結果（別アプリの話）

`Drive/Claude/自社用/経費生産/` を調べた結果、**原因は予定管理アプリ側ではない**：

- **元請マスタは共有されている。** 経費アプリは自前のマスタを持たず、
  Cloudflare Worker 経由で**予定管理アプリのGASから毎回取得**している。
  → **こちらでマスタに登録すれば、経費アプリのプルダウンにも出る**（今回の⑤が効く）
- **社長用画面（`president.html`）には元請・現場の入力欄がそもそも無い**（`motouke:''` 固定）。
  代わりに「会社（6法人の按分）」があり、その1つが**法人名としての「グローライズ」**。
  → 社長が入れた経費は元請が空欄のまま。**「グローライズ自社なのにグローライズ」はこれ**
- **経費アプリには過去データを元請名で検索する機能自体が無い**

**→ 経費アプリ側の改修は別件。管轄外なので手を付けていない。**

#### 確認

- テスト **897 → 958**。実データ2,604行の本番画面で実際に打って確認
  （`ぐろ`→グローライズ自社／`hsj`→ＨＳＪ含む3件／表記ゆれ4組／残す行のボタンは押せない）
- 守り（会社チェック・元請マスタの他社除外・新しいaction・古い画面からの実行止め・
  escAttr・二重押し止め・数珠つなぎ防止・ひらがな対応）を**1つずつ壊して赤くなること**を確認。
  うち2件は最初赤くならず、試験を作り直した
- ⚠️ **反映直後は古い画面がキャッシュで残る**（実機で踏んだ）。事務員に渡すときは一度再読込を頼むこと

### 3.22 🚨本番障害ふたたび: CronがCPU上限で落ちた（2026-08-31 未明）

**00:28 JST を最後に取り込みが停止。5分ごとのCronが毎回落ちていた。**
前日の分割（1回1仕事）で一度は直ったが、日報が 2,415→2,541→**2,571行**と増えて、
**取り込み1つだけでも上限に届いた**。放置すれば朝6時のアラートがまた0通になるところだった。

```
"*/5 * * * *" @ 2026/8/31 1:05:04 - Exceeded CPU Limit
```

⚠️ **CPU上限で落ちた回は `sync_log` に1行も残らない。** ログが飛んでいたら疑うこと。

#### 何にCPUを使っていたか

毎回この4つをやっていた：①784KBのJSONを解析 ②中身を組み直す
③もう一度JSON文字列にする ④1.5MB相当を**2回**バイト列に変換。

**ところが実測すると、GASの応答は同じ内容なら1バイトも変わらない**（2回取って完全一致を確認）。
そして `sync_log` を見ると、ほぼ毎回「変更なし」。**①〜④はほとんどの回で丸ごと無駄だった。**

#### 直したこと

- 生の応答をそのままハッシュし、前回と同じなら①〜④を全部やめる
  （`snapshot` に **`raw_hash`** 列を追加。NULLなら省かない＝安全側）
- **「生は違って見えたが組み直したら同じ」回にも `raw_hash` を控える**
  ★これが無いと `raw_hash` は「中身が変わった回」にしか入らず、予定が動かない期間は
  永久に省けない。**直した直後の本番が実際にその状態だった**（1回目の修正が効いていなかった）
- バイト数の測定とハッシュで2回やっていたバイト列変換を1回に
- JSONでない応答（GASのエラーページ）は解析せず、やり直しの対象にする

#### 確認

本番で「変更なし（前回と同じ応答のため取り込みを省きました）」を確認。
Cron **6回連続 Ok**。`/api/schedule` ok 2,271件・`/api/alerts` 200 に復旧。

#### ⚠️ 次に同じことが起きたら

データはこれからも増える。**次は「省けない回」（実際に予定が変わった回）が上限に当たる。**
そのときの選択肢：
1. GAS側で「変わった行だけ」返すようにする（作りが大きい）
2. **Cloudflareの有料プラン（$5＝約750円/月）**。CPU上限が10ms→30秒になり、この種の障害が根絶する
   ← お金の話なので利用者判断。**まだ相談していない**

---

### 3.23 🔴日当（単価）が公開状態だった／窓口から外した（2026-08-31）

#### 何が起きていたか（実機で確認）

アプリの画面は GitHub Pages で公開されており、**ソースを表示すると台帳の窓口URLが1行そのまま見える**
（`const GAS_URL='...'`）。そのURLをブラウザのアドレス欄に貼るだけで、
**合言葉も暗証番号も無しに**こう返っていた:

| | |
|---|---|
| 予定 | **2,579件**（メモの電話番号・住所つき） |
| 職人マスタ | **62人** ← **うち45人に日当（単価）** |
| 資格マスタ | 284件 |

さらに**保管場所（GitHub）自体が公開設定**で、`raw.githubusercontent.com` から誰でも
`index.html` を読める。「URLが漏れたら」ではなく**すでに公開されている**状態だった。

⚠️ **画面側は2026-06-11に塞いであった**（現場画面のCSVから職人マスタを除外）。
**窓口の裏側だけ塞ぎ忘れ**ていた。

#### 直したこと（バージョン67）

`doGet` の職人マスタの組み立てから `rate` を外した。**免許番号と同じ扱い**
（`projectQualifications_` の「★免許番号は引き続き出さない（個人情報）」と同じ考え方）。

**鍵を掛けるのではなく、そもそも送らない。** 送られないものはソースを読んでも取れない。

#### ⚠️ 合言葉（`CAL_REQUIRE_TOKEN`）は解決にならない — 利用者の指摘で確定

> 「その合言葉どこに入れるの？ソース表示したら出てしまうもんやのに。」

**そのとおり。** 合言葉を書く先は公開されるソースなので、
**URLを見つけた人は同じソースの中で合言葉も見つけている。** 提案として筋が悪かったので取り下げた。
（`cf/wrangler.toml` のコメントにも同じ結論が既に書いてあった）

なお実測で **`CAL_REQUIRE_TOKEN` は本番で有効になっていない**（合言葉なしで `get_history` が通った）。

#### ⚠️ 塞げていない道（正直に）

**単価が取れる道はもう1本ある: POST の `get_sheet('職人マスタ')`。**
管理画面の単価設定（`admin.html:6771` の `settingsMembers`）がこれを使っているので消せない。
合言葉なしで62行取れることも確認済み。

塞げたのは**「URLを貼るだけで見える」という一番うっかりしやすい道だけ**。
`get_sheet` は命令文を組み立てて送る必要があり、右クリックとコピペではできない。

| | 対策前 | 対策後 |
|---|---|---|
| URLを貼る | 🔴 日当が見える | ✅ 見えない |
| 命令文を送る | 🔴 見える | 🔴 **見える（変わらず）** |

**完全に塞ぐにはログインの仕組みが要る。→ 権限の設計（依頼10）とまとめて Phase 5 で検討。**

#### 確認したこと

- テスト **958 → 963**。`cf/test/gas-rate-leak.test.js` を新設し、**直す前に赤くなること**を確認
- 窓口から `rate` が消え、`name/company/division/butai/active` は残っている（実機）
- 取り込み ok・`/api/schedule` 200・`/api/alerts` 200
- 職人用の画面：予定2,612件・職人62人・資格284件・現場190件、**エラーなし**、`rate` を含まない
- `get_sheet('職人マスタ')` は 62行・単価45人分が取れる＝**管理画面の単価設定は今までどおり**

#### 未対応（別途判断）

- **保管場所（GitHub）が公開設定。** 非公開にすると本番が止まる可能性があり要調査
- `get_sheet('職人マスタ')` の道
- 職人マスタの削除だけ変更履歴が残らない（`gas.js:1280-1292`）
- アーカイブ移動は会社を受け取らず全社まとめて動かす

### 3.24 Phase 1: 案件ごとの「必要人員条件」を持たせた（2026-08-31・台帳側 完了）

社長指示 §2「今回の最優先改修 A」。**この段階は「入れ物」だけ。判定は Phase 2 以降。**

#### 現場マスタ 12列 → 18列（バージョン68）

拠点(11)・ステータス(12)と同じやり方で末尾へ。**既存12列は1ミリも動かしていない。**

| 列 | 名前 | 未入力のとき窓口が返す値 |
|---|---|---|
| 13 | 必要人数 | **`null`**（＝条件未登録） |
| 14 | 必要資格 | `[]` |
| 15 | 必要経験 | `''` |
| 16 | 現場住所 | `''` |
| 17 | 開始時間 | `''` |
| 18 | 終了時間 | `''` |

#### ★「0」を返してはいけない

`normalizeNeedCount_` は空欄・`0`・マイナス・非数値をすべて **`null`** にする。
**「0人必要」と「未登録」は別物**で、0にすると Phase 2 の判定が
「常に足りている」と誤って判断する。

#### ★必要資格は資格マスタの資格名と1文字も違ってはいけない

`normalizeNeedQuals_` が `、` `,` `，` 区切りを配列にし、前後の空白を落とす。
資格名は**105種類**あり、「高所作業車」と「高所作業認定」のように割れると永久に一致しない。
**画面は打ち込みではなく一覧から選ばせること**（元請名で起きた表記ゆれと同じ事故になる）。

#### ⚠️ 触ったのは gas.js の2か所だけ（当初の報告を訂正）

当初「Workerを先に反映しないと同期が全停止する／4か所セットで直す」と報告したが**誤り**。

- `cf/src/sync.js` の `OPTIONAL_HEADERS` が拒否するのは**日報データの列だけ**。
  現場マスタは `validateGasPayload` が「配列であること」しか見ない
- Cloudflare側も画面のキャッシュも `jobsites` を**丸ごとコピー**（書き足し不要）

→ **現場マスタに列を足すのは、日報データに足すより格段に安全。** 次も同じ手が使える。

#### ⚠️ `get_sheet` では列が広がらない

`handleGetSheet_` は `ss.getSheetByName()` で**素で読むだけ**。
`getOrCreateJobSiteSheet_` を通らないので列は作られない。
**`doGet` を1回叩くと列ができる**（実際にそれで18列になった）。

#### 確認したこと

- **バックアップを取ってから反映**（`【バックアップ】運用カレンダー_Phase1直前_20260831`）
- 現場マスタ **18列**・既存12列の名前と順番は変わらず・**190件のまま**
- 画面に6項目が届く。未入力は `null` / `[]` / `''`（**0にならない**）
- 既存の導出が生きている：**ステータス 施工中183/完工7**・完了フラグ・拠点・請求方式は今までどおり
- 予定2,612件・取り込みok・`/api/schedule` 200・`/api/alerts` 200
- **日当は0人分のまま**（§3.23の対策が生きている）
- テスト **963 → 985**。守り4つを1つずつ壊して赤くなることを確認

#### 次（Phase 1の残り）

**入力画面がまだ無い。** 管理画面の現場管理へ「人員条件」の入力欄を足す。
必要資格は**一覧から選ぶ形**にすること。

### 3.25 Phase 1: 保存の入口と入力画面（2026-08-31・Phase 1 完了）

#### 保存の入口（GAS `set_site_needs`・バージョン69）

`set_site_status` を手本に、6項目まとめて保存する入口を足した。

- **13〜18列目だけ**を書く。既存12列は1セルも触らない
- **空欄を送れば「未登録」に戻せる**（消せないと入力ミスが直せない）
- **送っていない項目は触らない**（`undefined` は素通り）
- **変更履歴に項目ごとの差分**を残す（社長指示14）。
  **履歴が書けなければ現場マスタを1セルも触らずエラー**（削除・編集と同じ順序）
- 中身が変わっていなければ履歴を増やさない
- 危険な操作の一覧に入れてロックを強く取る

#### 入力画面（`admin.html` の現場ごとの詳細）

工番修正の画面（現場ごとの詳細）に「👷 人員条件」を追加。

- **必要資格は打ち込みではなく `<select>` から選ぶ。**
  資格名は105種類あり「高所作業車」と「高所作業認定」のように割れると永久に一致しない
- **資格名をHTMLに埋めず `data-quals` に持たせる**（引用符事故よけ）
- **属性値は `escAttr`**（`esc` は引用符を逃がさない）
- **入力は任意。未入力は「未登録」と明示**（`required` を付けない＝既存運用を止めない）

#### ⚠️ デプロイ画面での取り違え（2回踏んだ）

**バージョンを選び直したつもりが古い版のままだったことが2回あった。**
1回目は「バージョン65」、2回目は「バージョン67」。**押していたら本番が巻き戻っていた。**

**必ず押す直前に拡大して「新バージョン」の文字を目で確認すること。**
座標クリックは不安定なので、**要素を指定して押す**方が確実（`find` → `ref` で押す）。

#### テスト 985 → 1,007

`phase1-save-needs.test.js` を新設し、**偽のスプレッドシートで実際に読み書き**させている。
列番号べた書きの取り違え（13列目のつもりが12列目）は、ソースの文字列検査では捕まらない。

守りを1つずつ壊して赤くなることを確認:
書き込む列をずらす／履歴を書く前に原本を触る／送っていない項目を上書きする／
資格を打ち込み欄にする／資格名を onclick に埋める／`escAttr` を `esc` に戻す／「未登録」をやめる

**★うち2件は最初赤くならなかった:**
- 資格の欄は **id の存在しか見ておらず、`<input>` に差し替えても素通り**した
  → `<select>` であることを見るテストに直した
- 履歴の壊し方が実装とずれていた（`appendRow` ではなく `getRange().setValues()`）
  → 壊す場所を実装に合わせた

#### 確認

- 新しい入口が本番で生きている（存在しない現場・項目なしの2通りで確認。**データは1件も変えていない**）
- 予定2,582件・現場190件・職人62人・資格284件、**ステータス 施工中183/完工7**
- **日当は0人分のまま**、6項目が画面まで届く
- 取り込み ok・`/api/schedule` 200・`/api/alerts` 200

#### 次（Phase 2）

**判定はまだ変えていない。** 人員不足・資格不足の正式判定、重複の3段階分けへ。

## 関連

- [[自社用/社長の話/2026-08-24/2026-08-24_話ごとまとめ/00_目次と読み方|2026-08-24 社長打合せ 話ごとまとめ]] — **前半（2〜13分）がこのアプリへの改修指示そのもの。**[01]表示を速くする3段階・[02]部隊/責任者ごとに振り分ける・[03]関東支店と大阪のカレンダーを1本にする
- [[自社用/社長の話/引き継ぎ|社長の話]] — 社長との打合せ録音の原本置き場

### 3.26 Phase 2: 判定を「正式」「参考」「判定できない」に分けた（2026-08-31）

社長指示 §3 §4 §6 §9。**必要人数はまだ1件も入力されていないので、今の通知の中身は1件も変わっていない。**

#### 重複を3段階にした（§6）

社長指示は「250件を検知しない仕様にはしないでください。検知を消すのではなく、
検知した上で通知レベルを変えること。**『47件が本物』と断定する実装・表示にしないこと**」。

- 拾う側（`countsForOverlap`）を広くした。今まで捨てていた事務所・現調・移動も**いったん拾う**
- 拾ったうえで `conflictSeverity` が強さを決める

| 段階 | 意味 | 実データ |
|---|---|---|
| 高優先 | 同じ日に**別々の現場作業**。物理的に両立しない | 49件 |
| 要確認 | 現場作業を含むが片方は別業務／時刻が入っていない | 32件 |
| 参考 | 現場作業を含まない（会議＋社内予定など） | 76件 |

**`findConflicts` の既定は `'high'`。** 呼び出し側を1か所も変えていないので、
カレンダーのバナーも保存時の確認も**今までどおり49件のまま**。全部欲しいときだけ `minSeverity:'info'`。

⚠️ 2026-08-27 のコメントに「★物理的に不可能＝これだけが本物」と書いてあった。
社長指示と正面から食い違うので書き直した。テストが `これだけが本物` を禁止している。

#### 人が足りない（§3）

- **第1優先＝正式判定**: 現場の `必要人数` と、その日入っている実人数を比べる（`shortOfficial`）
- **第2優先＝参考判定**: いつもの人数との比較（`shortStaff`。今までの実績ベースのまま）
- **必要人数を入れた現場は参考判定から外す**。同じ現場を2つの理屈で二重に出さない
- 文面も分けた。参考の方は「**いつもより人が少ない可能性があります（参考）**」と断定しない

⚠️ 正直に書いた限界（コードのコメントにも残した）:
- **その日1人も入っていない現場は判定しない。** 現場マスタに「いつからいつまで動く現場か」の
  欄が無く、休みの日と人が付いていない日を区別できない。全部出すと190現場ぶん毎朝鳴る
- **昼勤と夜勤を分けない。** 必要人数の欄は現場に1つで交替制を表せない。
  分けると「昼2＋夜2で必要4」の現場が両方とも不足と誤報する

#### 資格が足りない（§4 §9）

必要資格と、その日入っている人の資格を照らす。状態は5つ:

| 状態 | 意味 |
|---|---|
| ok | 有効な資格を持つ人がいる（1人いれば足りている） |
| soon | 期限が近い |
| expired | 期限が切れている |
| missing | **全員ぶん登録があって**、誰も持っていない＝正式に不足 |
| unknown | 資格情報が未登録／有効期限が読めない＝**判定できない** |

**★一番大事なのは missing と unknown を混ぜないこと。**
資格マスタに1行でも載っているのは **62人中22人**（実測）。混ぜると、
資格をまだ入力していないだけの40人が毎朝「資格不足」と名指しされる。

実データに仮の必要資格を入れて試すと **14件中11件が unknown（判定できない）** に落ちた。想定どおり。

通知の見出しも分けた:
- `■ 現場に必要な資格が足りていません N件`（正式判定）
- `■ 資格を確かめられませんでした N件（判定できません）`

#### ついでに塞いだ穴

既存の⑦資格通知が資格を**氏名だけで引いていた**ので、
和信カインドの同姓の人の資格がグローライズの通知に混ざり得た（§0 他社を巻き込まない違反）。
`qualsByPerson(qualifications, company)` で会社を先に絞るようにした。

#### テスト 1,025 → 1,060

`phase2-conflict-severity.test.js`（18件）と `phase2-official.test.js`（35件）を新設。
守りを1つずつ壊して赤くなることを確認済み:
資格情報が無い人を断定する／正式と参考を二重に出す／資格の会社境界を外す

#### まだ反映していないもの

- `cf/src/` の変更は **`wrangler deploy` が必要**（毎朝の通知に効く）
- `index.html` / `admin.html` は **push すれば GitHub Pages に出る**
- **画面側にはまだ正式判定を出していない**（Phase 5 の管理画面統合でやる）

### 3.27 職人を消しても記録が残っていなかった穴（2026-08-31・修正済み）

社長指示 §0「変更履歴を維持する」／依頼書の必須仕様⑦。

`remove_member`（職人マスタから人を消す）が、**変更履歴も操作ログも1行も書いていなかった。**
事業部・既定部隊・有効・単価の変更は全部記録しているのに、
**一番取り返しがつかない「消す」だけが無記録**だった。
誰がいつ誰を消したのか、後から誰にも分からない状態だった。

直したこと:

- **消す前に履歴を書く。書けなければ1人も消さずエラーを返す**
  （削除・編集・人員条件の保存と同じ順序。履歴が命綱なので先に確保する）
- 消える前の 氏名・会社・事業部・既定部隊・有効 を残す（空欄の項目は書かない）
- 操作ログにも1行残す
- **★単価（日当）は履歴に書かない。** 変更履歴は画面から読めるので、
  せっかく窓口から外した日当が履歴経由で出てしまう

テスト `gas-remove-member-history.test.js`（10件）。偽の職人マスタで実際に消させている。
壊して赤くなることを確認: 履歴を書かない／履歴が書けなくても消す／単価を履歴に書く

⚠️ **GASのデプロイが必要**（バージョン70になる）。まだ本番は旧のまま。

### 3.28 Phase 3: 現場の条件で候補を絞れるようにした（2026-08-31）

社長指示 §7「AI人員配置提案は、プログラム側で条件判定を行い、
AIには順位付けと理由説明だけをさせる。氏名の記号化は維持する」

Phase 1 で現場マスタに入れた条件（必要人数・必要資格・必要経験・住所・時間）を、
**空き確認の画面で実際に人選びに使えるようにした。**

#### 画面の動き

「空き確認」に **現場から条件を読む（任意）** を追加。現場を選ぶと:

- その現場の条件が表で出る（**未登録は「未登録」とはっきり出す**）
- **必要11人 / 選択1人 → あと10人** が上に出る
- 必要資格で候補が3つに分かれる:
  - **空いている N人（必要な資格を持っている人）**
  - **資格が分からない N人**（資格マスタに登録がありません。持っていないとは限りません）
  - **資格が足りない N人**（誰にどの資格が無いかを書く）
- 必要経験は文章なので自動で絞らない。「目で確かめてください」と出す

**現場を選ばなければ、今までと1ミリも変わらない。**

#### ★資格が分からない人を候補から消さない

資格マスタに1行でも載っているのは **62人中22人**。
「資格なし」で消すと、まだ入力していない40人が永久に候補へ出てこない。
§0「不明な資格を勝手に補完しない」／§4 と同じ考え方。

#### ⚠️ 途中で見つけて直した穴（危なかった）

`availSyncPicked(free)` は「渡した一覧に無い人」を選択から外す。
**資格で絞った後の一覧を渡していたので、「資格が分からない」欄から選んだ人が
次の描画で黙って消えていた。** 絞る前の `freeAll` を渡すように直した。
（選べるかどうかは「その日空いているか」で決まる。資格の分けは見せ方の問題）
既存テスト `ai-candidates.test.js` に、この約束を守らせる行を足した。

#### テスト 1,070 → 1,100

`phase7-assign.test.js`（28件）を新設。`siteNeedOf` / `screenByQuals` / `assignProgress`。
守りを6通り壊して赤くなることを確認:
資格未登録の人を「足りない」に落とす／見つからない現場に空の条件を返す／
必要人数0を通す／満たさない人を黙って捨てる／他社の資格を使う／2画面をズラす

#### 実機で確認（Chrome・ローカルの写しで）

本番のシートは1セルも触らず、画面のメモリ上にだけ条件を入れて動かした。
予定2,615件・現場190件・資格284件の実データで:

- 現場プルダウン223件、条件つきの現場が👷付きで一番上
- 「必要11人 / 選択1人 → あと10人」
- 空いている2人（資格あり）／資格が分からない11人（**消えていない**）
- 「資格が分からない」欄から選んだ人が、描き直しても✓のまま残る
- 「現場を選ばない」に戻すと 空いている13人（元どおり）
- コンソールのエラー 0件

### 3.29 Codexレビューの指摘を潰した（2026-08-31）

Phase 2 のコミット `9a13320` をCodexにレビューさせた。**再現コード付きで10件**返ってきた。

#### ★#7【重大・実データの86%に効く】期限のない資格を「読めない」にしていた

資格284行のうち **245行（86%）が有効期限が空**
（ゴンドラ特別教育・足場の組立て等・職長・安全衛生責任者など、そもそも切れない資格）。
`qualStatus('')` は `'none'`（期限なし）を返すのに、
私のコードは `ok/soon/expired` 以外を全部「読めない」に落としていた。
**＝資格の照合がほぼ丸ごと働いていなかった。**

直した後、実データの試算で「判定できない」が **11件 → 5件** に減った。

#### #1【P1】現場かどうかを、同じ現場の最初の1行だけで決めていた

```
① 移動      / 元請A・現場A
② 現場作業  / 元請A・現場A
③ 現場作業  / 元請B・現場B
```
この順で並ぶと現場Aが「現場作業ではない」ままになり、
**別々の2現場の重なりが高優先から落ちて、画面の警告から消えていた**（変更前は出ていた）。
→ 1行でも現場作業があれば、その現場は現場作業として数える。
画面2つとWorkerの3か所を同じに直した。

⚠️ 私の実データ照合（変更前後で49組/9組が完全一致）では **見つからなかった**。
その並び順が実データに無かったため。**「実データで一致した」は「穴が無い」の証明にならない。**

#### #5 期限切れの人が1人いると、資格情報が無い人がいても断定していた

A（期限切れ）とB（資格情報が1行も無い）がいる現場で `expired` と断定していた。
Bが持っているかもしれない。**断定できない方を先に見る**よう順番を変えた。

#### #4 必要資格だけ入れても動かなかった

「必要人数が無ければ何もしない」にしていたので、必要資格だけ入れた現場が素通りしていた。
画面では別々に入れられるのに。→ どちらか入っていれば判定する。

#### #8 全社で見ると他社の同姓の人の資格が使えた

`/api/alerts` は company を省くと **既定で「全社」** で動く。
資格の索引が氏名だけの鍵だったので、和信カインドの江頭さんの資格で
グローライズの江頭さんが「有資格」になり得た。
→ 索引の鍵を **会社＋氏名** にし、配置者も `{name, company}` で持つようにした。

#### #2 会社が空欄の資格の行が丸ごと落ちていた

会社の列が無い／セルが空の行は「どの会社か分からない」であって「他社の行」ではない。
落とすと期限のお知らせが黙って消える。実データでは今0件だが、1行足された瞬間に消える。

#### #10 `siteQualCheck` に null を渡すと落ちた（部品として使うとき）

#### 直さずに「限界」として書き残したもの

- **#3 現場マスタに会社の列が無い。** 同じ（元請名＋現場名）が2社にあると条件を取り違える。
  実データでは **0件**（現場マスタの鍵の重複も0件）。直すには列を足すしかない（Phase 5で検討）
- **#6 「2人以上が資格を持っていること」を表せない。** 1人いれば足りている扱い。
  画面も同じ資格を2つ選べない。今の運用でこれが要るかは未確認
- **#9 「移動」だけでその現場に入っている人は頭数に入らない。** 実データで14人日。
  現場作業の行もある人は、そちらで数えられている

#### テスト 1,101 → 1,114

7通り壊して全部赤くなることを確認済み。
**Codexが挙げた再現手順（移動→現場作業→別現場の並び）をそのままテストにした。**
